# ARBEITSPLAN — Q9

*English version: [ARBEITSPLAN.md](ARBEITSPLAN.md)*

Claudias Arbeitsplan: nächste Schritte, aktueller Status, geparkte Probleme.
Wird bei jeder Arbeitssession aktualisiert (feiner granular als context.txt).

**Status-Legende**:
💡 Vorschlag · 💤 Idle · 🟢 Ready · 🔄 in Arbeit · ✅ fertig · ⛔ blockiert/geparkt

| Status | Bedeutung |
|--------|-----------|
| 💡 Vorschlag | von Claudia geplant, **noch nicht besprochen** — wird nicht bearbeitet, bis Andreas entscheidet (→ Idle, Ready oder gestrichen) |
| 💤 Idle | gültiger Punkt, wird irgendwann bearbeitet — aber nicht jetzt |
| 🟢 Ready | freigegeben — darf vom eingetragenen Bearbeiter angegangen werden |
| 🔄 in Arbeit | wird gerade bearbeitet (immer nur EIN Schritt) |
| ✅ fertig | abgeschlossen, getestet, committet |
| ⛔ blockiert | geparkt, Problem unter "Geparkt" dokumentiert |

**Archiv:** Volltext-Details zu allen ✅-Schritten stehen in `ARBEITSPLAN_ARCHIV.md` (wird von der automatisierten Routine nicht gelesen).

**Wer**: Claudia (Claude Code) · Codex (geplant) · Andreas · — (offen, wird bei Freigabe festgelegt)

## Arbeitsregeln

1. Immer nur EIN Schritt 🔄 gleichzeitig.
2. **Timebox bei Problemen**: Wenn ein Schritt richtig klemmt → nicht verbeißen,
   sondern ⛔ setzen, Problem unter "Geparkt" dokumentieren, mit Andreas
   besprechen und solange am nächsten unabhängigen Schritt weiterarbeiten.
3. Nach jedem abgeschlossenen Schritt: Status hier aktualisieren,
   am Session-Ende zusätzlich context.txt.
4. **Sofort sichern (Limit-Schutz)**: Nach jedem abgeschlossenen Schritt und
   nach jeder Änderung an ARBEITSPLAN.md/context.txt sofort `git commit` +
   `git push` — nicht bis zum Session-Ende warten. Session-Limits kommen
   ohne Vorwarnung; alles was nur lokal oder nur im Chat existiert, kann
   verloren gehen. WIP-Commits sind ausdrücklich erlaubt.
5. **Nur 🟢 Ready wird bearbeitet** — 💡 Vorschläge und 💤 Idle bleiben liegen,
   bis Andreas sie freigibt.

---

## Nächste Arbeitsschritte

### Phase 0 — Fundament

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 0.1 | Toolchain installieren: w64devkit 2.8.0 (gcc 16.1.0 + make) + emsdk latest | ✅ | Claudia | portabel, ohne Admin; Doku: docs/TOOLCHAIN.md. → Details: ARBEITSPLAN_ARCHIV.md |
| 0.2 | `src/hal/q9_hal.h` ausformulieren (Konsole, Block-Device, Timer, Target-Info) | ✅ | Claudia | yield gestrichen: Host treibt q9_kernel_step(). → Details: ARBEITSPLAN_ARCHIV.md |
| 0.3 | Kernel-Minimalgerüst: `src/kernel/kernel.c`, Banner, Echo-Loop über HAL | ✅ | Claudia | nicht-blockierendes Step-Design. → Details: ARBEITSPLAN_ARCHIV.md |
| 0.4 | HAL `native/`: PC-Build (conio, Disk-Image-Stub), Makefile-Target `native` | ✅ | Claudia | Windows-only (conio); POSIX-Variante später. → Details: ARBEITSPLAN_ARCHIV.md |
| 0.5 | HAL `wasm/`: Emscripten-Build, `web/index.html` mit xterm.js, Makefile-Target `wasm` | ✅ | Claudia | q9.wasm = 1,3 KB 😄. → Details: ARBEITSPLAN_ARCHIV.md |
| 0.6 | Erster Test: `test/01_test_boot.py` (nativ) + Browser-Boot verifiziert | ✅ | Claudia | PASS; Bugfix: Konsole ist UTF-8-Bytestrom (C1-Falle). → Details: ARBEITSPLAN_ARCHIV.md |
| 0.7 | Git-Repo initialisieren + erster Commit + GitHub | ✅ | Claudia | github.com/foellmy51/Q9 (privat). → Details: ARBEITSPLAN_ARCHIV.md |

### Phase 1 — Kernel-Basis

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 1.1 | Syscall-Design entschieden (E7): OS-9-Nummern + Registerkonventionen, ABI-Spez in docs/SYSCALLS.md | ✅ | Claudia | Nummern/Fehlercodes aus Andreas' MWOS-SDK (M:\MWOS) verifiziert. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.2 | Dispatcher + erste Calls: I$Read/Write/ReadLn/WritLn, F$Exit/ID/Time; Kernel-REPL nutzt eigene Syscalls; Selbsttest + Test 02 | ✅ | Claudia | E$NotRdy statt Blockieren bis Phase 4 (dokumentiert). → Details: ARBEITSPLAN_ARCHIV.md |
| 1.3 | Device-Modell + Konsolen-Treiber als internes Modul (löst fest verdrahtete Pfade 0/1/2 ab) | ✅ | Claudia | device.c/dev_term.c, Mode-Check E$BMode, Test 03; Doku: docs/DEVICES.md. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.4 | I$Dup + I$Close als Syscalls (Pfadtabelle nach außen nutzbar machen, OS-9-Semantik: Dup liefert niedrigste freie Nummer) | ✅ | Claudia | q9_path_dup, 2 neue Selbsttest-Checks, SYSCALLS.md. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.5 | F$PrsNam + F$CmpNam (Pfadnamen-Parsing nach OS-9-Regeln) | ✅ | Claudia | name.c/h; E$Diff-Nummer ($E2) vorläufig → MWOS-Abgleich heute Abend. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.6 | I$Attach + I$Detach: Geräte per Name ("/term") an-/abmelden, nutzt F$PrsNam | ✅ | Claudia | q9_dev_attach/detach, E$MNF neu; Link-Count-Verwaltung. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.7 | Zweites Gerät /nil (Null-Device) als Mini-Treiber | ✅ | Claudia | dev_nil.c; q9_path_open jetzt namensbasiert ("/nil" mit Slash ok). → Details: ARBEITSPLAN_ARCHIV.md |
| 1.8 | I$GetStt/I$SetStt Grundgerüst: SS-Codes für /term (z.B. SS.Ready = Eingabe wartet?) | ✅ | Claudia | getstat/setstat-Ops im Treiber-IF; SS.Ready+SS.EOF; SS-Nummern beim MWOS-Abgleich prüfen. → Details: ARBEITSPLAN_ARCHIV.md |
| 1.9 | HAL-Erweiterung Echtzeit (q9_hal_time): native = localtime, wasm = Date.now → F$Time liefert echte Uhrzeit + F$STime | ✅ | Claudia | Kalenderlogik 2000–2136, Wochentag in d2; wasm-Teil ungetestet (emsdk fehlt hier). → Details: ARBEITSPLAN_ARCHIV.md |
| 1.10 | POSIX-HAL (`src/hal/posix/`, termios statt conio) + Makefile-Target, damit Q9 auf macOS/Linux baut | ✅ | Claudia | src/hal/posix/hal_posix.c (termios raw+nonblocking, clock_gettime, localtime); Makefile waehlt HAL per `$(OS)` (Windows_NT = conio, sonst POSIX)… → Details: ARBEITSPLAN_ARCHIV.md |

### Phase 2 — Modulsystem

Feinplanung 2026-07-03 abends mit Andreas besprochen. Reihenfolge folgt dem
OS-9-Boot-Ablauf aus docs/MODULES.md: **Suchen → Validieren → Bekanntmachen →
Link/Unlink.** Kein Dateisystem nötig — Module werden aus einem einkompilierten
„ROM-Image"-Blob **in-place referenziert** (wie OS-9s ROM-Module: PIC-Code
läuft direkt aus dem ROM, keine Kopie nötig) — deshalb auch **keine
Speicherverwaltung** für Phase 2 nötig, die kommt erst mit Phase 3 (Laden von
echter Datei) bzw. Phase 4 (Prozess-Stacks/Heaps).

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 2.1 | Modul-Header + CRC32-Routine im Kernel | ✅ | Claudia | src/kernel/module.h/.c neu: q9_modhdr_t (28 Byte, #pragma pack, Offsets exakt wie PROJECT.md), q9_crc32 (bitweise CRC-32/ISO-HDLC, kein Table, kein… → Details: ARBEITSPLAN_ARCHIV.md |
| 2.3a | Suchfunktion: ROM-Image-Blob nach Sync-Bytes durchsuchen, nach Fund um ModuleSize zum nächsten Modul springen | ✅ | Claudia | `q9_mod_scan_first`/`q9_mod_scan_next` in module.h/.c; reine Sync-Suche, Größe/CRC-Plausibilisierung bewusst noch nicht hier (kommt in 2.3b) — nur… → Details: ARBEITSPLAN_ARCHIV.md |
| 2.3b | Validierung: Sync/Größe plausibilisieren, CRC32 nachrechnen | ✅ | Claudia | q9_mod_validate in module.h/.c: HeaderSize/ModuleSize/NameOffset-Strukturchecks vor der vollen CRC32 (billig vor teuer); Zwei-Stufen-Check… → Details: ARBEITSPLAN_ARCHIV.md |
| 2.3c | Bekanntmachen: Directory-Eintrag anlegen, Namenskollisions-/Revision-Regel (höhere Revision gewinnt, bei Gleichstand bleibt das etablierte Modul) | ✅ | Claudia | q9_mod_register/q9_mod_find in module.c: Directory als statisches Array (Q9_MOD_MAXDIR=8, kein malloc, wie devtab/pathtab). → Details: ARBEITSPLAN_ARCHIV.md |
| 2.3d | F$Link + F$UnLink als Syscalls: Suche nach Name+Type+Language, Link-Count rauf/runter | ✅ | Claudia | q9_mod_link/q9_mod_unlink in module.c + Dispatcher-Cases F_LINK/F_UNLINK in syscall.c (a0=Name,d1.b=Type,d2.b=Lang ->… → Details: ARBEITSPLAN_ARCHIV.md |
| 2.2 | `tools/q9mod`: Compiler-Output → Q9-Modul (Header, CRC, Custom Section für WASM) | 💤 | — | sinnvoll, sobald echte (nicht handgebaute) Module gebraucht werden — nach 2.3 |
| 2.4 | dev_term als echtes Typ-2-Modul (Treiber) aus dem ROM-Image laden | 💤 | — | Nagelprobe: internes Modul → echtes Modul; nach 2.2 |

**Entschieden (2026-07-03 abends):**
- **Modul-Gruppen** (gemeinsames Unlink mehrerer Module): erstmal nicht — braucht Multi-Modul-Dateien, die es noch nicht gibt. → Ideenspeicher.
- **OS-9-Dreiklang File-Manager/Treiber/Descriptor** vs. kombiniertes `q9_dev_t`: bleibt kombiniert — lohnt sich erst bei mehreren Instanzen desselben Treibers mit unterschiedlicher Konfiguration. → Ideenspeicher, falls der Bedarf mal auftaucht.
- **Speicherverwaltung**: nicht Teil von Phase 2 (siehe oben, in-place Referenzierung reicht).
- **PC-seitiges Inspektions-Tool** (`ident`/`dump`-artig): nicht nötig, Selbsttest-Muster reicht zur Verifikation. → Ideenspeicher, als `--dump`-Modus in `q9mod` statt eigenes Tool, falls später gebraucht.

### Phase 3 — Filesystem (Vorschläge, 2026-07-03 spät mit Andreas vorbesprochen)

Grundentscheidungen aus der Besprechung: **O1 = FAT16** als erster FS-Typ
(Interop: dasselbe Image lässt sich am Mac/PC mounten und befüllen — `hdiutil
attach -imagekey diskimage-class=CRawDiskImage q9disk.img`, formatieren mit
`newfs_msdos -F 16`). Andreas' Bedenken gegen 8.3-Namen ist durch LFN-Lesen
ausgeräumt (macOS erzeugt beim Mount ohnehin LFN-Einträge); LFN-**Schreiben**
ist deutlich fummeliger (8.3-Alias-Generierung, Mehrfach-Einträge) → erstmal
nicht (Ideenspeicher). Reihenfolge lesend vor schreibend, weil `F$Load` (der
wichtigste Kunde) nur Lesen braucht. Image-Format zunächst **Superfloppy**
(Boot-Sektor bei LBA 0, keine Partitionstabelle); MBR-Partitionen später als
Schicht zwischen Block-Device und VFS (Ideenspeicher, spätestens Phase 7 mit
SD-Karte nötig). Design-Referenz für den File-Manager-Schnitt: Buch **„OS-9
Insights" (Peter Dibble)** — ältere Auflage enthält einen FAT16-File-Manager;
als Vorlage lesen, Code aber NICHT übernehmen (Copyright), eigene
C99-Implementierung. Test-Images erzeugen die test/-Skripte selbst per Python
(kein eigenes PC-Tool nötig).

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 3.1 | Block-Device `/d0` als Q9-Gerät (nutzt q9_hal_blk_read/write), Roh-Blockzugriff über GetStt/SetStt-SS-Codes; Test-Image per Python in test/ | ✅ | Claudia | dev_d0.c neu: SS.BlkRd($14)/SS.BlkWr($15) aus MWOS sg_codes.h übernommen (RBF-Vorbild), reine Ops read/write/readln/writln bewusst E$UnkSvc (kein… → Details: ARBEITSPLAN_ARCHIV.md |
| 3.2 | VFS-Schicht: Pfad-Routing `/d0/pfad/datei` (F$PrsNam trennt Gerät/Rest), File-Manager als austauschbare Einheit hinter schmaler Schnittstelle, Datei-Kontext pro Pfad, globales Arbeitsverzeichnis für I$ChgDir (pro-Prozess erst Phase 4) | ✅ | Claudia | `src/kernel/vfs.c/.h` neu: `q9_fm_t` (open/create/makdir/remove, restpath-String statt OS-9-Pfaddeskriptor-Internas — schmal genug für einen… → Details: ARBEITSPLAN_ARCHIV.md |
| 3.3 | FAT16 lesend: Boot-Sektor/Root-Dir/Cluster-Ketten, I$Open + I$Read + I$Seek, Verzeichnis lesen; 8.3 **und** LFN-Namen lesen | ✅ | Claudia | `src/kernel/fat16.c/.h` neu — Boot-Sektor (BPB) plausibilisieren, Root-Dir + Unterverzeichnisse durchsuchen (8.3 UND LFN, Namensteile rückwärts… → Details: ARBEITSPLAN_ARCHIV.md |
| 3.4 | FAT16 schreibend: I$Create, I$Delete, I$MakDir, FAT-Ketten allozieren/freigeben; neue Namen nur 8.3 (LFN-Schreiben → Ideenspeicher) | ✅ | Claudia | fat16.c/.h: I$Create/I$MakDir/I$Delete/I$Write echt implementiert, FAT-Ketten allozieren/freigeben (beide FAT-Kopien synchron), nur 8.3-Namen… → Details: ARBEITSPLAN_ARCHIV.md |
| 3.5 | F$Load komplettieren: Modul aus Datei laden (statt nur ROM-Image), validieren, registrieren | ✅ | Claudia | q9_mod_load (module.c) — statischer Load-Puffer-Pool (4x4096 Byte, kein malloc), Directory-Eintraege merken Puffer-Herkunft, automatische Freigabe… → Details: ARBEITSPLAN_ARCHIV.md |
| 3.6 | wasm-HAL: Block-Backend via OPFS (FileSystemSyncAccessHandle im Worker) + Image-Upload/-Download im Frontend | ✅ | Claudia | Kernel läuft jetzt im Worker (Pflicht für Sync-Access-Handle); Details siehe „Erledigt". → Details: ARBEITSPLAN_ARCHIV.md |
| 3.7 | FAT16-Directory-Einträge bekommen echtes Datum/Uhrzeit (q9_hal_time) statt Nullfeldern bei I$Create/I$MakDir | ✅ | Claudia | `fat_pack_datetime()` (fat16.c) — Details siehe „Erledigt" unten. → Details: ARBEITSPLAN_ARCHIV.md |

### Phase 4 — Prozesse (freigegeben 2026-07-04, Konzept 2026-07-03 spät mit Andreas besprochen)

Grundsatzentscheidung **E8** (PROJECT.md): **Step-Modell statt Stack-Umschaltung** —
WASM kennt keinen Stack-Wechsel, also verwaltet der Scheduler Prozess-**Zustände**
(Active/Waiting/Sleeping) und ruft pro `q9_kernel_step()`-Tick den nächsten aktiven
Prozess als Step-Funktion. Blockieren = Zustandswechsel + Weckgrund, nie ein
eingefrorener Stack. Ausführung ist pro Language-Byte austauschbar (intern/C =
Step-Funktion auf dem Kernel-Stack; 68k später = Musashi-Kontext; WASM = Instanz) —
die emulierten Prozesse werden also die einfachsten. F$Fork ist OS-9-semantisch
(neuer Prozess aus Modul, kein Unix-Fork) und baut direkt auf dem Modul-Directory
aus Phase 2 auf (Fork = F$Link + Descriptor + Active-Queue). Scheduler-Interna
(F$AllPD & Co.) bleiben 🚫 wie in docs/SYSCALL_ROADMAP.md aussortiert.

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 4.1 | Prozess-Descriptor-Tabelle (statisch, wie devtab): PID, Parent, Modul, Zustand, Exit-Code, eigene Std-Pfade 0/1/2; Scheduler als Round-Robin über Active in q9_kernel_step() | ✅ | Claudia | `src/kernel/proc.c/.h` neu — Details siehe „Erledigt" unten. → Details: ARBEITSPLAN_ARCHIV.md |
| 4.2 | F$Fork + F$Exit + F$Wait + F$Chain: Prozess aus Modul starten (via Modul-Directory), beenden, auf Kind warten (E$NoChld $E2), verketten | ✅ | Claudia | Entscheidung E9 (Q9_MOD_NATIVE) — Details siehe „Erledigt" unten. → Details: ARBEITSPLAN_ARCHIV.md |
| 4.3 | Echtes Blockieren: E$NotRdy-Provisorium (1.2) ersetzen — Waiting-Zustand + Weckgrund, /term weckt bei Eingabe (SS.Ready-Mechanik), F$Sleep (Ticks, 0 = yield) | ✅ | Claudia | Details siehe „Erledigt" unten. → Details: ARBEITSPLAN_ARCHIV.md |
| 4.4 | F$SSpd (suspendieren) + F$SPrior (Prioritätsfeld setzen) | ✅ | Claudia | Details siehe „Erledigt" unten. → Details: ARBEITSPLAN_ARCHIV.md |
| 4.5 | Signale: F$Send, F$Icpt, F$RTE (Signal bricht Waiting/Sleeping ab, Intercept-Handler als Step-Aufruf) | ✅ | Claudia | Details siehe „Erledigt" unten — damit ist Phase 4 vollständig. → Details: ARBEITSPLAN_ARCHIV.md |

**Anschluss 4.6–4.8 (freigegeben 2026-07-04 abends, Andreas' Einwand):**
Andreas' berechtigter Punkt: `F$Fork` "führt" bislang keinen echten geladenen
Code aus — Entscheidung **E9** trägt nur einen rohen C-Funktionszeiger direkt im
`Q9_MOD_NATIVE`-Modul ein (gültig nur im selben Host-Prozess, kein portables
Moduldateiformat). Bevor Phase 5 (Shell) sinnvoll ist, braucht es eine echte
Ausführungs-Engine — sonst wäre die Shell nur derselbe Etikettenschwindel wie
der jetzige Kernel-REPL. Anders als beim 68k-Ziel (klassisches PIC-Problem,
erst Phase 6/7) ist das für **WASM** (unser Primärformat) kein
Relozierungsproblem — WASM-Code referenziert nie absolute Host-Adressen.
Es fehlen stattdessen: eine Instanziierungs-Engine, eine Syscall-Rückverbindung
und die Übersetzung von Zeigern über die Modulgrenze (siehe PROJECT.md O5/O6).

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 4.6 | Eingebettete WASM-Runtime für den nativen Build (löst O5): eine Bibliothek (Kandidat wasm3 — klein, embeddable, keine harte malloc-Pflicht) einbinden, die ein beliebiges `.wasm`-Modul zur Laufzeit instanziieren und eine exportierte Funktion aufrufen kann. Noch OHNE Syscall-Bridge — reiner Grundbaustein | ✅ | Claudia | wasm3 (MIT, Commit `d77cd814`) vendored unter `third_party/wasm3/` (nur Kern-Interpreter, kein WASI/libc); `src/kernel/wasmrt.c/.h` als schmaler… → Details: ARBEITSPLAN_ARCHIV.md |
| 4.7 | `Q9_MOD_WASM`-Ausführung: F$Fork/F$Chain erkennen das Language-Byte `Q9_MOD_WASM`, instanziieren das Modul über die Runtime aus 4.6 (bzw. `WebAssembly.instantiate` im Browser) mit einem Import/Host-Funktion als Syscall-Bridge. Erstmal NUR Syscalls ohne Zeiger-Parameter (F$Time, F$ID, F$Exit) | ✅ | Claudia | Native Seite fertig: `src/kernel/wasmproc.c/.h` neu (Import-Bridge `q9.f_id`/`q9.f_time`/`q9.f_exit`), `entry_step_for()` in syscall.c erkennt… → Details: ARBEITSPLAN_ARCHIV.md |
| 4.8 | Speicher-/Pointer-Marshaling über die Modulgrenze: a0–a7-Register als Offsets in die modul-eigene lineare Speicherinstanz interpretieren (statt rohe Host-Zeiger), mit Bounds-Check. Damit werden auch I$Read/I$Write/I$Open (Puffer-/Pfadnamen-Zeiger) für WASM-Module nutzbar | ✅ | Claudia | Vier neue Importe q9.i_open/i_close/i_read/i_write in wasmproc.c. → Details: ARBEITSPLAN_ARCHIV.md |
| 4.9 | `wasm3` auf Fixed-Heap umstellen (kein Host-`malloc`/`calloc`/`realloc` mehr im WASM-Pfad) + erster Baustein einer Q9-Systemkonfiguration | ✅ | Claudia | `src/kernel/config.h` neu (`Q9_SYSTEM_MEM_BYTES` = 256 KiB), Makefile leitet `d_m3FixedHeap` per Compiler-Define daraus ab. → Details: ARBEITSPLAN_ARCHIV.md |

---

### Phase 5 — 68k-Runtime (Musashi) — **umnummeriert 2026-07-04 abends, siehe Entscheidung E11**

**Noch nicht feingranular geplant** — bewusst zurückgestellt für eine eigene,
gründliche Planungsrunde (wie bei Phase 3/4), nicht nebenbei. Diese Phase war
bis 2026-07-04 abends als "Phase 6" nummeriert; die alte "Phase 5" (Shell)
ist jetzt **Phase 6** (siehe dort) — Begründung: Entscheidung E11.

**Grober Umriss aus der heutigen Diskussion** (Ausgangspunkt für die
Detailplanung, keine fertigen Schritte):
- Musashi als 68k-CPU-Kern einbinden — nativ als gewöhnliche C-Bibliothek
  (keine WASM-Ebene nötig), im Browser zu WASM kompiliert (läuft dort effizient
  über die browsereigene WASM-Engine, nicht "doppelt emuliert" im
  Performance-Sinn).
- `Q9_MOD_M68K`: echte PIC-68k-Programme laden und ausführen (vbcc-Toolchain
  noch zu beschaffen, siehe docs/TOOLCHAIN.md „Noch nicht installiert").
- TRAP→Syscall-Bridge (Musashi-Trap ruft `q9_syscall`), analog zur
  `wasmproc.c`-Bridge aus 4.7, aber auf Ebene der CPU-Emulation statt WASM-Import.
  **PIC-Pflicht für 68k-Module** (wie echtes OS-9) — keine Relozierungstabelle
  nötig, wenn vbcc konsequent PC-relativen Code erzeugt.
- Echter Scheduler auf Musashi-Ebene: CPU-Zustand (Register + Stackpointer im
  emulierten RAM) ist reine Datenstruktur, an jeder Instruktion sicherbar/
  wiederherstellbar — das eigentliche Gegenstück zum Step-Modell aus E8, nur
  eine Ebene tiefer, mit echtem Anhalten/Fortsetzen statt Kooperations-Zwang.
- Das ist die Voraussetzung für Phase 6 (Shell): Eine Shell, die vor dieser
  Phase gebaut würde, wäre zwangsläufig nur ein weiterer `Q9_MOD_NATIVE`- oder
  kurzlebiger-WASM-Behelf (E9-Falle), kein echter langlebiger Prozess.

**Ziel-CPU: 68030** (Entscheidung E12, 2026-07-04) — MMU bleibt ungenutzt statt
abgeschaltet. Wichtige Nebenbedingung für später: unser 68k-Code (vbcc) sollte
sich auf einen Befehlssatz beschränken, den auch die reale CPU32-Zielhardware
kennt — der Emulator darf "voller" sein als das, was wir tatsächlich benutzen.

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 5.2 | **Vorschlag (Andreas, 2026-07-04 abends):** CB030-Board emulieren als Bootstrap/Validierung statt gleich eigene QUICC-Peripherie. Die CB030 hat ein fertiges Boot-ROM mit Microware-OS-9-Modulen — damit kann Musashi an echtem, produktivem 68k-Code getestet werden statt nur an unseren handassemblierten Testprogrammen aus 5.1. Plan: zuerst mit den originalen Microware-Modulen starten (nur lokal, NICHT ins Repo — proprietär, gleiche Regel wie MWOS-SDK), dann nach und nach durch eigene Q9-Module ersetzen, sobald `Q9_MOD_M68K` + Syscall-Bridge stehen. Ändert nichts an der eigentlichen Zielhardware (MC68EN360/QUICC bleibt Ziel für Phase 7) — reiner Zwischenschritt zur Validierung/zum schnelleren Start. Lizenzlage der Microware-Module im Detail und wie weit "graduelle Ersetzung" technisch sauber geht (Modul für Modul austauschbar über F$Link, s. Abschnitt 5.4 HANDBUCH.md?) noch offen | 💡 | — | Aufgeworfen 2026-07-04 abends; PROJECT.md O4 verweist hierher. Speicherkarte + Peripherie-Register vollständig dokumentiert: [`docs/BOARD.md`](docs/BOARD.md). In Einzelgeräte aufgebrochen (5.2a–d, s.u.) — bleiben 💡 bis zur Phase-5-Detailplanung, da die Anbindung an Musashis Speicher-Hooks/Interrupt-Mechanismus noch nicht architektonisch entschieden ist |

**Einzelgeräte für 5.2** (Andreas, 2026-07-04 abends — Aufschlüsselung nach `docs/BOARD.md`):

| # | Gerät | Status | Wer | Notizen |
|---|-------|--------|-----|---------|
| 5.2a | RAM/ROM/Remap-Speicherlogik | ✅ | Claudia | `src/kernel/q9board.c/.h` neu (native-only, wie m68krt.c): `q9_board_t` (rom/rom_len, ram/ram_len, remapped-Merker, unabhängig von Musashis… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.2b | 68681-DUART (seriell) | ✅ | Claudia | Minimalansatz umgesetzt: `q9board.c` `cb030_uart_read/write` — nur SRA (`UART_BASE+0x02`, TxRDY=0x04 immer gesetzt, RxRDY=0x01 wenn… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.2c | Compact-Flash-Interface | ✅ | Claudia | ATA-PIO-Minimalprotokoll umgesetzt: `q9board.c` `cb030_cf_read/write`, Register `CF_BASE+0` (Data, 1 Byte/Zugriff) / `+2` (Sectcount) / `+3..+5`… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.2d | Timer/IRQ3 | ✅ | Claudia | Kooperative Umsetzung wie geplant: `q9_board_poll_timer(board, now_ms)` in `q9board.c` — `TI_IRQ_ON`/`TI_IRQ_OFF` (reine Adress-Trigger… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.2e | HAL-Konsole: transparenter VT100-Durchreichbetrieb | 💤 | Claudia | Voraussetzung für den ersten Boot-ROM-Lauf mit Eingabe (nicht früher nötig, darum 💤). Der Host darf die serielle Leitung nicht verfälschen — Echo/CR-LF/Backspace regelt OS-9 selbst per `tmode`/`xmode` (SCF), der Host muss nur eine dumme Leitung sein. **POSIX (`hal_posix.c`): schon erledigt** — `q9_hal_init()` macht seit 1.10 termios raw (ICANON+ECHO aus, Wiederherstellung per atexit). **Offen ist nur Windows** (`hal_windows.c`): `SetConsoleMode` mit `ENABLE_VIRTUAL_TERMINAL_INPUT` (stdin, sonst verschluckt die Konsole Pfeiltasten — von Andreas real beobachtet) + `ENABLE_VIRTUAL_TERMINAL_PROCESSING` (stdout), `ENABLE_LINE_INPUT`/`ENABLE_ECHO_INPUT` aus; Fehlschlag (Windows < 10) abfangen und ignorieren. Anschauungs-/Diagnosetool dazu existiert schon: `tools/vtmode.c` (zeigt empfangene Tastendrücke als Hex — Pfeiltaste muss als `ESC [ A` = `1B 5B 41` ankommen). Fürs echte CB030-Board ist NICHTS zu tun (PuTTY/TeraTerm bringen die VT100-Emulation mit) |
| 5.4 | **Erster echter OS-9-Boot** — Microware-ROM bootet bis zur interaktiven Shell 🎉 | ✅ | Claudia + Andreas | Das unveränderte `romimage.dev.running.BIN` (ROMBUG, 512K, lokal — nie im Repo) bootet in `q9.exe --cb030 <rom>` bis zum mshell-Prompt; `mdir` läuft… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.5a | CF-Vorbereitung: Multi-Sektor-Transfers + `--cf <pfad>`-Option | ✅ | Claudia | Beide Teile umgesetzt: (1) **Multi-Sektor**: `cb030_cf_read/write` (q9board.c) zaehlen `cf_sectcnt` jetzt echt durch (neues Feld `cf_remaining` in… → Details: ARBEITSPLAN_ARCHIV.md |
| 5.5b | CF-Image bespielen — OS-9 mit echtem Dateisystem (und perspektivisch CF-Boot) | 💤 | Andreas | Wartet auf Andreas (2026-07-06): fertiges CF-Image (evtl. sogar mit C-Compiler!) einbauen — entweder nach `cb030_cf.img` kopieren oder nach 5.5a per `--cf <pfad>` direkt verwenden. Dann testen: liest OS-9 das Dateisystem (`dir /dd`, `pd`)? Fernziel: `os9gen` aufs CF → Boot von CF statt ROM |
| 5.6 | **RTC-Emulation — als RTC72421 umgesetzt** (Andreas 2026-07-14: RTC72421 statt 72423, Lesen = Host-Uhr, Schreiben ignoriert). Emulator: 16-Byte-Fenster $FFFFD000–$FFFFD00F (frei), BCD-Nibbles S1..W + Control D/E/F, S1-Latch fuer konsistente Zeitstempel. MWOS-Q9-Port: `SYSMODS/rtc72421.a` (Sbrtn, Modulname rtclock **Rev 1** — gewinnt gegen das DS1302-rtclock Rev 0 aus dem Bootfile, tickgeneric linkt bei jedem Aufruf frisch, darum reicht `load` + `setime -s` im Startup, keine Bootfile-Chirurgie). **Nebenbei zwei Ur-Bugs gefunden+behoben: Timer-IRQ lief auf Level 3 statt 6 (Handler nie erreicht — die OS-9-Uhr hat NIE getickt) + Tick-Verlust im poll_timer (jetzt Nachholung mit 30-s-Deckel).** Akzeptanz erfuellt: date = Host-Zeit sekundengenau, sleep -s 3 = 3,0 s (vorher 25 s), Autostart auf OS9SYS.hda. → Details: ARBEITSPLAN_ARCHIV.md | ✅ | Claudia | fertig 2026-07-14 nachts (autonome Session, direkt nach 5.10) |
| 5.7 | **TX-Ringpuffer für die DUART-Konsole — Gegenstück zum RX-FIFO.** Der RX-FIFO (4 MByte, s. `cb030_uart_poll_rx`) ist bereits umgesetzt und verhindert Zeichenverlust auf der Empfangsseite. Die Ausgabeseite hat aber noch das alte Muster: `q9_hal_con_put()` (`hal_posix.c`) macht pro einzelnem von OS-9 echoten Zeichen ein blockierendes `fputc()`+`fflush(stdout)`. Hält der Leser auf der anderen Seite der Pty (Terminal.app/iTerm, oder `expect` in Testskripten) nicht mit, blockiert der naechste `write()`, bis der Leser wieder Platz macht — und weil CPU-Emulation und Host-I/O in `q9boardrun.c` in EINEM Thread laufen, friert in diesem Moment die GESAMTE Emulation ein (auch das RX-Polling, das ja im selben Loop haengt). Reproduziert mit `test_paste_burst.exp` (400-Zeilen-Paste): mit dem RX-FIFO allein kommt zwar kein Zeichen mehr dauerhaft abhanden, die Übertragung dauert aber unerwartet lange (>60s statt der rechnerisch erwarteten ~14s) und ein direkt danach gesendeter `list`-Befehl lief in `Read I/O error - Error #000:003` (E$Read, vermutlich Race: `list` traf auf noch aktives `build`). **Aufgabe:** `q9_hal_con_put()` darf den Haupt-Loop nie mehr blockieren koennen — analog zum RX-FIFO ein kleiner Software-TX-Ringpuffer in `hal_posix.c`, der pro Hauptschleifen-Durchlauf so viel wie moeglich nicht-blockierend per `write()` (STDOUT per `fcntl(..., O_NONBLOCK)`) rausschreibt; bei `EAGAIN`/`EWOULDBLOCK` bleibt der Rest im Puffer und wird beim naechsten Durchlauf weiterversucht. TxRDY/TxEMT in `cb030_uart_read`/`q9board.c` muessen dann ehrlich den Fuellstand dieses TX-Puffers widerspiegeln statt wie bisher immer "sofort bereit" zu melden — sonst glaubt OS-9, der Sender sei immer fertig, obwohl noch Daten warten. **Akzeptanzkriterien:** `test_paste_burst.exp` läuft ohne Race/Fehlermeldung durch, `list paste_burst.txt` zeigt den kompletten Originalinhalt; `Q9_BOARD_DEBUG=1`-Heartbeat setzt zu keinem Zeitpunkt laenger als ~3-4s aus; Gesamtdauer der 400-Zeilen-Übertragung nahe an der theoretischen Sendezeit (~14s bei 3ms/Zeichen), kein unerklaerter Timeout mehr; `test_paste_paced.exp` (Kontrolltest) weiterhin fehlerfrei; `make test` PASS, `make native` warnungsfrei | ✅ | Claudia | Umgesetzt: TX-Ringpuffer (256 KiB) in `hal_posix.c`, `q9_hal_con_put()` schreibt nicht-blockierend per `write()` (STDOUT `O_NONBLOCK`), Rest bleibt im Puffer und wird bei jedem weiteren `con_put` UND per neuer `q9_hal_con_flush()` (aufgerufen aus `q9boardrun.c`- und `hal_posix.c`-Hauptschleife) nachgeliefert. Neue HAL-Funktionen `q9_hal_con_flush/tx_ready/tx_empty` (q9_hal.h, alle drei Targets implementiert — native/wasm nur trivial, da dort synchron). `q9board.c` SRA liefert TxRDY/TxEMT jetzt ehrlich aus dem Pufferzustand statt immer "bereit", ebenso die TxRDYA-IRQ-Bedingung. Verifiziert: `test_paste_burst.exp` UND `test_paste_paced.exp` laufen beide sauber durch, kein Freeze, `list paste_burst.txt` zeigt alle 400 Zeilen vollstaendig und unveraendert. Das abschliessende "Read I/O error - Error #000:003" nach `list` tritt IDENTISCH auch im Paced-Kontrolltest auf (also vor UND unabhaengig vom TX-Puffer) — vorbestehendes, von 5.7 unabhaengiges OS-9/`list`-Verhalten, kein Regressionseffekt dieses Schritts; nicht weiter verfolgt (Timebox), bei Bedarf eigener Folge-Schritt. `make test` PASS, `make native` warnungsfrei |
| 5.8 | **Ctrl-]-Host-Escape + DEL→BS-Mapping in `q9_hal_con_get()` verifizieren.** Aus einem frueheren, nicht committeten Arbeitsstand liegt noch ein kleiner Patch fuer `hal_posix.c` vor (Ctrl-], 0x1D, beendet den Emulator sauber; DEL/0x7F wird als BS/0x08 an OS-9 durchgereicht, macOS-Backspace-Taste). Beide Aenderungen sind unabhaengig vom RX-FIFO (5.7 sequenziert) und vom sonstigen Terminal-Raw-Mode (der ist bereits ueber Andreas' Commit `3fd6f15` in `origin/main`). **Aufgabe:** Beide Verhaltensweisen gezielt verifizieren, BEVOR committet wird (bisher kein Testskript/Log dazu vorhanden) — (1) Ctrl-] im laufenden Emulator senden, erwartet: sofortiges sauberes Beenden mit der Meldung "Host-Escape Ctrl-] — Emulator beendet."; (2) DEL-Byte (0x7F) an die Konsole senden, erwartet: OS-9 empfaengt/verarbeitet es wie ein echtes Backspace (0x08), sichtbar z.B. beim Editieren einer Kommandozeile. Danach committen (oder verwerfen, falls sich Nebenwirkungen zeigen) | ✅ | Claudia | Patch aus dem Stash uebernommen (nur die `q9_hal_con_get()`-Ergaenzung — die Raw-Mode-Anteile des Stashes waren bereits ueber Andreas' Commit `3fd6f15` im Hauptbaum). Beide Verhaltensweisen gezielt mit eigenen expect-Testskripten verifiziert: (1) Ctrl-] (0x1D) an den laufenden Emulator gesendet → sofortiges sauberes Beenden mit "Host-Escape Ctrl-] — Emulator beendet.", Termios wird ueber den bestehenden `atexit`-Handler korrekt wiederhergestellt. (2) Eingabe "abc" + DEL (0x7F) + Enter → Kernel echot sichtbar Backspace-Space-Backspace und liefert "echo: ab" zurueck (das "c" wurde durch OS-9s Zeileneditor entfernt) — DEL kommt also als echtes BS (0x08) an, nicht als rohes 0x7F. `make test` PASS, `make native` warnungsfrei |
| 5.9 | **Idle-Drossel für den CB030-Runner** — Host-CPU im OS-9-Leerlauf von 100 % auf wenige Prozent senken. **Befund (Claudia, 2026-07-09, gemessen mit `Q9_BOARD_DEBUG=1` am Login-Prompt):** OS-9 idlet per `STOP #$3000` (PC konstant `0xA2E8`, SR `0x3000`, nur die 100-Hz-Timer-IACKs laufen weiter); die 100 % entstehen allein, weil die Schleife in `q9boardrun.c` nach `q9_m68krt_execute()` sofort die nächste Runde dreht und Musashi eine gestoppte CPU die angeforderten Takte nur "verbrennen" lässt. **Aufgabe in drei Teilen:** (1) Musashi-Vendor-Patch: schmalen Accessor `m68k_is_stopped(void)` ergänzen (gibt zurück, ob das interne `CPU_STOPPED`-Flag gesetzt ist), Änderung in `third_party/musashi/Q9_VENDOR.md` dokumentieren. (2) Neue HAL-Funktion `q9_hal_sleep_ms(ms)` in `q9_hal.h` + beide HALs (POSIX: `usleep`/`nanosleep`; Windows: `Sleep`) — bisher gibt es nur ein internes `usleep(1000)` in `hal_posix.c`, keine öffentliche API. (3) Hauptschleife `q9boardrun.c`: wenn CPU gestoppt UND kein IRQ anliegt → `q9_hal_sleep_ms(1)` statt sofort weiterzudrehen; der Timer-Poll rechnet mit `q9_hal_ticks_ms()` (Wanduhr), die OS-9-Uhr geht also nicht falsch — der nächste 10-ms-Tick setzt IRQ3 und weckt die STOP-CPU. **Akzeptanzkriterien:** Idle am Login-Prompt < 5 % Host-CPU (`ps`-Stichprobe); Boot bis Login, Login als `super`, `dir /dd` und ein kompletter `cc hello.c`-Durchlauf funktionieren unverändert; `make test` PASS, `make native` warnungsfrei. **Testweg:** Clone-Image `local_images/OS9SYS.claudia-test.hda` + expect-Automatisierung — Muster (CR-Anklopfen nach "devices online", gebremstes Senden ~50 ms/Zeichen), Login `super`/`Al35uUbC` (s. `/dd/SYS/password` im Image, wie in `test_paste_burst.exp` verwendet). Hinweis: Andreas' dauerhaft laufender Emulator profitiert erst nach Neustart mit dem neuen Binary | ✅ | Claudia | Alle drei Teile umgesetzt: (1) `m68k_is_stopped(void)` in `third_party/musashi/m68kcpu.c`/`m68k.h` (Vendor-Patch, dokumentiert in `Q9_VENDOR.md`), dazu der Q9-Wrapper `q9_m68krt_is_stopped()` in `m68krt.c/.h`. (2) `q9_hal_sleep_ms(ms)` in `q9_hal.h`, POSIX via `usleep`, Windows via `Sleep`, wasm als No-Op (kein echtes Schlafen im synchronen Worker-Kontext moeglich, dort auch nicht gebraucht). (3) `q9boardrun.c`-Hauptschleife: `q9_hal_sleep_ms(1)`, wenn CPU gestoppt UND kein IRQ ansteht. Verifiziert per Clone-Image (`OS9SYS.hda` → temporaeres Klon-Image) + expect-Automatisierung: Idle-CPU am Login-Prompt sank von 100 % auf ~1,5 % (`ps`-Stichprobe ueber 10s), Boot bis Login als `super`, `dir /dd` und ein kompletter `cc hello.c`-Durchlauf (Compile + Ausfuehrung, Ausgabe "hello q9") funktionieren unveraendert. `make test` PASS, `make native` warnungsfrei |
| 5.10 | **OS-9-SCF-Treiber fuer die Q9-Netzwerk-Terminals — als /x1../x8 mit ACHT Kanaelen umgesetzt** (Andreas 2026-07-14: Namen x1..x8 statt t1..t4, da t1 im Port existiert; Emulator auf 8 Kanaele erweitert). Emulator: MAX_CHANNELS 8, Fenster $FFFF1010–$FFFF108F, Vektoren 70–77, network_irq_resync gegen verlorene IRQs, TCP-Backpressure statt Byte-Verwurf. MWOS-Q9-Port: Treiber `SCF/nettty.a` (eigene Implementierung, 656 B, good crc), 8 Descriptoren x1..x8, systype.d-Makros, Merge `xterms` auf OS9SYS.hda (+Backup), Autostart in /dd/SYS/startup (load+iniz+tsmon). **Akzeptanz erfuellt: 8/8 parallele Telnet-Logins auf dem Produktiv-Image, Reconnect ok, make test PASS.** → Details: ARBEITSPLAN_ARCHIV.md | ✅ | Claudia | fertig 2026-07-14 nachts (autonome Session); Build-Weg (a) Wine-Toolchain, Laufzeit-Lade-Weg wie 5.11 statt Bootfile-Chirurgie |
| 5.12 | **vmnet-Netzwerk-Backend — OS-9 kommt echt ins Netz (raus UND rein) + DNS.** Das QUICC-Backend bekommt neben dem Mini-NAT (`--net nat`, Default, unveraendert) einen zweiten Modus `--net vmnet`: Apples vmnet.framework im Shared Mode, macOS uebernimmt NAT+DHCP+Routing — der Emulator reicht nur rohe Ethernet-Frames durch (`q9_quicc_rx_frame` rein, `q_backend_tx` raus). Entscheidung Andreas (12.7. abends): vmnet statt libslirp; braucht `sudo` beim Start. Subnetz-Trick: vmnet bekommt per `vmnet_start_address_key` das Subnetz 10.0.0.0/24 mit Gateway **10.0.0.2** verordnet — exakt die bisherige Mini-NAT-Gegenstelle, dadurch bleibt die komplette OS-9-Konfiguration (inetdb2: enet0=10.0.0.1/24) unangetastet. MAC-Uebersetzung noetig: vmnet erzwingt seine zugewiesene Absender-MAC, der spqe0-Descriptor hat fest 00:73:39:33:36:30 — das Backend schreibt deshalb Quell-/Ziel-MAC (und die MAC-Felder in ARP-Paketen) in beide Richtungen um. MWOS-Seite (DNS): `netdb_dns` (fertiges SDK-Modul, OS9/68020/CMDS/BOOTOBJS/SPF) mit in den netmods-Merge; lokale `resolv.conf` (nameserver 10.0.0.2 = vmnet-Gateway, Fallback 8.8.8.8) und `routes.conf` (`default 10.0.0.2`) in Q9/SPF/ETC, inetdb/inetdb2 per idbgen neu bauen (Wine-Toolchain), netmods v2 aufs Image (Autostart via SYS/startup laedt ihn beim Boot). **Akzeptanz:** `--net nat` verhaelt sich wie bisher (Regressionstest test_quicc_net.exp PASS); mit `sudo ./q9.exe --net vmnet ...`: `ping 10.0.0.2` (jetzt echtes macOS-Gateway), `ping 8.8.8.8` (Internet), `ping heise.de` (DNS!) — und von aussen: Mac erreicht das OS-9 (telnetd), `make test` PASS, `make native` warnungsfrei; ohne Root bricht `--net vmnet` mit klarer Fehlermeldung ab | ✅ | Claudia | **FERTIG (2026-07-13 nachts, interaktive Session mit Andreas — ALLE Akzeptanzkriterien erfuellt):** `ping 10.0.0.2` (echtes macOS-Gateway), `ping 8.8.8.8`/`1.1.1.1`-Fehlversuche vorher lagen an nat-Modus aus der Shell-History, mit sudo+vmnet dann **ping raus, DNS-Aufloesung (`ping heise.de`) und ping REIN (Mac -> 10.0.0.1, 3/3, ~1.4ms)** verifiziert; `make test` PASS, NAT-Regression gruen. Emulator: Commit 'Release 1.63' (vmnet_net.c/.h, quicc-MAC-Uebersetzung, --net-Flag). MWOS: netdb_dns ERSETZT netdb_local im netmods (beide tragen den Modulnamen 'netdb' — nie beide laden!), lokale resolv.conf (**idbgen crasht ohne domain-Zeile!**) + routes.conf (default-Route setzt ipstart selbst aus inetdb — kein route add in startspf), inetdb/inetdb2 neu, netmods v2 (24 Module) + Autostart auf OS9SYS.hda. Vorab erledigt: /dd/ETC, SYS/startspf, Netz-AUTOSTART in /dd/SYS/startup (DAS Boot-Skript — /dd/startup im Root ist Legacy!); ToolShed: Textdateien brauchen `os9 copy -l` (LF->CR). Bekannte Kosmetik: OSK-ping ist One-Shot und zeigt bei <10ms-Ticks 0ms; netstat -r wirkt im nat-Modus wegen Reverse-DNS-Timeouts haengend. Ausbaustufe (💡 bei Bedarf): telnetd/ftpd aktivieren (startspf-Kommentare), dann Mac -> OS-9 einloggen. *(Nachtrag 2026-07-13: Adressierung im Zuge von 5.13 auf 192.168.200.2 [Gast] / 192.168.200.1 [Gegenstelle], Subnetz /16 umgestellt — die 10.0.0.x-Werte oben spiegeln den Stand zum Zeitpunkt dieses Eintrags.)* |
| 5.13 | **Bridge-Netzwerk-Backend (`--net bridge:<ifname>`) — dritter QUICC-Zugangsweg neben `nat` und `vmnet`, ohne root/Entitlement.** Ziel war drei unterschiedliche Netzwerkzugänge über eine Kommandozeilenoption steuerbar zu machen: (1) `vmnet` — braucht sudo/Entitlement beim Emulator-Start, s. 5.12, ✅ fertig. (2) "Client Net" — Idee: Emulator hat keine eigene IP, nutzt die Host-Netzwerkschnittstelle wie ein gewöhnliches Host-Programm; genaue Ausgestaltung war in der Session, in der der Rechner abgestürzt ist, besprochen worden, ist aber nicht mehr rekonstruierbar und wurde von Andreas am 2026-07-13 bewusst **zurückgestellt** (zu viel Aufwand gemessen am aktuellen Nutzen) — bei Bedarf neu durchsprechen, bevor daran gebaut wird. (3) **Bridge** — Frames gehen per BPF (`/dev/bpf*`) roh an eine PHYSISCHE Netzwerkschnittstelle (echtes Layer-2-Bridging, keine MAC-Übersetzung nötig anders als bei vmnet, Gast bekommt seine IP direkt vom echten Router per DHCP); braucht KEIN root, sondern einmalig per Setup-Skript angepasste BPF-Geräterechte (`tools/macos/setup_bpf_access.sh` + `chmod_bpf.sh`, ChmodBPF-Muster wie bei Wireshark); Voraussetzung ist eine dedizierte Kabel-Ethernet-Schnittstelle (WLAN lässt sich bei den meisten Access Points nicht bridgen, verwirft Frames mit fremder Absender-MAC). **Umsetzung:** neu `src/kernel/bpf_net.c/.h` (BPF-Ringpuffer, Reader-Thread, Start/Send/Recv-API), `quicc.h/.c` verallgemeinert von `use_vmnet` (int) auf `net_backend` mit `Q9_NET_NAT`/`Q9_NET_VMNET`/`Q9_NET_BRIDGE`, `Makefile` baut `bpf_net.c` auf Darwin mit `-DQ9_HAVE_BPF` mit ein, kleinere Anpassungen in `vmnet_net.c/.h`, `hal_posix.c`, `q9boardrun.h`, `test_quicc_net.exp`. **Akzeptanzkriterien (noch offen):** `ping`/echter Datenverkehr über die physische Bridge-Schnittstelle end-to-end (Gast raus UND rein), `make test` PASS, `make native` warnungsfrei, bestehende `nat`/`vmnet`-Regressionen unverändert grün | 🟡 | Claudia (AF) | **Code steht, Test steht noch aus (Stand 2026-07-13, 14:51 Uhr — danach ist der Arbeitsrechner abgestürzt, working directory war zu diesem Zeitpunkt komplett uncommitted, nichts verloren).** `make native` baut sauber (nur eine harmlose Linker-Warnung zu Segment-Alignment). Laut Code-Kommentar in `bpf_net.c/.h` selbst: nur Kompilierbarkeit + Fehlerpfad ohne Interface verifiziert, der eigentliche Frame-Transport über eine physische NIC mangels zweiter Schnittstelle NICHT end-to-end getestet. **Nächster Schritt:** mit echter zweiter physischer Netzwerkschnittstelle (z.B. USB-Ethernet-Adapter oder eingebautes Ethernet, kein WLAN) `--net bridge:<ifname>` end-to-end testen, danach `test_quicc_net.exp`-Regression ergänzen und Eintrag auf ✅ setzen. "Client Net" (Punkt 2 oben) bleibt bewusst 💭/zurückgestellt, bis neu besprochen. **Prozessentscheidung Andreas (2026-07-13):** Die Erstellung der Netzwerkdatenbanken (`inetdb`/`inetdb2`, bisher per `idbgen` über die Wine-Toolchain am Mac erzeugt, s. 5.12) soll künftig DIREKT IM EMULATOR passieren (OS-9-Bordmittel, `idbgen` läuft ja auch nativ unter OS-9), nicht mehr extern am Host mit anschließendem ToolShed-Transfer aufs Image — Workflow-Wechsel für alle künftigen Netzwerk-Konfigurationsschritte, betrifft auch eine eventuelle spätere Neuauflage von "Client Net" |
| 5.14 | **Netzwerkdatenbanken (`inetdb`/`inetdb2`) direkt im Emulator erzeugen** — löst den bisherigen Weg über die Wine-Toolchain am Host ab (s. Prozessentscheidung Andreas 2026-07-13 in 5.13). `idbgen` läuft künftig als OS-9-Bordmittel INNERHALB des laufenden Q9-Emulators statt am Mac. **Akzeptanzkriterien (Andreas, 2026-07-13):** (1) Bei korrekt konfigurierten Quelldateien im SDK (`OS9/68030/PORTS/Q9/SPF/ETC/`: `resolv.conf`, `routes.conf`, `interfaces.conf`) muss sich die Datenbank im Emulator OHNE FEHLER erzeugen lassen (`idbgen` als OS-9-Kommando). (2) Die Quellkonfiguration muss zuvor nach `/dd/ETC` im laufenden Image kopiert werden (Transferweg: bestehender Paste-/Kermit-Mechanismus, s. `test_paste_burst.exp`/`test_paste_paced.exp` bzw. Ideenspeicher-Eintrag Kermit). (3) **Zielort der fertigen Datenbanken:** `/dd/CMDS/BOOTOBJS/SPF/inetdb` und `/dd/CMDS/BOOTOBJS/SPF/inetdb2` — exakt der Pfad, den `startspf`/`net.bl` beim Laden erwarten (bisher per Wine-Toolchain-Build dorthin erzeugt, s. `MWOS/OS9/68030/PORTS/Q9/SPF/ETC/makefile`, `ODIR = $(PORT)/CMDS/BOOTOBJS/SPF`); `idbgen` muss also mit passendem Zielpfad-Argument im Emulator aufgerufen werden (kein hartcodierter Pfad im Tool selbst, Ziel kommt als Positionsargument). (4) Beim nächsten Emulatorstart (Boot-Weg oder Laufzeit-Weg `load netmods` + `mbinstall` + `ipstart`) muss die NEUE Konfiguration aus der frisch erzeugten Datenbank geladen werden — verifizierbar z.B. über `ifconfig` (neue IP/Route) und `ping` auf die neue Gegenstelle. (5) Regression: bestehender `--net nat`/`vmnet`-Workflow bleibt unverändert funktionsfähig, `test_quicc_net.exp` PASS. **Offene Frage vor Beginn:** ob `idbgen` als Teil des net.bl-Mergesets bereits an Bord ist oder erst separat als Modul ins Image geladen werden muss — vor dem ersten Testlauf klären. **Wie die Datenbanken bisher erzeugt wurden (Quelle der Wahrheit für den Emulator-Aufruf): `MWOS/OS9/68030/PORTS/Q9/SPF/ETC/makefile`** (Zeilen 53-62): `$(IDBGEN) -to=$(OS) -tp=$(CPU) -d=. -d=$(SDIR) $@` erzeugt aus `$@` (`inetdb`) automatisch auch `$@2` (`inetdb2`) im selben Lauf; `-to`/`-tp` sind reine Cross-Build-Flags fuer die Wine-Toolchain (Ziel-OS/-CPU) und entfallen beim nativen Lauf im Emulator, `-d=<pfad>` bleiben (Suchpfade fuer Quelldateien). **Wichtig — idbgen braucht ELF Quelldateien, nicht nur unsere drei:** laut `SFILES`/`RFILES` im Makefile: `hosts`, `hosts.equiv`, `protocols`, `networks`, `services`, `inetd.conf`, `host.conf`, `rpc` (bisher generisch aus `$(MWOS)/SRC/ETC`, NICHT Q9-spezifisch) PLUS die drei lokalen Q9-Dateien `resolv.conf`, `interfaces.conf`, `routes.conf` (aus `SPF/ETC/`, s. Kommentar dort: "kommen LOKAL aus diesem Verzeichnis... statt aus dem SDK"). Fuer den Emulator-Testlauf muessen ALLE ELF Dateien nach `/dd/ETC` transferiert werden, sonst bricht `idbgen` beim Fehlen von z.B. `hosts`/`protocols`/`services` ab — die generischen SDK-Vorlagen dafuer liegen unter `/Volumes/SSD1TB/projects/MWOS/SRC/ETC/`. Nativer Aufruf im Emulator voraussichtlich: `idbgen -d=/dd/ETC inetdb` (erzeugt `inetdb`+`inetdb2` im aktuellen Verzeichnis — vorher dorthin wechseln bzw. Zielpfad `/dd/CMDS/BOOTOBJS/SPF/inetdb` als `$@` uebergeben, analog zum `RPCDBGEN`-Aufruf mit `-o=`) | ✅ | Claudia (AF) | **FERTIG UND AUF DEM PRODUKTIVEN IMAGE DEPLOYT (2026-07-13 abends, `apply_idbgen_5_14.exp`, Backup vorher: `local_images/OS9SYS.hda.backup-vor-5.14-20260713-170652`).** Nach echtem Neustart (zweiter `q9.exe`-Prozess, KEIN manuelles Eingreifen): `ifconfig enet0` zeigt `192.168.200.2 netmask 0xffff0000 broadcast 192.168.255.255`, `ping 192.168.200.1` erfolgreich, `hostname` -> `q9`, `netstat -i` sauber (enet0 UP, 0 Fehler). Alle fuenf Akzeptanzkriterien erfuellt. **Zusaetzlich geloest: Sicherheitsproblem beim Deployment-Mechanismus** — geprueft, ob `modbust` (ToolShed) einen Default aus `netmods` extrahieren koennte (Absturz mit SIGBUS, unbrauchbar), danach den saubereren Weg gewaehlt: `idbgen` einmalig VOR dem Boot-Skript-Update direkt auf dem Zielimage laufen lassen, damit `/dd/CMDS/BOOTOBJS/SPF/inetdb`+`inetdb2` beim Einspielen von `loadidb`/`startup` bereits existieren — die im vorherigen Status beschriebene Boot-Abbruch-Gefahr (fehlende Datei bricht den GESAMTEN restlichen Bootvorgang ab, nicht nur das Sub-Script) bleibt als dokumentiertes Verhalten bestehen (s. Kommentare in `loadidb`), ist aber durch die Deployment-Reihenfolge (erst idbgen, dann Boot-Skripte) auf dem Produktivimage nicht mehr aktiv. **Bei kuenftigen, komplett frischen Images (idbgen noch nie gelaufen) gilt weiterhin: erst idbgen-Workflow durchlaufen lassen, DANACH `loadidb`/`startup`/`startspf` einspielen — nicht umgekehrt, sonst bricht der Netzwerk-Boot.** SDK-Quellen final: `MWOS/OS9/68030/PORTS/Q9/CMDS/BOOTOBJS/BOOTFILES/{startup,loadidb,startspf}`, `SPF/ETC/{resolv.conf,routes.conf,interfaces.conf}` (alle bereits 192.168.200.x). Testklone (`local_images/OS9SYS.claudia-idbgen-*.hda` u.a.) und alle `test_idbgen_*.exp`-Skripte bleiben lokal als Referenz erhalten, nichts committet außer den lokalen Dateien selbst |
| 5.15 | **Netzmasken-Kollision `/16` vs. Heimnetz behoben + TCP-Haenger bei langer Telnet-Ausgabe ueber eth0 untersucht.** Andreas meldete (2026-07-14): Telnet-Verbindung ueber die echte QUICC-Ethernet-Schnittstelle (nicht die `/x1-8`-Terminals) haengt bei laengerer Ausgabe. **Befund 1 (behoben):** `vmnet_net.c` (`QV_NETMASK`) und der MWOS-Q9-Port (`SPF/ETC/interfaces.conf`, `enet0`) setzten `/16` (`255.255.0.0`) fuer das Gast-Subnetz `192.168.200.0`. Der Mac-WLAN-Adapter (`en1`) hat ebenfalls eine `/16`-Maske (`192.168.123.x/16`) — macOS haelt dadurch den KOMPLETTEN Bereich `192.168.0.0/16` fuer ueber WLAN direkt erreichbar und routet jeglichen Traffic zu `192.168.200.2` an der vmnet-Bruecke (`bridge100`/`vmenet0`) vorbei (per `tcpdump` verifiziert: 0 Pakete am vmnet-Interface). Fix: Maske auf `/24` (`255.255.255.0`) verengt, IPs unveraendert (`.1`/`.2`) — eine `/24`-Route ist gegenueber der `/16`-Route des WLAN-Interfaces per Longest-Prefix-Match automatisch bevorzugt. **Befund 2 (lokalisiert, nicht behebbar ohne Quellcode):** Nach dem Netzmasken-Fix bestand das eigentliche Symptom (Haenger bei langer Ausgabe) weiter fort. Systematisch eingegrenzt per QUICC-RX/TX-Debug-Logging + paralleler `tcpdump`-Mitschnitte + Gegenprobe mit rohem `nc` (schliesst Test-Tooling als Ursache aus) sowie Vergleich mit identischem Kommando auf der lokalen Konsole (laeuft dort fehlerfrei durch, beliebig lang): OS-9 sendet nach ca. 15 TCP-Segmenten (~2,4 KB) schlicht **keine weiteren Daten mehr**, obwohl der Host durchgehend ACKs mit offenem Fenster (win ~2048, nie 0) zurueckschickt — kein Zero-Window, keine RX/TX-Ring-Drops im Emulator (0 verworfene Frames geloggt), keine CPU-Blockade (idlet normal weiter, ACK-Zaehler laeuft). Der Stillstand ist damit ein Producer-seitiges Verhalten (SPF/`sptcp`-Stack oder `telnetd`), nicht die QUICC-Hardware-Emulation. Fuer `sptcp`/`spip`/`telnetd` liegt im SDK (`MWOS/SRC/DPIO/SPF/DRVR/SPTCP`, `SPIP`) **kein Quellcode** vor — nur Microware-Copyright-Header, Makefiles und der Deskriptor-Header `spf_desc.h` mit den einstellbaren, aber grosszuegig bemessenen Konstanten `TCPWINDOW` (0x4000), `SENDBUFFER` (0x5000), `DFLT_MSS` (512), `READSZ` (4096) — keine erklaeren das ~2,4-KB-Limit. Die eigentliche TCP-Logik ist kompilierter, closed-source Microware-Code (`OS9/*/CMDS/BOOTOBJS/SPF/sptcp`, `telnetd`), ohne Disassemblierung nicht weiter diagnostizierbar. **Interessanter Nebenbefund:** vor dem RTC/Timer-Fix von 5.6 (letzte Nacht) tickte die Systemuhr ueberhaupt nicht — jeder tick-basierte Recovery-Mechanismus (z.B. ein TCP-Retransmission-/Persist-Timer) waere daher vermutlich NIE angelaufen; in einem Testlauf nach dem 5.6-Fix wurden nach ~59s zwei Sondierungspakete beobachtet, das System haengt also nicht mehr zwangslaeufig fuer immer. Betrifft ausschliesslich `telnetd`/eth0, NICHT die `/x1-8`-Terminals (eigener TCP-Server im Emulator selbst, `m68krt.c`, unabhaengig von `sptcp`) | ✅ / 🟡 | Claudia (AF) | **Netzmasken-Fix FERTIG UND AUF DEM PRODUKTIVEN IMAGE DEPLOYT** (`local_images/OS9SYS.hda`, Backup: `OS9SYS.hda.backup-vor-5.15-netmask24-20260714-101245`, Deployment analog 5.14 per `idbgen`-Workflow). Nach echtem Neustart verifiziert: `ifconfig enet0` zeigt `netmask 0xffffff00` (vorher `0xffff0000`), `ping 192.168.200.1` erfolgreich, `netstat -i` sauber; zusaetzlich mit echtem `sudo ./q9.exe --net vmnet` gegen das Produktivimage gegengeprueft (0% Paketverlust). `make test` PASS. **TCP-Haenger bleibt 🟡 offen, aber drei weitere Hypothesen gezielt widerlegt (Andreas' berechtigter Einwand: ein Bug, der JEDE TCP-Verbindung trifft, haette nie produktiv laufen koennen — also wahrscheinlicher ein Emulator-spezifisches Detail als ein echter sptcp-Bug):** **Nachtrag 2026-07-15: ZWEI unabhaengige Bugs identifiziert.** Bug 1 (SS_SEvent-Luecke, `telnetdc` wartet timeoutlos in `Ev$Wait`) ist mit einem Option-B-Binaerpatch (14 Byte bei `telnetdc`-Offset `0xf4c`, CRC per `fixmod -u` repariert) **behoben** — per Live-Trace zweifelsfrei belegt, die alte `Ev$Wait`-Signatur tritt danach nie mehr auf. Der Haenger besteht trotzdem fort: Bug 2, per Live-Trace mit PC-Watchpoints direkt hinter dem `I$GetStt`/`SS_Ready`-Aufruf gefunden — `SS_Ready` liefert nach einem kurzen Anfangsschub (18 Erfolge in den ersten Sekunden, passend zur initialen Verzeichnisauflistung) fuer den Rest eines 10-Minuten-Dauertests (60.025 Polls) **kein einziges Mal mehr Erfolg**, nur noch `E$NotRdy` (Fehlercode 246) — ein echter, dauerhafter Stillstand auf einer tieferen Ebene, vorher von Bug 1 verdeckt. `SS_Ready`/`I$GetStt` ist closed-source Microware-Kernelcode (kein eigenes Modul wie `telnetdc`), Patchen daher deutlich schwerer. Reproduktion inzwischen vollstaendig automatisiert (passwortloses `sudo` fuer `q9.exe` via `/etc/sudoers.d/q9-emulator`, `expect`-Skript mit echtem `telnet`-Client, kein manuelles Eingreifen mehr noetig). Details: `docs/re_telnetdc/OPTION_B_STATUS.md`. **Nachtrag 2026-07-15 abends: Bug 2 umlokalisiert von `pkdvr` auf `spf_rx`.** Ein Live-Trace-Verdacht auf drei gleichzeitig geladene `pkdvr`-Kopien erwies sich per `mdir -e`-Gegenprobe als Messfehler (nur EINE `pkdvr`-Kopie, korrekt geteilt) — zwei der drei vermuteten "Basen" lagen tatsaechlich in den Adressbereichen der Module `spf`/`spf_rx` (beide rufen ebenfalls `Ev$Wait` auf). Der wirklich permanent blockierte `Ev$Wait` (Event-Handle nie signalisiert, ueber zwei komplette 10-Minuten-Laeufe) sitzt bei `spf_rx`-Offset `0x0624` (per Rohbyte-Scan bestaetigt) — vermutlich derselbe Bug wie der urspruengliche Sendeaussetzer aus Befund 2 oben, da ein haengender Empfangsprozess plausibel erklaert, warum die Sendeseite nie ein "Fenster wieder offen"-Signal sieht. Naechster Schritt: `spf_rx` wie `pkdvr` in Ghidra rekonstruieren, Offset `0x0624` analysieren. Details: `docs/re_telnetdc/OPTION_B_STATUS.md`.
(1) **IRQ-Ack-Fix committet** (`m68krt.c`, s. eigener Commit) — echte Hardware haelt pro Geraet eine eigene IRQ-Leitung, unser Emulator warf bei JEDER Quittierung ALLE gleichzeitig anstehenden Level weg statt nur das quittierte; behoben durch sofortiges Neuanlegen des naechsthoechsten Levels. Eigenstaendig sinnvoll (RTC/DUART/8-Terminal-Login alle weiterhin korrekt, `make test` PASS), aber **byteidentischer Haenger bei exakt derselben Stelle** — widerlegt die IRQ-Race-Hypothese.
(2) **Kuenstliche Netzwerklatenz** (macOS `dnctl`/`pfctl`, dummynet-Pipe 25ms je Richtung, verifiziert per `tcpdump`-Zeitstempel auf ~50ms RTT statt ~1-3ms) — **byteidentischer Haenger**, widerlegt die "ACKs kommen unrealistisch schnell"-Hypothese.
(3) **QUICC-Ringgroessen** `RX_RING_CNT`/`TX_RING_CNT` in `MWOS/OS9/68030/PORTS/Q9/SPF/SPQUICC/DEFS/spf_desc.h` von 4 auf 32 erhoeht, `spqe0` per Wine-Toolchain neu gebaut (`build_spqe0.bat`, Ziel `spfdesc`), auf Klon-Image per `os9 copy`/gepatchtem `SYS/startup` (`load -d` VOR `netmods`) deployt und per Emulator-Diagnose zweifelsfrei verifiziert, dass die neue 32er-Ringkonfiguration tatsaechlich aktiv war (`INIT_RXTX: RBASE=0900 TBASE=0a00` = 256 Byte / 8 = 32 BDs) — **byteidentischer Haenger, KEIN einziges TX-WRAP-Ereignis geloggt** (Ring lief bei nur ~15 Segmenten nie ueber) — widerlegt die Ringgroessen-Hypothese vollstaendig. Aenderung zurueckgesetzt (kein Nutzen, unnoetige Abweichung vom SDK-Original).
(4) **Kumulatives Byte-Budget pro Verbindung widerlegt:** ein kleines `echo`-Kommando VOR dem grossen `dir -e` in derselben Session aendert nichts am Abbruchpunkt des `dir -e` (steigt nur um die eigene Kommandogroesse, ~35 Byte) — der Haenger ist an den einzelnen Schreib-Burst gebunden, nicht an die Gesamtmenge seit Verbindungsaufbau.
**Web-Recherche:** kein offizieller Microware-Patch/Errata auffindbar; `sptcp` wurde laut offizieller Support-Seite (freestation.co.jp) bis V6.1 (2019) gepflegt, aber NUR fuer x86/PPC/ARM/SH4 — 68K-Support endete bei unserer v3.2 (1998) und wurde nie wieder angefasst, d.h. ein evtl. vorhandener Bug in dieser Version wurde fuer 68K nie gepatcht (unabhaengig davon, ob er in unserer Emulation oder echt in `sptcp` sitzt).
**DURCHBRUCH (ftpd-Kontrolltest):** `ftpd` aktiviert (Klon-Image, `startspf` einkommentiert) und per Python `ftplib` (Active-Mode, macOS hat kein `ftp`-CLI mehr) getestet: `dir -e /dd/CMDS/BOOTOBJS`-Aequivalent (`LIST` auf `/dd/CMDS/BOOTOBJS`) ueberträgt **alle 196 Zeilen (~11.900 Byte)** fehlerfrei bis zum letzten Eintrag — **5x mehr als die ~2,4 KB, an denen `telnetd` immer haengt, und ueber denselben `sptcp`/`spip`-Stack.** Das schliesst `sptcp` als Ursache DEFINITIV aus (waere der Bug dort, muesste auch `ftpd` haengen) — die Ursache sitzt spezifisch in `telnetd` (9.422 Byte Code+Daten, viel kleiner als `sptcp` mit 21.398 oder `spip` mit 56.602 Byte). Erklaert auch endlich Andreas' berechtigten Einwand: ein Nischen-Bug in der interaktiven Telnet-Session-Schreiblogik haette produktive FTP-/generische-TCP-Nutzung nie betroffen.
**Reverse-Engineering-Infrastruktur aufgebaut:** Ghidra 12.1.2 + OpenJDK 21 per `brew install ghidra` installiert (Formula, kein sudo/Pkg-Installer noetig, brew-cask-Installer scheiterte an fehlendem TTY fuer sudo). Eigener Python-Parser fuer das OS-9/68000-Modul-Merge-Format geschrieben (`os9module.h`-Header aus dem ToolShed-Quellcode als Referenz: 48-Byte-Header, `size` bei Offset 4/4 Byte, Namensoffset bei Offset 12/4 Byte, Name null-terminiert nicht High-Bit-terminiert — `modbust` aus ToolShed stuerzt an dieser Datei mit SIGBUS ab, s. bereits 5.14 dokumentiert), damit alle 24 Module aus `netmods` sauber extrahiert (`sysmbuf` bis `inetdb2`). `telnetd` einzeln aus `CMDS/telnetd` extrahiert. Ghidra-Import ueber `analyzeHeadless` mit `-loader BinaryLoader -processor 68000:BE:32:MC68030` (Raw-Loader noetig, kein Standard-Objektformat). **Wichtiger Fallstrick gefunden:** Ghidras automatische Funktionssuche fand den ECHTEN Modul-Einsprungpunkt nicht (Modul-Header-Feld `exec` bei Offset `$50`, Ghidra started zufaellig bei `$13c` und disassemblierte dadurch nur ~700 von ueber 2000 tatsaechlichen Instruktionen) — behoben durch eigenes Ghidra-Skript (`FixEntry.java`), das ab dem echten `exec`-Offset disassembliert und eine Funktion anlegt, danach volle Analyse. `telnetd` ruft interne Funktionen ueber eine **indirekte Sprungtabellen-Konvention** auf (`movea.l #imm,A0; jsr (off,PC,A0*1)`, typisch fuer Microware-C mit getrennten Code-Segmenten).
**VOLLSTAENDIGER ROOT-CAUSE GEFUNDEN (2026-07-14, Disassemblierung + entscheidender Laufzeittest):** Aus den String-Referenzen ergab sich, dass `telnetd` nur der **Listener** ist und pro Session ein Kindmodul **`telnetdc`** (14.858 Byte, `CMDS/telnetdc`) forkt (`fork telnetdc`, im proclist sichtbar) — die eigentliche Relay-Logik steckt dort. `telnetdc` fuehrt eine ereignisgesteuerte Hauptschleife (`FUN_00000e5c`, Schleifenkopf `0xee0`) mit zwei Relay-Richtungen: `FUN_0000118c` = tty→net (Shell-Ausgabe zum Client, DIE Haenger-Richtung) und `FUN_000010ae` = net→tty (Tastendruecke, mit IAC-Verarbeitung). Beide Relays pollen nicht-blockierend per `I$GetStt SS_Ready` (Code 0x8d); der EINZIGE blockierende Punkt ist ein **`F$Event`/Ev$Wait** (F-Code 0x53, Unterfunktion D1=4; `_os9_ev_wait(ev,*val,min=1,max=0x7fff)` — **KEIN Timeout**, wartet unbegrenzt bis der geteilte Event-Zaehler > 0). **Die Setup-Funktion `FUN_000009e4` registriert die beiden Aufweck-Events ASYMMETRISCH:** Netz per `I$SetStt` mit Code **0x48 = `SS_SPF`** (SPF-eigener SetStat, wird vom sptcp/spip-Stack selbst bedient — ueber den QUICC-Hardware-IRQ Level 5), tty per `I$SetStt` mit Code **0x3e = `SS_SEvent`** ("set event on data ready", generischer SCF-Mechanismus). **Der SCF-Treiber, der das Telnet-Pseudo-Terminal bereitstellt, honoriert `SS_SEvent` NICHT** — `SS_SEvent` ist im gesamten MWOS-SDK-Baum zwar in `sg_codes.h` definiert, aber in KEINEM SCF-Treiber implementiert. Folge: das tty-Event wird bei Shell-Ausgabe nie signalisiert, telnetdc parkt fuer immer im Ev$Wait, waehrend die Shell weiterproduziert bis der pty-Puffer voll ist (~2,4 KB) und dann blockiert. **Entscheidender Laufzeittest (Python-Socket): Haenger ausgeloest (~1,4 KB), EIN Tastendruck gesendet → +1,4 KB Ausgabe floss sofort byte-genau weiter nach.** Ein eingehendes Netz-Paket erzeugt das (funktionierende) SS_SPF-Event → weckt telnetdc → es findet die gestaute Shell-Ausgabe → sendet sie. **Bestaetigt Andreas' Beobachtungen exakt:** (a) `tmode nopage` half nicht (kein Paging, sondern verlorenes Aufwecksignal); (b) "nach ein paar Aufweck-Tastendruecken ganz blockiert" (die Tastendruecke fuellen die net→tty-Richtung/tty-Eingabepuffer, bis auch dieser Aufweckweg dicht ist). **WICHTIG: Das ist KEIN Bug im Emulator-C-Code, sondern eine Faehigkeitsluecke im OS-9-SCF-Pseudo-Terminal-Treiber** (SS_SEvent nicht implementiert). Erklaert, warum FTP/lokale Konsole/`/x1-8` funktionieren (nutzen SS_SEvent nicht) und warum echte Microware-Systeme nicht betroffen waren (anderer pty/SCF-Treiber mit SS_SEvent-Support). **Fix-Optionen fuer eine Folge-Session:** (A) den pty-bereitstellenden SCF-Treiber (bzw. den `scf`-FM zentral) um `SS_SEvent`-Behandlung erweitern — sauberster OS-9-seitiger Fix, braucht Treiberquelle + Rebuild; (B) `telnetdc` binaer patchen, sodass es nicht auf das tty-Event angewiesen ist (schwierig, da Ev$Wait timeoutlos — muesste auf Poll/Alarm umgebaut werden); (C) pruefen, ob ein anderer, SS_SEvent-faehiger pty-Treiber im SDK existiert und telnetd darauf umstellen. RE-Infrastruktur (Ghidra-Projekt, `FixEntry.java`/`ExportStrings.java`/`FindRefs.java`, Modul-Extraktor) im Scratchpad, per dokumentierten Schritten schnell reproduzierbar. **Option-B-Fortschritt (2026-07-14 nachmittags):** blockierender `Ev$Wait`-Aufruf exakt bei Offset `0x00000f4c` in `FUN_00000e5c` lokalisiert (14 Byte: `pea 0x7fff; move.l event,D0; moveq 1,D1; bsr Ev$Wait-Wrapper`), bestaetigt kein Timeout-Parameter vorhanden; zusaetzlicher Fund: `telnetdc` hat bereits einen (per `-t`, standardmaessig AUS) Idle-Alarm-Mechanismus ueber eine weitere `F$Event`-Unterfunktion (vermutlich `Ev$Link`), haengt aber an einem separaten Event-Objekt, nicht am gemeinsamen tty/net-Event. **Korrektur (2026-07-14 abends):** die vermutete Disassemblierungs-Fehlausrichtung gab es nicht — echte Erklaerung: der F$Event-Aufrufcode (0x53) steckt als Inline-Datenwort HINTER `trap #0`, nicht in einem Register (klassische OS-9-Konvention). Deshalb Live-Trace-Infrastruktur in Musashi gebaut (`M68K_TRAP_HAS_CALLBACK` + `Q9_TRAP_TRACE`-Env-Var in `m68krt.c`, liest Inline-Callcode bei PC+2 mit). Nach mehreren Iterationen (Performance-Fallstricke, falsche Register-Filter) steht der Code jetzt sauber (Build+Tests gruen), **der entscheidende Testlauf wurde aber noch nicht abgeschlossen** (Session unterbrochen). Testimage: `local_images/OS9SYS.claudia-optionb-trace.hda` (frischer Klon von OS9SYS.hda MIT Netzwerk-Autostart, anders als das aeltere `claudia-net-test.hda`). Voller Stand + naechste Schritte: `docs/re_telnetdc/OPTION_B_STATUS.md` |
| 5.16 | **Stray "." im Login-Prompt der Netzwerk-Terminals `/x1../x8` behoben (Andreas meldete: nach Verbindungsaufbau + ENTER steht schon ein nicht loeschbarer "." im Username-Feld, Login damit unmoeglich).** Root Cause per Live-Test gegen den laufenden Emulator gefunden: echte Telnet-Clients senden bei ENTER CR+LF (`0x0D 0x0A`), OS-9 kennt als klassisches serielles System nur ein einzelnes CR als Zeilenende. Das CR triggert `tsmon`, den Login-Prozess zu starten (Banner + `User name?: ` erscheint), das nachfolgende LF wird dabei aber NICHT mitverbraucht und bleibt im Ring-Puffer stehen — es ist damit das erste Byte, das der frisch gestartete Login-Prozess liest. OS-9s Zeileneditor kann mit diesem nicht druckbaren Zeichen nichts anfangen, stellt es als "." dar und behandelt es als bereits committetes erstes Zeichen des Benutzernamens (nicht mit Backspace loeschbar). Reproduziert per raw-Socket-Test gegen Port 2000: `\r\n` gesendet -> `.` im Prompt; nur `\r` gesendet -> sauberer Prompt. Kein Bug in `nettty.a` selbst, sondern fehlende Telnet-NVT-Zeilenenden-Normalisierung auf Emulator-Seite (die Server-Implementierung reicht jeden Byte roh durch, ohne jegliche Telnet-Protokoll-Behandlung — bisherige Tests liefen ueber rohe Sockets ohne LF, daher nie aufgefallen) | ✅ | Claudia | **Fix in `src/kernel/m68krt.c`/`q9board.h`:** neues Feld `last_was_cr` in `os9_uart_t` (ein Byte Zustand je Kanal); in `update_network_terminals()` wird ein LF verworfen (nicht in den Ring-Puffer/IRQ-Pfad uebernommen), wenn das direkt vorangegangene konsumierte Byte ein CR war — Reset des Flags bei neuer Verbindung und bei Verbindungsabbruch. Betrifft alle 8 Kanaele gleichermassen. **Verifiziert:** frischer Klon `local_images/OS9SYS.claudia-crlf-fix-test.hda` (Testklon bleibt lokal erhalten, nicht committet), frisch gebauter Emulator auf Port 2000 (alte, unterbrochene Option-B-Instanz dafuer mit Andreas' Freigabe beendet). Raw-Socket-Test: `\r\n` -> `User name?: ` ohne "."; kompletter Durchlauf `super`/Passwort/Willkommensbanner/`pd`/`logout` fehlerfrei, byte-genau, keine Fremdzeichen. Nebenbefund (kein Bug meines Fixes, vorbestehende Eigenart): ohne sauberes Logout haengt der OS-9-seitige Login-Prozess nach TCP-Disconnect unbegrenzt weiter (keine Carrier-Detect-Emulation) — ein neu verbundener Client auf demselben Kanal setzt dann die alte, noch wartende Session fort statt eine frische zu bekommen; nicht weiter verfolgt (ausserhalb des gemeldeten Problems). `make test` PASS, `make native` warnungsfrei (nur die bekannte Linker-Alignment-Warnung aus 5.13) |
| 5.11 | **QUICC-Ethernet-Emulation (68360 SCC1) im Q9-Emulator — Gegenstück zum fertig gebauten `sp360`-Treiber.** **Vorarbeit MWOS-Seite ist KOMPLETT erledigt (Claudia, 2026-07-12, interaktive Session mit Andreas):** Der originale Microware-SPF-Ethernet-Treiber `sp360` + Descriptor `spqe0` wurde aus `OS9/CPU32/PORTS/QUADS` in den `Q9`-Port übernommen (`OS9/68030/PORTS/Q9/SPF/`), für 68020/030 neu übersetzt (beide Module CRC Good, per ident verifiziert) und die KOMPLETTE SPF-Infrastruktur integriert: ETC-Konfiguration (`interfaces.conf`: enet0 = 10.0.0.1/24, binding `/spqe0/enet`, hostname `q9` — steckt in `inetdb2`!), neues Bootlist-Flavor `BOOTFILE/net.bl` (= dev + kompletter SPF-Stack aus `OS9/68020`: sysmbuf/pkman/pk*, spf, spip/sptcp/spudp/spraw/sproute + Descriptoren, netdb_local, spenet/enet, sp360/spqe0, inetdb/inetdb2, ipstart/ifconfig/ping/netstat/route/telnet(d)/ftp(d)/ndbmod/hostname/idbdump), Startskript-Vorlage `startspf`, CHANGELOG + context.txt aktualisiert. `net.bf` = 838.898 Bytes, 98 Module — passt NICHT ins 512K-ROM-Fenster (`$FE000000–$FE07FFFF`; das bisherige `romimage.dev` war mit 511.970 Bytes exakt an der Kante!), muss also per `os9gen` in den CF-Bootbereich (CBOOT bevorzugt das CF-Bootfile) oder zur Laufzeit per `load` von `/dd` kommen; `dev.bl`/`romimage.dev` blieben deshalb Byte-identisch unangetastet. **Was jetzt fehlt, damit der Treiber redet: die Emulator-Seite.** Der Treiber programmiert die 68360-QUICC-Register direkt — Quelle der Wahrheit für das zu emulierende Verhalten sind die Treiberquellen `MWOS/OS9/SRC/DPIO/SPF/DRVR/SPQUICC/` (`regs360.h` Register, `pram360.h` Parameter-RAM, `qedvr.h`/`init.c`/`isr.c` Ablauf). Zu emulieren (in `src/kernel/q9board.c` oder eigenem `quicc.c`, native-only wie gehabt): (1) Dual-Port-RAM + MBAR-relative Registerbank — Descriptor sagt PORTADDR `$22C00` (QUADS-Wert; entweder dort emulieren oder eigene Q9-Adresse wählen und `spf_desc.h` im MWOS-Q9-Port anpassen + neu bauen, Build-Weg via Wine-Toolchain `tools/macos/bin/os9make` ist dokumentiert und funktioniert). (2) SCC1 im Ethernet-Modus: GSMR/PSMR minimal, CPM-Kommandoregister (INIT RX/TX PARAMS, RESTART TX), RxBD/TxBD-Ringe im DPRAM (Buffer-Descriptor-Flags Empty/Wrap/Last/First abarbeiten wie eine echte SCC-DMA). (3) Interrupt-Pfad: SCC1-RX/TX-Events → IRQ mit Vector 254/IRQLEVEL 5 (Descriptor-Werte) über den bestehenden IACK-Mechanismus in `m68krt.c`. (4) Host-Backend für die Frames — Design offen, Optionen: (a) User-Mode-Mini-NAT im Emulator (ARP + ICMP-Echo beantworten, TCP/UDP per Host-Sockets NATten — kein Root, kein Kext; für ping/telnet reicht ARP+ICMP+TCP), (b) libslirp vendoren (BSD-lizenziert, fertige Lösung, aber größere Abhängigkeit), (c) UDP-Socket-Bridge à la QEMU (einfachster Code, braucht aber Gegenstelle). Empfehlung: mit (a) minimal starten. MAC des Descriptors: `00:73:39:33:36:30`. **Adressentscheidung (Andreas + Claudia, 2026-07-12): QUICC-Basis = `$FFFF2000`** (8K-Fenster `$FFFF2000`–`$FFFF3FFF`, kollisionsfrei geprüft gegen alle Decodes in q9board.c/m68krt.c; Netz-Terminals enden bei `$FFFF104F`, REMAP beginnt erst `$FFFF8000`). `spf_desc.h` im MWOS-Q9-Port ist bereits auf `PORTADDR $FFFF2C00` umgestellt, `spqe0` + `net.bf` neu gebaut und verifiziert (Adresse steht 2x im Descriptor, CRC Good). Treiber-Logik dazu (init.c/qedvr.h): Basis = PORTADDR & `$FFFFF000`, Kanal = Bits 8-9 (`$xC00` = SCC1), BDs liegen im DPRAM (`BD_BASE=0`), die eigentlichen Frame-Puffer sind mbufs im System-RAM (SDMA-Emulation = direkte Gast-RAM-Zugriffe). **Akzeptanzkriterien:** OS-9 bootet mit net.bf (CF-Bootbereich via os9gen, Klon-Image verwenden!), `ipstart` läuft fehlerfrei durch, `ifconfig` zeigt enet0 UP mit 10.0.0.1, `ping 10.0.0.2` (Host-Gegenstelle im Backend) bekommt Antworten, idealerweise `telnet` vom OS-9 zum Host oder umgekehrt; `make test` PASS, `make native` warnungsfrei; bestehende Boot-Wege (dev-ROM, CF ohne Netz) unverändert | ✅ | Claudia | **FERTIG (2026-07-12, interaktive Session mit Andreas — vom Eintrag bis ping am selben Abend).** `src/kernel/quicc.c/.h` neu (Fenster `$FFFF2000`, Offsets per offsetof aus Motorolas quicc.h verifiziert, BD-Ringe mit tbptr/rbptr-Fortschreibung, SCCE/SCCM/CIPR/CIMR-Pegellogik, IRQ 5/Vektor 254, Mini-NAT-Backend ARP+ICMP als 10.0.0.2), Dispatch+IACK in m68krt.c, Poll+IRQ in q9boardrun.c. **End-to-End verifiziert** (`test_quicc_net.exp`, Klon-Image): OS-9 bootet, `load netmods` (23 SPF-Module als Merge via ToolShed aufs Image) + `mbinstall` + `ipstart` laufen fehlerfrei, `ping 10.0.0.2` → "64 bytes from 10.0.0.2: ttl=255 time=0 ms", `netstat -i` zeigt enet0 UP, MAC 00:73:39:33:36:30, 10.0.0.1, 0 Fehler. `make test` PASS, `make native` warnungsfrei. **Wichtige Erkenntnis:** `sysmbuf` ist ein Coldstart-Systemmodul (Typ $C) — beim Laufzeit-Weg (load statt Bootfile) MUSS `mbinstall` vor `ipstart` laufen, sonst "sysmbuf is not installed"; ToolShed-kopierte Dateien brauchen `attr -e -pe` (Error 214 sonst). Statt Boot-Bereich-Chirurgie (os9gen mit net.bf) wurde bewusst der Laufzeit-Lade-Weg getestet — der net.bf-Boot via os9gen bleibt Andreas' Image-Pflege ueberlassen. **Ausbaustufe (neuer 💡 bei Bedarf):** TCP/UDP-NAT im Backend (dann telnet/ftp vom Gast zum Host), außerdem koennte das Backend statt Mini-NAT auch echte Host-Netzanbindung bekommen. HANDBUCH Abschnitt 3 + neues 5.11 aktualisiert. *(Nachtrag 2026-07-13: Adressierung im Zuge von 5.13 auf 192.168.200.2 [Gast] / 192.168.200.1 [Gegenstelle], Subnetz /16 umgestellt — die 10.0.0.x-Werte oben spiegeln den Stand zum Zeitpunkt dieses Eintrags.)* |
| 5.3 | Musashi ↔ CB030 verdrahten + Boot-ROM laden + Boot-Runner | ✅ | Claudia | Damit ist der Weg frei für den ersten Boot-Versuch mit dem echten Microware-ROM. → Details: ARBEITSPLAN_ARCHIV.md |
| 5.1 | Musashi als CPU-Kern einbinden — Grundbaustein + Rauchtest, KEINE Scheduler-/Syscall-Bridge-Entscheidungen (die kommen erst mit der Detailplanung). Analog zu 4.6 (wasm3): Makefile-Integration (Musashis Zweistufen-Build — `m68kmake` generiert `m68kops.c/.h` aus `m68k_in.c` zur Bauzeit, siehe `third_party/musashi/Q9_VENDOR.md`), schmaler Wrapper `src/kernel/m68krt.c/.h` (analog `wasmrt.c/.h`), CPU-Typ `M68K_CPU_TYPE_68030`. Rauchtest: ein von Hand geschriebenes/assembliertes 68k-Testprogramm (z.B. zwei Zahlen addieren) in emuliertes RAM legen, `m68k_pulse_reset()` + `m68k_execute()` aufrufen, Ergebnis über die emulierten Register prüfen | ✅ | Claudia | Makefile-Integration steht: `m68kmake` wird als Host-Tool gebaut, generiert `m68kops.c/.h` zur Bauzeit nach `build/native/musashi_gen/` (nicht… → Details: ARBEITSPLAN_ARCHIV.md |

**Konfigurierbare Hardware-Module (5.17–5.21)** — geplant 2026-07-14 abends,
gemeinsame Planungsrunde Andreas + Claudia (im Chat als „6.1–6.5" skizziert,
hier als 5.17–5.21 nummeriert, weil Phase 6 seit E11 für die Shell reserviert
ist). **→ Vollständige Design-Referenz mit allen Begründungen und dem
Referenz-Beispiel der Config-Datei: `docs/HWCONFIG.md`** — dort zuerst lesen,
die Tabellenzellen hier sind die Kurzfassung. **Ziel (Andreas):** (1) Hardware-Module einzeln erstellen und per
Konfiguration ins Board aufnehmen, (2) Descriptor-Angaben (Adresse, Name,
Vektor, Level, …) konfigurierbar — und zwar EINMAL für beide Seiten (Emulator
+ MWOS-Port), (3) maximale Geschwindigkeit mit möglichst wenigen
Adressabfragen, (4) lauffähig auf macOS, Windows und Linux.
**Dispatch-Design (Andreas' Idee, gemeinsam verfeinert):** RAM-Abfrage IMMER
zuerst (>99 % aller Zugriffe inkl. Opcode-Fetches: 1 Vergleich, direkter
Zugriff), dahinter Binärsuche über die nach Basisadresse sortierten
Geräte-Fenster (8 Geräte = 3 Halbierungen + 1 Bereichs-Check; heute laufen
bis zu ~10 Bereichsabfragen VOR dem RAM-Fall). REMAP wird Funktionszeiger-
Umschaltung statt Zustandsabfrage je Zugriff; 16/32-Bit-RAM-Zugriffe lesen
direkt big-endian statt 2×/4× durch den Byte-Pfad. Häufigkeits-Optimierung
(unbalancierter Baum / Hot-Device-Vorabcheck nach Zugriffszählern aus dem
Benchmark) als optionaler Feinschliff. **Einbindung der Module: statisch**
(alle Gerätetypen in einer expliziten Registry-Tabelle einkompiliert, die
Config entscheidet nur über die INSTANZIIERUNG — kein dlopen/LoadLibrary:
drei ABI-Pfade, wasm-untauglich, kein Nutzen bei Modulen dieser Größe);
plattformspezifische Backends wie bisher per `#ifdef`/Makefile
(Q9_HAVE_VMNET-Muster). Default ohne `--config` = exakt das heutige Board
(Adressen/Vektoren/Level identisch — bestehende Images und Descriptoren
laufen unverändert). Nach jedem Schritt: `make test` + Boot-Test gegen
Klon-Image mit Login über Port 2000.

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 5.17 | **Geräte-Interface + Registry, Bestandsgeräte umziehen.** `q9_device_t` (type/name/base/size/level/vector/state + vtable: read8/write8, optional read16/32+write16/32 — NULL = aus Byte-Zugriffen synthetisiert, CF behält eigene —, poll, irq_pending, reset); explizite Registry-Tabelle der verfügbaren Typen (bewusst KEINE Linker-Magie wie `__attribute__((constructor))` — portabel, wasm-tauglich, im Projektstil). Die sechs Bestandsgeräte (68681-DUART, CF, Timer-Trigger, RTC72421, nettty-Terminals, QUICC) EINZELN nacheinander umziehen, Verhalten unverändert. Damit verschwinden die hartkodierten Geräteketten an drei Stellen: Speicher-Dispatch (m68krt.c), Hauptschleifen-Poll (q9boardrun.c), IRQ-Ack/Reassert (m68krt_board_int_ack + reassert_pending_irq iterieren dann über registrierte Geräte nach Level) | ✅ | Claudia | **FERTIG (2026-07-14, alle sechs Geräte einzeln umgezogen + committet, s. Releases 1.68-1.73).** Neue Datei `src/kernel/devreg.c/.h` (Entscheidung E14, s. PROJECT.md): `q9_device_t`/Vtable wie geplant, zusätzliches Feld `level_held` (unterscheidet level-gehaltene IRQ-Quellen — DUART/Netz-Terminals/QUICC, nehmen an IACK/Reassert teil — von einmaligen Pulsen — Timer, wird bewusst NICHT reasserted, wie vor 5.17) sowie ein optionaler `irq_vector_fn`-Hook für Geräte mit laufzeit-/instanzabhängigem Vektor (DUART: OS-9-Treiber programmiert IVR selbst; Netz-Terminals: EIN Registry-Eintrag für alle 8 Kanäle, aber 8 verschiedene feste Vektoren 70-77 — Hook liefert den Vektor des ersten pendenten Kanals, exakt die alte Suchreihenfolge). Migrationsreihenfolge wie geplant (DUART→CF→Timer→RTC→nettty→QUICC), pro Schritt: `make test` PASS + Boot-Test gegen Klon-Image `local_images/OS9SYS.claudia-517-devreg.hda` (Kopie von `OS9SYS.claudia-crlf-fix-test.hda`, bleibt lokal) über die lokale DUART-Konsole (`test_517_boot.exp`); zusätzlich beim nettty-Schritt Login über Port 2000 (`test_517_nettty.py`, raw-Socket, bestätigt auch: kein 5.16-Regressions-Punkt) und beim QUICC-Schritt `ping 192.168.200.1`/`netstat -i` (`test_517_quicc_ping.exp`). CF/DUART/Timer/RTC: Instanzen zeigen weiterhin auf das gemeinsame `q9_board_t`-Board (kein Struct-Split nötig, nur der Dispatch-Aufruf wandert aus `cb030_read_byte`/`cb030_write_byte` in die Registry — die vier if-Blöcke dort sind komplett entfernt, q9board.c kennt danach nur noch REMAP-Trigger + RAM/ROM). nettty: EIN Registry-Eintrag für alle acht Kanäle (statt acht Instanzen) — `read8`/`write8` delegieren unverändert an die bestehenden `network_read8`/`network_write8`, die selbst schon über alle Kanäle suchen; damit bleibt auch der (praktisch nie auftretende) Randfall einer 16/32-Bit-Adresse an einer Kanalgrenze exakt erhalten. QUICC: delegiert unverändert an die bestehende `q9_quicc_*`-API (hatte schon eigene 16/32-Bit-Pfade). Registrierungsreihenfolge bewusst aufsteigend nach IRQ-Level gehalten (DUART 3, Netz-Terminals 4, QUICC 5, Timer 6 — Timer wird deshalb erst in `q9_m68krt_attach_quicc`, nach QUICC, registriert statt in `attach_board`) — wichtig, weil `q9_m68krt_set_irq()` nur eine kombinierte Leitung nachbildet und der letzte Aufruf je Runde gewinnt; dieselbe Reihenfolge galt vor 5.17 durch die hartkodierte Aufrufreihenfolge in `q9boardrun.c`. **Dokumentierte Nebeneffekte (bewusst in Kauf genommen, kein Funktionsverlust):** (1) `m68krt_reassert_pending_irq` prüft jetzt alle level-held Geräte einheitlich statt QUICC zuerst separat abzufragen — ändert die interne Tie-Break-Reihenfolge beim sehr seltenen Fall gleichzeitig anstehender Interrupts *innerhalb eines einzelnen Reassert-Aufrufs* (vorher 5,3,4 / jetzt 3,4,5), ohne Auswirkung auf Boot/Login/Netzwerk in den Tests. (2) `q9boardrun.c`s Hauptschleife fragt jetzt auch die Netz-Terminals über die generische `irq_pending`-Schleife ab (vorher nur DUART/QUICC/Timer) — rein additiv (nettty verwaltet sein IRQ ohnehin selbst über `network_irq_resync`), kann in Idle-Runden das 1ms-Sleep-Fenster minimal verkürzen, keine funktionale Änderung. q9board.c bekam ein neues Feld `timer_irq_pending` (transienter Ein-Runden-Merker für den Timer-Poll-Adapter). **Verifikation gesamt:** `make test` PASS nach jedem Schritt, `make native` durchgehend warnungsfrei; Selbsttests in kernel.c (5.2b/5.2c/5.5a/5.5b/5.2d) auf die neue Registry-API umgestellt (reine Testinfrastruktur-Anpassung). Doku: docs/HANDBUCH.md Abschnitt 5.12 (neu) + PROJECT.md Entscheidung E14 aktualisiert. |
| 5.18 | **Binärsuche-Dispatch + Benchmark** (Detailrunde Andreas + Claudia, 2026-07-14 spät). Bereichs-Array `{base, top, mem, dev}` — `mem != NULL` = direkter RAM/ROM-Zeiger (damit bleiben weitere/colored Speicherbereiche fast Haupt-RAM-schnell: Baum-Vergleiche, aber KEIN Funktionsaufruf, 16/32-Bit direkt big-endian), sonst Geräte-vtable mit Offset (`addr - base`). Gekapselt in `q9_busmap_t` (Haupt-RAM-Zeiger/-Länge für den Schnellpfad + sortiertes Array + Anzahl). **Aufbau beim Start (einmalig):** (1) Sammeln — Geräte melden ihre Fensterlänge SELBST aus dem Konstruktor (RTC=16, nettty=channels×$10, …), Config liefert nur base; `ram0` (Bereich ab 0) wird als Schnellpfad herausgezogen; (2) Insertion-Sort nach base; (3) Überlappungs-Validierung benachbarter Einträge → Fehlermeldung mit BEIDEN Instanznamen + Adressen, Abbruch; (4) ZWEI komplette busmaps bauen (Reset-Zustand mit ROM-Spiegel / Remap-Zustand mit RAM ab 0) — der REMAP-Trigger tauscht nur noch einen globalen Zeiger, kein Zustands-Check mehr im heißen Pfad. **Zugriff:** RAM-Vergleich zuerst (>99 % inkl. Opcode-Fetches, 1 Vergleich), dann Suche im Bereichs-Array; Loch im Adressraum liest 0/verwirft wie heute. **Suchstrategie (Nachtrag Detailrunde):** primär LINEARE Abfrage, nach Zugriffshäufigkeit sortiert (heißestes Gerät zuerst = 1 Vergleich für den häufigsten I/O-Fall; Sortierung aus den Benchmark-Zählern) — Andreas' berechtigter Einwand: bei nur ~8 Einträgen schlägt die Sprungvorhersage-/Cache-Freundlichkeit des sequenziellen Durchlaufs die theoretisch weniger Vergleiche der Binärsuche (datenabhängige Halbierungs-Branches = Fehlvorhersagen à ~15-20 Takte, Springerei über die 2 Cachezeilen des Arrays). Binärsuche (lo/hi-Halbierung = Andreas' ADR_MIN/ADR_MAX-Idee) als Rückfalloption implementieren und im A/B-Test messen — relevant erst, falls die Gerätezahl deutlich wächst (~32+). Adress-Trigger (REMAP, TI_IRQ_ON/OFF) werden Mini-Geräte mit Fenster — keine Sonderbehandlung im Dispatch. Benchmark einbauen (emulierte Zyklen/Wandzeit + Zugriffszähler je Eintrag), Vorher/Nachher + Linear-vs-Binär dokumentieren. **Nachtrag (Andreas + Claudia, 2026-08-13): dritte, schnellere Stufe fuer den festen I/O-Bereich.** Reale Adresskarte geprueft (nicht nur Basisadressen, s. `q9board.h`/`devices/*/*.h`): Framebuffer ($FD000000) und ROM-Remap ($FE000000) liegen weit ausserhalb des Geraete-Clusters $FFFF0000-$FFFFFFFF -- fuer GENAU diesen 64-KB-Cluster zusaetzlich zum sortierten Array eine **direkt indizierte Tabelle** (`index = (adresse - 0xFFFF0000) >> 8`, 256-Byte-Raster, 256 Eintraege à 1 Zeiger = 2 KB) mit echtem O(1)-Zugriff, kein Vergleich noetig. Deckt heute NICHT sauber ab: Netz-Terminals x1-x8 (nur 16 Byte auseinander) und MC6845/CLUT (ebenfalls 16 Byte) -- mehrere Geraete im selben 256-Byte-Slot. **Loesung dafuer bewusst NICHT im Dispatch-Code, sondern in der Adresskarte selbst** (Andreas kontrolliert beide Seiten, es ist keine echte Hardware): Mehrfachgeraete (Netz-Terminals) bekommen ein Index/Daten-Registerpaar in EINEM Fenster (Muster existiert schon bei MC6845 und bei DUART MRA/MRB `uart_mr_ptr_a/b`) statt eigener Adressen je Instanz -- damit passt alles ins 256-Byte-Raster, keine Sonderfaelle im Dispatch. **Config-seitig bewusst BEIDES anbieten:** ein Geraet bekommt entweder eine automatisch zugeteilte "Tabellenadresse" (garantiert 256-Byte-ausgerichtet, laeuft ueber die schnelle Tabelle) ODER eine frei eingegebene eigene Adresse (keine Ausrichtungsgarantie, laeuft ueber das sortierte Array/den Fallback-Scan) -- volle Flexibilitaet bleibt erhalten, die Tabelle ist ein Angebot, keine Pflicht. Migration bestehender Geraete (nettty/MC6845/CLUT) auf das Index/Daten-Muster ist eigene Arbeit auf BEIDEN Seiten (Emulator UND MWOS-Q9-Port-Descriptoren/Treiber, separates Repo) -- noch nicht eingeplant, nur als Zielbild festgehalten. | 🟡 | Claudia | Nach 5.17 (✅). Erwartung: deutlicher Boot-/Durchsatzgewinn, da heute JEDER RAM-Zugriff durch die komplette I/O-Kette läuft. **Erster Teilschritt FERTIG (2026-08-13): RAM-Fast-Path** (`m68krt.c` `ram_fast_hit()`) faengt RAM-Zugriffe im `remapped`-Zustand VOR der `devreg_hit()`-Registry-Suche ab -- die einzige Pruefung, die den Reset-Zustand (ROM-Spiegel statt RAM, s. `q9board.c` `board_read_byte`) korrekt ausschliesst, ohne Ueberschneidungsrisiko mit Geraeten (alle weit oberhalb von RAM). Verifiziert per Gegenprobe: Boot-Transkript mit und ohne die Aenderung (`git stash`) byte-identisch, `make test` 0 Fails. **Noch offen:** die 256-Byte-Tabelle fuer den festen I/O-Cluster, das sortierte Array mit Zugriffszaehlern fuer frei platzierte Adressen, das Index/Daten-Registermuster fuer Mehrfachgeraete (Netz-Terminals), Benchmark-Dokumentation. |
| 5.19 | **Board-Konfigurationsdatei + CLI** (Detailrunde Andreas + Claudia, 2026-07-14 spät). INI-artiges Textformat (C99-Parser ohne Fremdbibliothek, Kommentare mit `;`/`#`, Hex mit 0x, Größen-Suffixe K/M): **`[board]` mit `name=`** (Boot-Banner, Logs, Generator-Kommentarkopf); **Speicher als eigene Abschnitte** `type=ram|rom` mit base+size+optional `preload=<datei>` — MEHRERE RAM-Bereiche möglich (colored RAM, Andreas' Anforderung); `ram0` ab Adresse 0 = Schnellpfad, weitere Bereiche als mem-Direktzeiger im Baum (s. 5.18); ROM-Flag `mirror_at_reset` + Remap-Trigger als eigenes Gerät bilden die CB030-REMAP-Eigenart deklarativ ab. **Abschnittsname `[...]` = frei wählbarer INSTANZname, `type=` = MODULname** (so gehen mehrere Instanzen desselben Typs: `[xterm]`/`[xterm2]` beide type=nettty). **`size` NUR bei Speicher** — Geräte melden ihre Fensterlänge selbst (Hardware-Eigenschaft des Typs, vermeidet Config/Modul-Widersprüche); Geräte-Abschnitte: type, base, level/vector (Pflicht nur bei IRQ-Geräten), typspezifische Parameter mit Defaults im Modul (channels/port/image/backend/mac/period). **`os9_*`-Keys** (os9_color für MemList, os9_names, os9_driver, os9_fm) transportieren die rein OS-9-seitigen Angaben für den Generator 5.20 in DERSELBEN Datei — der Emulator ignoriert sie. Pfade in der Datei relativ zur Config-Datei (nicht zum CWD). `--config <datei>`; ohne Angabe `./q9board.cfg`; existiert auch das nicht: eingebaute Default-Config = heutiges Board byte-genau. Prioritätsreihenfolge: eingebaute Defaults → Config-Datei → CLI (bestehende Optionen --cb030/--cf/--net bleiben als überschreibende Kurzformen). Validierung beim Start: Fensterüberlappung, Vektor-/Level-Kollisionen, base mitten im RAM, unbekannte Typen, auf der Plattform nicht verfügbares Backend → klare Fehlermeldung mit Zeilennummer statt stillem Fehlverhalten | 🟡 | Claudia (AF) | **Teil 5.19a FERTIG (2026-07-16, interaktiv mit Andreas):** Config-DATEI als Einstiegspunkt + MEHRERE CF-Images. Umgesetzt: (1) **Erster Positionsparameter OHNE `-` = Config-Datei** (`q9.exe <config>[.q9] [optionen]`), fehlende Extension → `.q9` (Andreas' Vorgabe statt `--config <datei>`); bestehende Optionen bleiben und ÜBERSCHREIBEN die Config (Vorrang: Default < Config < CLI). (2) **INI-Parser `src/kernel/boardcfg.c/.h`** (C99, kein Fremdmodul, Kommentare `;`/`#`, Pfade relativ zur Config-Datei), Abschnitte `[board]` (name/rom/net) + beliebig viele `[cfN]` (type=rbf\|pcf, bus=onboard\|rc2014, unit=master\|slave, image=). (3) **CF-Emulation mehrfach instanziierbar** (`q9board.c`: neuer Typ `q9_cf_t`/`q9_cf_unit_t`, CF-Zustand aus `q9_board_t` herausgezogen): Onboard-CF `$FFFFE000` (c0..c3) + **RC2014-SC145-Zweitinterface `$FFFFC010` (e0/f0)**, je Master/Slave über das DEV-Bit in LBA3 (`$E0`/`$F0`, exakt die e0/f0-Descriptorwerte); `type=pcf` schaltet die RBF-256-Byte-Heuristik ab (FAT-Bootsektor würde sonst als LSN0 fehlgedeutet). (4) **Neue Test-Images** (512-Byte-Sektoren = funktionierende Buffergröße): `local_images/q9data-rbf.img` (RBF via ToolShed `os9 format -bs512 -c32`), `q9-fat12.img`/`q9-fat16.img` (neuer Generator `tools/make_fat_image.py`). **Verifiziert:** `make test` PASS (neuer Selbsttest „5.19: Board-Config-Parser"), config-gesteuerter Boot des echten OS9SYS-Klons bis Login (`super`) + `dir /dd` + Logout (`test_519_config_boot.exp`), alter CLI-Weg `--cb030 … --cf …` unverändert. Beispiel-Config `docs/q9board.example.q9`. **OFFEN (Rest von 5.19, weiter 💤):** Speicher-/Geräte-Abschnitte (`type=ram\|rom`, colored RAM, `[uart0]`/`[eth0]` usw.), die Instanziierung ALLER Geräte über die Typ-Registry, `os9_*`-Keys für den Generator (5.20), Fensterüberlappungs-/Vektorkollisions-Validierung — hängen an 5.18 (Binärsuche-Dispatch, noch 💤). Bis dahin bleiben die Nicht-CF-Geräte hartkodiert wie in 5.17 | 💤/🟡 | Claudia | Nach 5.18 (Rest); Teil 5.19a vorgezogen auf Andreas' Zuruf |
| 5.20 | **Descriptor-Generator für die MWOS-Seite.** Tool (Python, wie idbgen-Weg aus 5.14) liest DIESELBE Board-Config und erzeugt daraus die OS-9-Seite: `systype.d`-Fragmente + Descriptor-Quellen (`x1.a`-Muster) für den Q9-Port; gebaut wie gehabt über die Wine-Toolchain (os9make). Adresse/Vektor/Level stehen damit nur noch an EINER Stelle. Ausbaustufe (separat zu entscheiden): Descriptor-Module direkt binär erzeugen inkl. CRC (Modul-Format-Parser aus 5.15 existiert) — spart den Wine-Roundtrip, kostet einen eigenen Binär-Generator in der Pflege | 💤 | Claudia | Nach 5.19. Deployment ins Image bleibt wie gehabt (ToolShed + attr -e -pe + startup), ggf. Skript |
| 5.19b | **Echte CF-Descriptorzuordnung für das Q9-Hardwareprofil.** `c0.a` als Strukturvorlage: drei zukünftige CF-Fenster `CF_BASE_C0=$FFFFE000`, `CF_BASE_C0+$100=$FFFFE100` und `CF_BASE_C0+$200=$FFFFE200`; `e0/e1` teilen sich das dritte Fenster als zwei Partitionen desselben Backing-Images, nicht als Master/Slave. Descriptoren tragen Start-LSN (`LSNOffset`/`PD_LSNOffs`) und Länge (`PartSize`, soweit der Descriptor-Typ das Feld besitzt); `d0/e0` beginnen bei LSN 64, `e1` bei `0x200040` (1 GiB + 64). Ein späteres Master/Slave-Flag wird separat ergänzt. | 🟡 | Claudia (AF) | Config-/Offset-Unterstützung, lokale Images und Descriptor-Templates erstellt; ROM/Bootfile-Integration und Hardware-Decoder/Adressprüfung als separates Hardware-Workitem offen; Details: `docs/q9board.realprofile.md`, `docs/q9_cf_profile.a` |
| 5.21 | **Windows + Linux: Build- und Laufzeit-Parität.** (1) Winsock2-Shim (WSAStartup/closesocket/ioctlsocket, betrifft nettty-Server + Mini-NAT), (2) Netz-Backend-Abstufung: Mini-NAT (reine Sockets) = portabler Default auf allen drei OS, vmnet/BPF bleiben macOS-Extras, TAP (Linux)/npcap (Windows) als spätere eigene Vorschläge, (3) `make native` + `make test` auf allen drei OS grün (w64devkit-Pfad im Makefile existiert seit 0.1/1.10, conio-HAL vorhanden). Ergebnis: /x1-8-Terminals + TCP/IP via Mini-NAT überall; echtes LAN-Bridging zunächst nur macOS | 💤 | Claudia | Nach 5.19 (braucht die Config nicht zwingend, aber sinnvoll danach); Windows-Test auf Andreas' Desktop AF-PC nötig |
| 5.22 | **Descriptoren + Board-Config gemeinsam erzeugen, per Template-Binärpatch statt Cross-Compile.** (Planungsrunde Andreas + Claude, 2026-07-15, noch nicht ausgearbeitet/begonnen.) Idee: 5.19 (Board-Config) und 5.20 (Descriptor-Generator) nutzen bereits dieselbe Config-Datei (`os9_*`-Keys). Statt daraus per Wine-Toolchain (`os9make`/`r68`/`l68`) neue Descriptor-Objekte zu cross-compilen und aufs Image zu kopieren, sollen fertige, bereits kompilierte Descriptor-Module aus dem SDK als TEMPLATES dienen: pro Gerätetyp einmalig Offset+Typ (byte/word/long) jedes konfigurierbaren Feldes (Basisadresse, Level/Vektor, Kanäle, etc.) in der Konstantentabelle ermitteln (Descriptoren haben ein viel einfacheres, festeres Layout als Programmmodule wie `telnetdc`/`pkdvr`) — dieselbe Technik, die bei `telnetdc` schon funktioniert hat (Rohbyte-Patch + `fixmod -u` für die CRC, s. `docs/re_telnetdc/OPTION_B_STATUS.md`). Ablauf: (1) Config parsen, (2) pro Instanz das passende Template-Modul mit den Config-Werten patchen + CRC reparieren ("Pool" aus Templates + Patch-Routine), (3) alle gepatchten Descriptoren + Boot-Bootstrap-Code per `merge` (nativer OS-9-Tool, wie bereits in `BOOTFILE/bootfile.make` genutzt) zu einem NEUEN ROM-Image zusammenbauen — das ersetzt `romimage.dev.running.BIN` für genau diese Konfiguration. Eine Hardware-Konfiguration = ein generiertes ROM-Image, ausgewaehlt einfach ueber das bestehende `--cb030 <romfile>`-Argument — keine CF-Bootfile-Ladelogik zur Laufzeit noetig fuer die Descriptor-Seite. **ROM-Groesse:** aktuell 512 KB (`0xFE00_0000`–`0xFE07_FFFF`, entspricht dem realen CB030-Flash-Chip 29F040 — reale Hardware kann nicht veraendert werden, s. `docs/BOARD.md`); fuer mehrere/groessere Descriptor-Saetze ggf. auf 1 MB (oder mehr, Zweierpotenz fuer saubere Reset-Spiegelung) vergroessern. Adress-Check (2026-07-15): Basis muss dafuer vermutlich NICHT tiefer gelegt werden — der Bereich `0xFE08_0000` bis knapp unter `0xFFFF_0000` (~16 MB) ist komplett frei, ein groesseres ROM waechst einfach nach oben ohne Kollision mit Netz-Terminals (`$FFFF1010`), QUICC (`$FFFF2000`) oder REMAP-Registern (`$FFFF8000`+). Ein groesseres ROM ist bewusst eine Abweichung vom reale-CB030-Profil, passend zur 5.19-Richtung eigener virtueller Board-Profile. **Zurueckgestellt:** bestehende Descriptoren + `romimage.dev.running.BIN` reichen aktuell aus, kein akuter Bedarf.

**Nachtrag 2026-07-15 (Offset-Ermittlung fuer Descriptor-Felder gepruefft):**
Idee gepruft, ob `rdump` (ToolShed, `os9/os9rdump.c`; Wine-Fallback
`MWOS/DOS/BIN/rdump.exe`, lokal nicht in unser gebautes `os9`-Kommando
eingetragen — `os9_main.c` kennt nur `modbust`, kein `rdump`-Dispatch-
Eintrag, muesste bei Bedarf ergaenzt+neu gebaut werden) die Feld-Offsets
direkt aus einem assemblierten `.r`-File liefern kann. Ergebnis:
**NICHT wie angenommen.** `-g` (globale Definitionen) zeigt Descriptor-
Felder nicht, da sie in der geteilten Vorlage `OS9/SRC/IO/SCF/DESC/
scfdesc.a` als reine MODUL-INTERNE Struktur-Offsets angelegt sind, nicht
als global exportierte Symbole — der Treiber bekommt die Werte nicht per
Linker-Symbolaufloesung, sondern weil IOMan die Rohbytes zur Laufzeit in
eine feste `io.h`-Struktur kopiert (kein Cross-Modul-Linking noetig).
`-o` (lokale Offsets) zeigt zwar echte lokale Referenzen, aber OHNE
Namen (nur Offset+Typ-Flag, s. `showlcls()` in `os9rdump.c`) — daher
für unsere Zwecke nicht direkt nutzbar.

**Aber:** nicht noetig, da die Descriptor-Kopfstruktur FEST und fuer
ALLE SCF-Descriptoren IDENTISCH ist (direkt aus `scfdesc.a` ablesbar,
nur EINMAL zu ermitteln, nicht pro Geraetetyp neu):
```
Offset 0   Port      (4 Byte, Portadresse)
Offset 4   Vector    (1 Byte)
Offset 5   IRQLevel  (1 Byte)
Offset 6   Priority  (1 Byte)
Offset 7   Mode      (1 Byte)
Offset 8   FileMgr   (2 Byte, Namens-Offset)
Offset 10  DevDrv    (2 Byte, Namens-Offset)
Offset 12  DevCon    (2 Byte, Namens-Offset)
Offset 14  reserviert (8 Byte)
```
Nur die NACHFOLGENDEN, geraetespezifischen Abschnitte (Logical-Unit-
Static-Storage-Defaults, Path-Descriptor-Options — Baudrate, Kanalzahl
etc.) muessten pro Treiber/Geraetetyp einmalig aus der jeweiligen
`.a`-Quelle abgezaehlt werden, dieselbe Vorlage gilt aber fuer alle
Instanzen desselben Typs (z.B. `x1.a`..`x8.a` teilen sich `scfdesc.a` +
denselben Treiber, nur der Name/die Config-Werte unterscheiden sich). | 💤 | — | Nach 5.19/5.20; Idee/Grobkonzept vorhanden, noch nicht in Detailplanung |
| 5.23 | **Schnelle "virtuelle" Geraetetypen als Default-Profil (kein `--config`).** (Planungsrunde Andreas + Claude, 2026-07-15, noch nicht ausgearbeitet/begonnen.) Bisher orientieren sich alle emulierten Geraete an echter Hardware (Baudraten-Timing, Puffergroessen, Polling-Verhalten etc.). Zusaetzlich zu den hardware-getreuen Board-Profilen (5.19) sollen eigene, auf Geschwindigkeit optimierte virtuelle Geraetetypen entstehen — grosszuegige/"unendliche" Puffer, keine simulierte Latenz, keine Baudraten-Drosselung, kein unnoetiger Schnickschnak (Flow-Control-Timing etc.) — als DEFAULT-Board-Profil, wenn keine `--config` angegeben wird. Architektonisch simpel: ueber die Geraete-Registry (5.17, fertig) einfach weitere `q9_device_t`-Implementierungen mit grosszuegigen Defaults, ausgewaehlt ueber ein eigenes Default-Profil im Board-Config-System (5.19) — keine neue Kernarchitektur noetig. **Wichtige Abgrenzung (2026-07-15 festgehalten):** Das beschleunigt nur die EMULATOR-seitige Hardware-Simulation, behebt aber NICHT Bugs wie den aktuell verfolgten `pkdvr`/`SS_SEvent`-Haenger (s. `docs/re_telnetdc/OPTION_B_STATUS.md`) — der sitzt im geschlossenen OS-9-GAST-Kernel (Treiber-interne Ev$Wait-/Puffer-Logik), unabhaengig davon wie schnell oder ideal das darunterliegende simulierte Geraet ist. Schnellere virtuelle Hardware ist also kein Workaround fuer Guest-seitige Kernel-Bugs, sondern ein separates Ziel (reine Emulator-Performance/-Bequemlichkeit) | 💤 | — | Nach 5.17 (Voraussetzung erfuellt) und 5.19; Idee/Grobkonzept vorhanden, noch nicht in Detailplanung |
| 5.24 | **MC6845-Register-Simulation + Video-Modus-Feld.** (Planungsrunde Andreas + Ada, 2026-08-03.) Neues `q9_device_t` fuer die MC6845-CRT-Controller-Register, MMIO-Fenster ab `0xFFFFA000` (Adresse bereits in Q9-Frame `WORK_PACKAGES.md` WP03 festgelegt). Register R0-R15 wie bei echter Hardware (Timing/Geometrie/Cursor-Start-Ende/Start-Adresse); daraus wird intern ein **6845-Info-Objekt** abgeleitet (Breite/Höhe/Stride aus den Timing-Registern). **Wichtige Ergänzung ggü. echter Hardware:** ein zusätzliches, nicht-6845-echtes **Video-Modus-Feld** (INDEXED1/2/4/8, RGB565, RGB555I, RGB888 — identisch zu `Q9VideoMode` aus Q9-Frame) wird ins Info-Objekt aufgenommen, da der reale MC6845 selbst keine Bittiefe kennt (reiner Adress-/Timing-Generator) — ohne dieses Feld wüsste niemand, welches Pixelformat gerade aktiv ist. **FERTIG (2026-08-03):** neue Datei `src/kernel/mc6845.c/.h` — klassisches Index/Daten-Registerpaar (`+0` waehlt R0-R18, `+1` liest/schreibt), alle Register frei R/W (dokumentierte Vereinfachung, analog RTC72421), kein IRQ. **Geometrie-Vereinfachung (im Header dokumentiert, mit Andreas abgestimmt):** R1 direkt als Bytes/Zeile (Stride), R6 direkt als Pixelhoehe (R9/MaxScanLine nicht zur Vervielfachung herangezogen) — bewusste Abweichung von echter 6845-Texttreue zugunsten eines reinen Grafikmodells; R0-R3 zaehlen wie beim echten Chip in Byte-/Zeichentakt-Einheiten, die daraus resultierende PIXEL-Granularitaet haengt vom Modus ab (8px-Schritte bei 1bpp, 1px bei 8bpp, Vielfache von 2/3 bei 16/24bpp noetig). R18 = eigenes Video-Modus-Register (0-6, Q9_MC6845_MODE_*; R16/R17 = VideoClk, s. 5.25). Registriert in `q9_m68krt_attach_mc6845()` (m68krt.c/.h, analog attach_quicc/attach_cf), Aufruf aus `q9boardrun.c` nach `attach_quicc`. Makefile ergaenzt (BOARD_SRC/BOARD_HDR). **Verifiziert:** `make native` baut fehlerfrei, eigenstaendiger Registertest (Adress-/Datenregister-Roundtrip, Stride/Hoehe/Modus-Ableitung, Index-Begrenzung bei ungueltigem Wert) 5/5 gruen, `q9.exe --help` startet weiterhin fehlerfrei (kein Boot-Regressionstest gegen echtes Image bisher, da rein additives neues Geraet ohne Beruehrung bestehender Adressfenster) | ✅ | Ada | Erster Schritt der neuen Video-Integration; Grundlage für 5.25-5.32 |
| 5.25 | **Si5351A-Clock-Generator-Simulation (vereinfacht, kein I2C).** Der reale Baustein wird über I2C angesprochen und erzeugt per PLL den variablen Video-Dot-Clock für den MC6845. Simuliert wird nur das Ergebnis: direkte Registerschreibzugriffe (kein I2C-Bus-Protokoll nachgebildet — bewusste Vereinfachung, Andreas 2026-08-03), daraus berechnete Ausgabefrequenz wird ins 6845-Info-Objekt (5.24) mit aufgenommen. **Design-Entscheidung (Andreas, 2026-08-03): KEIN eigenes `q9_device_t`,** stattdessen zwei weitere Register auf demselben MC6845-Gerät ("können wir dann immer noch machen wenn wir es mal brauchen") — R16/R17 "VideoClk High/Low", 16-Bit-Wert in kHz, kein PLL-Multiplikator/Divider-Modell. **Reihenfolge auf Andreas' Wunsch:** VideoClk VOR Mode (R16/R17 als Registerpaar auf geradem Index, Mode als Einzelregister auf R18 danach ebenfalls gerade) — Mode dafür von R16 nach R18 verschoben. **FERTIG (2026-08-03):** `q9_mc6845_videoclk_khz()` in `mc6845.c/.h` ergänzt (liest R16:R17 als zusammengesetzten 16-Bit-Wert), `Q9_MC6845_NUM_REGS` 17→19, Header-Dokumentation + Registertest erweitert (VideoClk-Roundtrip mit 25175 kHz/VGA-Pixeltakt als Beispielwert). **Verifiziert:** eigenstaendiger Registertest 6/6 gruen, `make native` weiterhin fehlerfrei | ✅ | Ada | Nach 5.24 (Info-Objekt muss existieren) |
| 5.26 | **Framebuffer als `q9_device_t` + Dirty-Tracking.** VRAM-Fenster ab `0xFD000000` (Q9-Frame `WORK_PACKAGES.md` WP02), konfigurierbare Größe (Default 1 MB, max. 16 MB). Dirty-Tracking direkt in `write8`/`write16`/`write32` des Geräts abgreifen (jeder CPU-Schreibzugriff markiert nur seinen Bereich, kein Scannen des kompletten VRAM) — Umsetzung des Prinzips aus Q9-Frame `ARCHITECTURE.md`, hier erstmals tatsächlich verdrahtet. Wiederverwendbar: die gedeckelte Rechteck-Liste mit Überlapp-Merge aus `Q9-Frame/tests/dummy_server.cpp` (13x Bandbreiten-Gewinn ggü. einfacher Bounding-Box, siehe dortige Historie). **FERTIG (2026-08-03):** neue Datei `src/kernel/framebuf.c/.h` — Speicher wird wie RAM/ROM in `q9boardrun.c` extern statisch bereitgestellt (kein malloc, `q9_framebuf_init(&fb, vram, cap, size, &crtc)`), Dirty-Rects sind Byte-Spalte×Zeile (NICHT Pixel — die Byte→Pixel-Umrechnung ist bewusst Sache von 5.27, da sie vom Video-Modus abhängt), Zeilenlänge kommt aus `q9_mc6845_stride()` (5.24); ohne programmierten Stride gilt der gesamte Puffer als eine Zeile (kein Div-durch-0). Eigene `read16/32`/`write16/32`-Pfade (nicht aus `write8` synthetisiert), damit ein mehrbytiger Zugriff EIN zusammenhängendes Rect bleibt statt zwei angrenzender, die sich wegen der strikten Überlapp-Prüfung nicht von selbst verschmelzen würden. Registriert in `q9_m68krt_attach_framebuf()` (m68krt.c/.h), Aufruf aus `q9boardrun.c` nach `attach_mc6845`. Makefile ergänzt. **Bewusst vertagt:** Board-Config-Anbindung für eine vom Default abweichende Größe (analog CF/5.19) — fester Compile-Zeit-Default, kein Blocker für 5.27. **Verifiziert:** `make native` baut fehlerfrei, eigenständiger Test (Init, Byte/Word/Long-Dirty-Mapping inkl. Zeilengrenzen-Überlauf, Merge bei echter Überlappung, Liste-voll-Invariante) 14/14 grün, `q9.exe --help` weiterhin fehlerfrei | ✅ | Ada | Kann parallel zu 5.24/5.25 starten, Kopplung ans Info-Objekt erst bei 5.27 nötig |
| 5.27 | **Host-Video-Bridge: TCP-Video-Service + UDP-Discovery im Emulator.** Bisher spricht nur der Test-Dummy (`Q9-Frame/tests/dummy_server.cpp`) das Q9-Frame-Protokoll (VIDEO_INFO/PALETTE/FRAME_FULL/FRAME_UPDATE + UDP-Discovery-Antwort). Dessen Architektur (Dirty-Rect-Merge-Liste, Palette-Versand bei indizierten Modi, Live-Moduswechsel via erneutem VIDEO_INFO+PALETTE+FRAME_FULL mitten in der Verbindung) wird in den Emulator portiert bzw. adaptiert, jetzt gespeist aus dem ECHTEN VRAM (5.26) + 6845-Info (5.24/5.25) statt synthetischen Testmustern. Läuft als eigener Host-Service neben der CPU-Emulation (Q9-Frame `ARCHITECTURE.md`: Host-Service darf die Emulation nie blockieren). **FERTIG (2026-08-03):** neue Datei `src/kernel/videobridge.c/.h`. Alle Sockets O_NONBLOCK (Muster wie `m68krt.c` Netz-Terminals), `q9_videobridge_poll()` einmal je Hauptschleifen-Runde, kehrt immer sofort zurueck. Grosse Payloads (volles Frame) werden ueber einen statischen Sendepuffer (`out_buf`, bis zu VRAM-Fenstergroesse) mit resumierbarem `send()` ueber mehrere Poll-Runden gestreut — kein Blockieren selbst bei langsamem Client/vollem Kernel-Sendepuffer. Byte→Pixel-Umrechnung der Dirty-Rects (framebuf.h liefert Byte-Spalte×Zeile) ueber neue `q9_mc6845_bpp()/width_px()` (mc6845.c). **Zusaetzliches Register R19 "Netz-Update-Rate" (Hz, Andreas' Wunsch 2026-08-03: "ein Register, wie oft der Frame aktualisiert wird")** — R19=0 (Reset) faellt auf `Q9_MC6845_NET_HZ_DEFAULT`=30 zurueck (deckungsgleich mit `ARCHITECTURE.md` "update_hz"). **Bewusste Vereinfachungen ggue. Zielarchitektur (im Header dokumentiert):** genau EIN aktiver Client (kein Multi-Client-Sessionmanagement), kein Backpressure/Queueing bei langsamen Clients (Schreibfehler -> Verbindung wird getrennt statt Updates zu sammeln), CLUT fuer indizierte Modi ist eine Graustufen-Rampe (`build_grayscale_clut`-Portierung) mangels eigenem CLUT-Geraet. **Verifiziert per echtem Live-Test** (eigenstaendiger Testserver + Python-Client gegen echte Sockets, s. scratchpad, NICHT gegen ein reales Boot-Image): UDP-Discovery-Antwort, TCP-Handshake (VIDEO_INFO+PALETTE+FRAME_FULL mit korrektem Inhalt), Dirty-Update nach echtem VRAM-Schreibzugriff (korrekte Byte→Pixel-Umrechnung), Ablehnung eines zweiten gleichzeitigen Clients, Live-Geometriewechsel (Hoehe 4→6) loest korrekten Re-Handshake auf bestehender Verbindung aus — alle 5 Szenarien gruen. `make native` fehlerfrei, `q9.exe --help` weiterhin ok | ✅ | Ada | Nach 5.24, 5.25, 5.26 (braucht echte Daten aller drei) |
| 5.28 | **OS-9-Treiber + Descriptoren für MC6845 und Si5351A.** Guest-seitiger Zugriff auf die Register aus 5.24/5.25, analog zum bereits etablierten Custom-Status-Call-Muster von `/d0` (`SS.BlkRd`/`SS.BlkWr` über `I$GetStt`/`I$SetStt`, s. Q9-Flux `docs/DEVICES.md` — dieses Muster stammt aus dem GUEST-seitigen wasm3-C-Kernel, hier nur als KONZEPT übernommen, kein Codeklau). Descriptor-Quellen fürs MWOS-Q9-Board (analog `x1.a`-Muster aus 5.20). **FERTIG (2026-08-03), Grundlage per Recherche in echten MWOS-SDK-Vorbildern (`null.a`, `ram.a`, `sc16c954.a`/`oxc16954.a`, `scfdesc.a`, `funcs.a`/`module.a`/`sysio.a`) statt geraten:** neue Dateien `MWOS/OS9/68030/PORTS/Q9/SCF/mc6845.a` (Treiber) + `crtc0.a` (Descriptor) + `scf_mc6845.make`; `SCF/defsfile` um MC6845/VideoClk-Registerindizes (R_HTOTAL..R_NETHZ, NUM_REGS=20) + zwei eigene Custom-Status-Codes erweitert (`SS.RegRd`=$100, `SS.RegWr`=$101 — eigene Codes MÜSSEN ab $100 beginnen, da $00-$FF laut `funcs.a` für Microware reserviert sind); `systype.d` um `_MC6845Base`=`$FFFFA000` + `crtc0`-Makro (identisches `SCFDesc`-Muster wie die bestehenden `x1..x8`-Netz-Terminal-Deskriptoren — auch dort sind Parity/Baudcode bedeutungslose Füllwerte für ein Nicht-Seriell-Gerät, exakt derselbe Kniff hier übernommen) erweitert; `SCF/scf_descriptors.make` + `SCF/makefile` um den neuen Deskriptor/Treiber ergänzt. **Treiber-Design (Vorbild `null.a`, Microware-Original):** Read/Write geben sofort `E$EOF`/Erfolg zurück (kein Zeichenstrom), GetStat/PutStat kennen ausschließlich `SS.RegRd`/`SS.RegWr` (d1.w=Registerindex, d1.b=Wert bei Rd bzw. d2.b=Wert bei Wr) — intern exakt der reale 6845-Zugriffsablauf: Index ins Adressregister (`V_PORT(a2)`), Wert aus/ins Datenregister (`V_PORT+1`). Ungültiger Index → `E$Param`, unbekannter Statuscode → `E$UnkSvc`, beides Carry-Flag-Konvention wie im gesamten SDK. **Verifiziert:** beide neuen `.a`-Dateien assemblieren mit dem ECHTEN `r68.exe` (Wine, V2.9.1) 0 Fehler — Gegenprobe mit absichtlich fehlerhaftem Mnemonic bestätigt, dass der Assembler Fehler auch wirklich meldet (kein stiller No-op). **Noch NICHT verifiziert (kein Boot-Test):** tatsächliches Verhalten von `I$Open`/`I$GetStt`/`I$SetStt` gegen ein laufendes echtes OS-9-Image — das wäre der nächste Schritt vor 5.29, sobald ein Testprogramm existiert, das den Pfad öffnet und ein Register liest/schreibt | ✅ | Ada | Nach 5.24/5.25 (Register-Modell muss auf Host-Seite stehen) |
| 5.29 | **OS-9-Framebuffer-Treiber (Grundfunktionen).** Guest-seitiger Treiber mit den Basisoperationen: löschen, Pixel setzen, Linie, CLUT laden, Font/Text, Cursor. Naheliegende Grundlage: die bereits fertige, portable C89-Bibliothek **Q9-Ink** (`q9ink_set_pixel`/`q9ink_get_pixel`, laufzeit-modusfähig über alle 7 Videomodi, bereits als 68020-Assembler verifiziert gegen den echten `r68`-Assembler — s. dortige Projekt-Historie) — entweder direkt als Tiny-C-Quelle (Q9-QCC) kompiliert oder die verifizierte 68k-Assembler-Fassung übernommen. **Offene Frage:** Font-Glyphen-Quelle (welche Schriftart, eingebettete Bitmap-Tabelle) noch zu klären. **Hinweis Andreas (2026-08-03):** vermutlich schon vorhanden unter `/dd/GDP/FONTS` auf einem echten OS-9-Image (MC68000-Font) — noch nicht verifiziert, da OS-9-interner Pfad (kein Host-Dateisystempfad), müsste aus einem gemounteten Image extrahiert werden. **TEILWEISE FERTIG (2026-08-03) — Clear/SetPixel/GetPixel, Linie/CLUT-Laden/Font-Text bewusst vertagt:** neue Dateien `MWOS/OS9/68030/PORTS/Q9/SCF/gdp.a` (Treiber) + `vram0.a` (Descriptor) + `scf_gdp.make`; `systype.d` um `_VRAMBase`=`$FD000000` + `vram0`-Makro erweitert (identisches Muster wie `crtc0`/`x1..x8`); `SCF/defsfile` um drei neue Custom-Status-Codes erweitert (`SS.FBClear`=$110, `SS.FBSetPx`=$111, `SS.FBGetPx`=$112 — eigener Nummernkreis ab $110, getrennt von mc6845.a's $100/$101, rein zur Übersichtlichkeit). **Design:** Bit-Packungslogik für alle 7 Modi 1:1 aus der bereits verifizierten `Q9-Ink/tests/q9ink_set_pixel_asm_test.s` übernommen (BFINS/BFEXTU für INDEXED1/2/4, direkter Byte/Wort/Dreifachbyte-Zugriff sonst), aber an GetStt/SetStt statt eines `Q9InkCanvas`-Structs angepasst — kein malloc, schreibt direkt ins echte VRAM (`V_PORT(a2)`); Geometrie (Stride/Höhe/Modus) wird bei JEDEM Aufruf frisch direkt vom MC6845-Registerpaar gelesen (Direktzugriff auf `$FFFFA000`, kein Umweg über den mc6845-Treiber — beide laufen im selben Supervisor-Adressraum, analog eng gekoppelter realer Hardware-Treiber). `SS.FBClear` löscht den AKTIVEN Frame (Stride×Höhe), nicht das komplette VRAM-Fenster. Breiten-Bounds-Check pro Pixel-Aufruf wird modusabhängig aus dem Stride abgeleitet (identische Granularitätstabelle wie in `mc6845.h`). **Cursor entfällt hier bewusst** (Design-Entscheidung Andreas, 5.30: Cursor wird nur client-seitig in Q9 Frame gerendert). **Verifiziert:** beide neuen `.a`-Dateien assemblieren mit dem echten `r68.exe` 0 Fehler (inkl. der komplexeren 68020-Befehle `BFEXTU`/`DIVU.L`); Register-Logik in allen 7 Modus-Zweigen manuell gegen die Q9-Ink-Vorlage nachverfolgt (u.a. einen Bug beim ersten Entwurf gefunden+behoben: `GetPixel`-Erfolgspfad hätte den gelesenen Pixelwert mit `moveq #0,d1` überschrieben statt nur das Carry-Flag zu löschen). **Noch offen:** Linie, CLUT-Laden, Font/Text (Font-Quelle-Frage weiterhin ungeklärt), sowie wie bei 5.28 kein Boot-Test gegen ein echtes Image.

**Update (2026-08-03, großer Sprung): echte GDP-Kompatibilität statt eigenem Schema.** Andreas fand auf einem echten Disk-Image (`VCS OS-9 Windows V3.3`, 1991, "(C)1989 by VCS/HV") das komplette historische Microware/VCS-**GDP**-Grafiktreiber-Subsystem (für einen HRG-82786-Grafikprozessor) samt `DEFS/gdp.d` (alle echten `SS_xxx`-Statuscodes + Struct-Layouts) und — entscheidend — `C/ss_gdp.a` (die C-Bibliotheks-Assembler-Quelle, die die EXAKTE Registerbelegung jedes Aufrufs zeigt: Aufrufer legt Pfad in D0, Code in D1 ab, Parameter in D2/D3/D4/D5/A0/Stack; der Kernel wandelt das für den Treiber in D0.w=Code (aus D1), A1=Pfad-Deskriptor (aus D0) um, D2/D3/D4/D5/A0 laufen durch — kein Raten nötig). Auf Andreas' Wunsch ("wäre es möglich die Aufrufe kompatibel zu machen?") **komplett umgebaut:** `gdp.a` v2 implementiert jetzt die ECHTEN Codes `SS_Move`/`SS_MvRel`/`SS_Point`/`SS_Color`/`SS_Line`/`SS_Circle` (Werte 1:1 aus `gdp.d` in `SCF/defsfile` übernommen, inkl. aller nicht implementierten Codes dazwischen, damit die Nummern nicht verrutschen). **Plotter-Modell** (wie im echten GDP): `SS_Move`/`SS_MvRel` setzen eine "aktuelle Position" (neue Static Storage: `DrawX`/`DrawY`/`FgCol`/`BgCol`, per `vsect`), `SS_Point`/`SS_Line` zeichnen relativ dazu und aktualisieren die Position auf den Endpunkt; `SS_Circle` zeichnet nur um die aktuelle Position (kein dx/dy, echtes Verhalten). Interner Kern `PlotPixel` (mode-aware wie zuvor) ist jetzt ein "schwarzer Kasten", der ALLE benutzten Register inkl. D1/D2/D3 sichert/restauriert (nötig, da Linie/Kreis zu viele gleichzeitig lebende Werte für die wenigen freien Datenregister brauchen) und Punkte außerhalb der Geometrie still verwirft (Clipping-Verhalten statt Fehler). `SS_Line` = Bresenham (alle Oktanten, Wikipedia-Variante, Konstanten auf dem Stack statt in Registern). `SS_Circle` = Midpoint-Algorithmus, 8-fache Symmetrie. Eigene Erweiterung `SS.FBGetPx` (weit außerhalb des realen Codebereichs bei `$1000`) bleibt für Debug-Zwecke. **Fenster-Manager bewusst weggelassen** (Andreas: "den lassen wir erstmal weg") — die drei gefundenen echten Testprogramme (`mal.c`/`b.c`/`rotcr.c`) öffnen alle `/g` über den vollen Fenster-Manager (Bitmap-/Fenster-Verwaltung), was wir nicht haben; unser Treiber ist ein einziger impliziter Framebuffer ohne Fenster/Bitmap-Objekte. **Verifiziert:** `gdp.a` v2 assembliert UND LINKT (gegen `sys.l`+`scfstat.l`) fehlerfrei mit dem echten `r68.exe`/`l68.exe` — `file` erkennt das Ergebnis korrekt als vollständiges OS-9-Treibermodul, `M$Mem`-Feld zeigt exakt 8 Byte Static Storage (= die vier neuen Wort-Felder), bestätigt dass der `vsect`-Mechanismus für eigene Treiber-Static-Storage wie erwartet funktioniert. Ein Syntaxfehler beim ersten Anlauf gefunden+behoben (Label ohne Anweisung, nur Kommentartext dahinter). **Als Nächstes (mit Andreas so vereinbart):** ein kleines eigenes Test-Assemblerprogramm schreiben, das `/vram0` direkt öffnet und dieselben echten `SS_xxx`-Aufrufe macht (ohne Fenster-Manager-Umweg), dann ECHTER Boot-Test gegen ein (zu klonendes) OS-9-Image, visuell über Q9-Frame verifiziert. **Noch offen/vertagt:** Rect/Fill/Clip (Registerbelegung bereits im defsfile dokumentiert), CLUT (`SS_clut`/`SS_clutall` — braucht noch ein echtes Paletten-Gerät auf Host-Seite, s. 5.27-Lücke), Font/Text (Font-Quelle-Frage weiterhin offen, evtl. lösbar über die auf derselben Diskette gefundenen echten `.font`-Dateien).

**Update (2026-08-04, echter Boot-Test — Andreas unterwegs, autonom weitergemacht):** Kompletter Deployment-Weg etabliert und verifiziert: Module über die echte `os9make`/Wine-Toolchain gebaut (nicht nur Host-Testassemblierung), per ToolShed (`MWOS/tools/macos/bin/os9 copy/attr/ident`, ganz OHNE Emulator-Boot) direkt in ein Disk-Image eingespielt. **Eigener isolierter Test-Klon** `local_images/OS9SYS.gdp-test-20260803.hda` angelegt (nicht das produktive `OS9SYS.hda`, da dessen viele jüngste Backups auf aktive parallele QCC/Parsec-Arbeit hindeuteten — bewusst nicht angerührt). Deployment-Skript `test/expect/test_gdp_5_29.exp`/`test_gdp_5_29_run.exp` (neu, Vorbild `apply_idbgen_5_14.exp`) bootet, loggt ein (`super`/`Al35uUbC`), lädt alle vier Module per `load /dd/CMDS/BOOTOBJS/<name>` (voller Pfad nötig, `load` sucht nicht automatisch in BOOTOBJS) — **alle vier laden fehlerfrei, per `mdir` bestätigt.**

**Test-Programm rein in Assembler** (nicht C — der Versuch, `ss_gdp.a` reinzupasten und mit klassischem `cc`+`clib.l` zu linken, scheiterte an einer fehlenden `xdef`-Exportdeklaration und undurchsichtigem `l68`-Verhalten; direkter Weg über `I$Open`/`I$SetStt`/`F$Exit` mit der aus `docs/SYSCALLS.md` bestätigten Registerkonvention war zuverlässiger). **Echter Bug gefunden+behoben:** `mc6845.a`s ursprüngliche SS.RegRd/RegWr-Konvention (D1=Registerindex) war falsch — D1 trägt beim Sprung in den Treiber IMMER NOCH den SS-Code selbst (Kernel kopiert D1→D0, löscht D1 dabei nicht), kollidiert also mit einem Index-Parameter. Auf D2=Index/D3=Wert umgestellt (deckt sich mit der echten, verifizierten GDP-Konvention). Treiber neu gebaut, im Testimage ersetzt.

**Nach dem Fix: alle SS-Aufrufe melden Erfolg** (Diagnose-Ausgaben `123456789A` = jeder Schritt bestätigt, inkl. drei `SS.RegWr`-Aufrufe zur MC6845-Konfiguration + neun `SS_xxx`-Zeichenaufrufe). **Aber:** eine Rückprobe (sofort nach dem Schreiben von R1=80 wieder auslesen) zeigt `$00` statt `80` — die Video-Bridge (host-seitig, Live-Verbindung während der laufenden Session) bestätigt ebenfalls `VIDEO_INFO: 0x0 stride=0`, die Konfiguration kommt also nachweislich NICHT im Host-Zustand an, obwohl der Treiber-Code keinen Fehler meldet. **Tiefere Diagnose:** ein temporärer Status-Code, der `V_PORT(a2)` roh zurückgibt, zeigt `$000001FF` — das ist exakt der eigene Diagnose-Statuscode selbst, NICHT `$FFFFA000`! Reale SDK-Treiber (`scpio117.a` u.a.) lesen `V_PORT(a2)` überall nur, setzen es nirgends selbst — die Erwartung ist also, dass der KERNEL es aus dem Deskriptor (`M$Port`, im Deskriptor-Dump bestätigt korrekt `$FFFFA000`) automatisch befüllt. Warum das hier nicht der Fall ist (Static-Storage-Layout, `V_PORT`-Symbolauflösung, oder ein Zusammenhang mit den bereits dokumentierten PMMU-Themen des Projekts) — **noch ungeklärt, braucht tieferes Kernel-Wissen oder Reverse-Engineering** (ähnlich wie die frühere `SS_Ready`/`Ev$Wait`-Untersuchung in 5.15). Diagnose-Code + Testprogramm liegen im Scratchpad, nicht committet. Alte, unbeteiligte `dummy_server`-Testinstanz (Port 2001, aus früherer Session-Arbeit) als Störquelle gefunden+beendet, bevor die eigentliche Ursache klar wurde.

**Update (2026-08-04 Nacht, autonom weitergemacht während Andreas schlief): V_PORT-Ursache gefunden — Treiber-Fix, aber tiefere Kernel-Baustelle entdeckt.** Per Vergleich mit dem echten, funktionierenden `sc16c954.a` (Zeile `move.l M$Port(a1),a3` / `move.l a3,V_PORT(a2)` in dessen `Init`) bestätigt: bei Microware ist NICHT der Kernel dafür zuständig, `M$Port` in `V_PORT` zu kopieren, sondern JEDER Treiber selbst in seiner eigenen `Init`-Routine (einmalig beim ersten Öffnen). `mc6845.a`s `Init` war komplett leer (reines No-op, Edition 2) — das war der eigentliche Fehler. **Fix (Edition 3):** `Init` kopiert jetzt `M$Port(a1)` nach `V_PORT(a2)`, exakt wie `sc16c954.a`. Neu gebaut, deployt, erneut getestet — Rückprobe zeigt aber weiterhin `$00` statt `80`. **Weiter diagnostiziert (temporäre Test-Codes, wieder entfernt):** `(a1)` und `(a2)` sind beim Sprung in `GetStat` selbst KEINE gültigen Zeiger — eine Abfrage mit Statuscode `$1FE` liefert `a2=$000001FE` zurück, eine mit `$1FD` liefert `a1=$000001FD` zurück (also jeweils exakt der eigene angefragte Code, nicht ein Pfad-Deskriptor-/Static-Storage-Zeiger). Das erklärt auch, warum der `Init`-Fix nichts half: `V_PORT(a2)` schreibt/liest an einer Adresse, die von Aufruf zu Aufruf mit dem zuletzt benutzten SS-Code "mitwandert" statt stabil auf echte Static Storage zu zeigen. **Schlussfolgerung:** Der eigentliche Bug liegt NICHT mehr in `mc6845.a` (dessen Code ist jetzt korrekt, Edition 4 — Diagnose-Reste entfernt), sondern im Kernel-Dispatch dieses Q9-Ports, der beim Sprung in GetStt/SetStt/Init eines NEU hinzugefügten Custom-Treibers `(a1)`/`(a2)` nicht (korrekt) mit Pfad-Deskriptor-/Static-Storage-Zeigern befüllt. Ob das an der Geräte-Registrierung (Static-Storage-Allokation beim ersten Open), an einem Problem mit der IOMan-Portierung für dieses Board, oder an etwas mit den bereits dokumentierten PMMU-Themen des Projekts zusammenhängt — **weiterhin ungeklärt, braucht Kernel-/IOMan-seitige Untersuchung** (z.B. ob bestehende, funktionierende Geräte wie `x1..x8` denselben Dispatch-Pfad durchlaufen oder einen Sonderweg, der zufällig funktioniert). Guter nächster Einstiegspunkt für eine Session mit mehr Zeit/mit Andreas.

**Kurzer Folgetest (2026-08-04 morgens):** Naheliegender Verdacht — Testskript rief `load crtc0`/`load vram0` auf, aber nie `iniz crtc0`/`iniz vram0` (das Boot-Startup tut das für `dd`/`x1..x8` explizit: `iniz dd c0 c0_fmt r0`, `iniz x1 x2 ... x8`). `iniz crtc0`/`iniz vram0` in `test_gdp_5_29_run.exp` ergänzt (dauerhaft drin, schadet nicht) und erneut getestet: beide `iniz`-Aufrufe laufen fehlerfrei durch, aber die Rückprobe scheitert weiterhin identisch (`$00` statt `80`) — `iniz` allein löst das Problem also nicht.

**DURCHBRUCH (2026-08-04 Vormittag): Ursache gefunden und behoben — 5.29-Registerzugriff funktioniert jetzt Ende-zu-Ende.** Umbau mit Andreas: `vram0`/`gdp.a` als eigenes Gerät entfernt, VRAM-Adresse (`VRAM_START`/`VRAM_LENGTH`) und CRTC-Adresse (`CRTC_START`) als feste Assembler-Konstanten direkt in `mc6845.a` (kein `M$Port`/`V_PORT`-Umweg mehr nötig, da beide Adressen bei uns fest sind — GDP-Zeichenlogik aus `gdp.a` als Kommentarblock zur späteren Reaktivierung übernommen). Damit war der vorherige a1/a2-Verdacht nicht mehr relevant (keine Static-Storage-Zeiger mehr im Spiel) — **aber die Rückprobe scheiterte trotzdem weiterhin identisch**, was zeigte: das eigentliche Problem lag nie bei V_PORT/a1/a2, sondern woanders.

Per direktem C-seitigem Trace im Emulator selbst (temporäre `fprintf(stderr,...)` in `mc6845_dev_read8/write8`, Aktivierung über `Q9_MC6845_TRACE=1`) zweifelsfrei bewiesen: **die emulierte MC6845-Hardware selbst liefert bei jedem Register-Lesezugriff den korrekten Wert zurück** (Trace zeigte exakt `0x50`=80 bei beiden Testleseversuchen). Das Problem lag also ausschließlich darin, wie der gelesene Wert vom Treiber zurück zum aufrufenden Gastprogramm gelangt.

Andreas fand dazu die reale historische C-Bibliotheksquelle `C/ss_gdp.a` (derselben 1989er-Diskette) — der Wrapper `_gs_gwdd` zeigt die echte Konvention: `movea.l d1,a0` (Pufferzeiger aus dem zweiten C-Argument nach A0 kopieren) VOR `move.w #SS_WdInfo,d1` (Statuscode). Das bestätigte den Verdacht, dass Datenrückgabe über einen Pufferzeiger (nicht über D1) laufen muss. Ein erster Test mit A0 als Pufferzeiger scheiterte aber ebenfalls — **zweifelsfrei per Boot-Test bewiesen: bei diesem Kernel werden auch A0 (nicht nur A1/A2) beim Sprung in GetStt/SetStt NICHT zuverlässig durchgereicht.** Umgestellt auf einen Pufferzeiger, der in einem DATENregister (D4, wie D2/D3/D5 nachweislich zuverlässig) übergeben wird — **funktioniert sofort korrekt** (Selbsttest zeigte `y`, danach die echte `SS.RegRd`-Rückprobe zeigte `Y`).

**Endgueltige, jetzt zweifelsfrei bestätigte Kernel-Eigenart dieses Q9-Ports (gilt vermutlich für JEDEN künftigen eigenen GetStt-Code mit Datenrückgabe):** Beim Sprung von I$GetStt/I$SetStt in einen eigenen Custom-SCF-Treiber werden nur DATENregister (D0/D2/D3/D4/D5) zuverlässig durchgereicht — Adressregister (A0/A1/A2) NICHT, und D1 trägt beim Rücksprung zum Aufrufer NICHT den vom Treiber gesetzten Wert (der Aufrufer sieht weiterhin seinen eigenen Vor-Trap-D1-Wert). Datenrückgabe aus GetStt-Aufrufen daher IMMER über einen in einem Datenregister übergebenen Pufferzeiger, NIE über D1 oder ein Adressregister. `mc6845.a` jetzt Edition 6, `SS.RegRd` nutzt `d4.l=Pufferzeiger`. Nebenbei einen zweiten, unabhängigen Bug gefunden+behoben: der gemeinsame `Return`-Rücksprungpunkt (`moveq #0,d1`) hätte ohnehin jeden über D1 zurückgegebenen Wert zerstört (separater eigener `ReturnOk`-Rücksprungpunkt ohne D1-Anfassen ergänzt) — analog zum frueheren `GetPixel`-Fund in `gdp.a`.

**Root-Cause-Nachforschung (2026-08-04, auf Andreas' Wunsch): Ursache eingegrenzt, differenzierteres Bild als angenommen.** Per Recherche-Agent geprüft, ob Musashis `TRAP`/`RTE`-Befehle oder Q9-Flux' eigener Trap-Callback (`m68krt.c`) A0-A2/D0-D7 verändern — **Ergebnis: nein, beide rühren nur PC/SR/den Stack an, der Trap-Callback ist beweisbar reines Read-Only-Tracing.** Der Fehler musste also im echten, unveränderten Kernel-Dispatch selbst liegen (nicht im Emulator-Kern).

Per direktem Instruktions-Trace (temporär in `m68krt.c` ergänzt, `Q9_TRAP_TRACE`+eigener Zaehler, danach wieder entfernt) den kompletten Weg vom `TRAP`-Aufruf bis in `GetStat` hinein verfolgt: **A1/A2 UND `V_PORT(a2)` sind jetzt tatsächlich korrekt** (`a1=$00F21170`, `a2=$00F20E10`, `V_PORT(a2)=$FFFFA000` — echte, sinnvolle Zeiger, kein Funktionscode-Echo mehr) — obwohl unser eigenes `Init` seit dem Umbau gar nichts mehr tut! Das heißt: der ursprünglich gefundene "a1/a2 sind Funktionscode statt Zeiger"-Bug war vermutlich **kein dauerhafter Emulator-/Kernel-Defekt**, sondern ein Artefakt der DAMALIGEN Testbedingungen (zwei gleichzeitig geladene, dynamisch hinzugefügte Custom-Geräte `crtc0`+`vram0`, und/oder fehlendes `iniz`) — beides ist seither behoben (nur noch ein Gerät, `iniz crtc0` fester Teil des Testskripts).

**Was dagegen unabhängig davon weiterhin real und bestätigt bleibt:** D1 gibt bei `I$GetStt` den vom Treiber gesetzten Rückgabewert NICHT an den Aufrufer zurück (das ist die eigentliche Ursache, die den D4-Puffer-Trick nötig macht, unabhängig vom A1/A2-Kapitel). Die D4-Pufferkonvention aus `defsfile` bleibt also die richtige, notwendige Lösung für Datenrückgabe — nur die Erklärung "weil generell alle Adressregister kaputt sind" war zu pauschal.

**Nachtrag (2026-08-04, gezielt erneut geprüft auf Andreas' Nachfrage): A0 ist NICHT wie A1/A2 nur ein Testartefakt — A0 bleibt eigenständig unzuverlässig.** Derselbe Pufferzeiger-Test, der über D4 zuverlässig funktioniert, wurde 1:1 mit A0 unter identischen, bereits bereinigten Bedingungen (ein Gerät, `iniz crtc0`, alle sonstigen Fixes) wiederholt — **A0 scheitert weiterhin** (`x00`), während D4 im selben Testlauf `Y` liefert. D.h. A1/A2 (feste, vom Kernel für Pfad-Deskriptor/Static-Storage belegte Register) UND D0/D2/D3/D4/D5 sind zuverlässig, aber A0 als freier Parameter-/Pufferkanal ist es nicht. **Praktische Konsequenz:** echte, unveränderte historische GDP-Client-Programme (die laut `ss_gdp.a` durchgängig A0 als Pufferzeiger nutzen) würden mit unserem aktuellen Kernel/Emulator NICHT funktionieren — nur eigener, an D4 angepasster Code. Root Cause für die A0-spezifische Unzuverlässigkeit weiterhin ungeklärt (bräuchte Kernel-Binärcode-Disassemblierung).

**Erklärung BESTÄTIGT (Andreas kennt die damalige Hardware aus erster Hand, 2026-08-04):** `ss_gdp.a`/`gdp20`/`wman` liefen historisch auf einem echten **68020**-System (Andreas erinnert sich sicher: dort war tatsächlich ein M68020 verbaut), nicht auf einem 68030. Unser Q9-Port braucht dagegen zwingend einen **68030**-Kernel (`OS9P2`/`OS9P3`, mit PMMU) — dessen komplexerer Systemaufruf-Dispatcher (zusätzliche PMMU-Buchführung) nutzt A0 offenbar als eigenes Arbeitsregister, während der einfachere 68020-Kernel es unangetastet ließ. Passt exakt zur Trace-Beobachtung (A0 wechselt im Dispatch mehrfach den Wert, s.o.). Damit ist die A0-Frage geklärt: kein Emulator-Bug, sondern eine echte Verhaltensdifferenz zwischen den beiden realen Kernel-Versionen, die sich nicht beheben lässt, ohne den 68030-/PMMU-Kernel aufzugeben.

**GDP-Zeichencode reaktiviert + echte visuelle Verifikation ERFOLGREICH (2026-08-04):** `mc6845.a` Edition 7 -- der zuvor auskommentierte Zeichencode aus `gdp.a` (unveraendert, nur `VRAM_START` statt `V_PORT(a2)`) ist jetzt aktiv: `SS_Move/SS_MvRel/SS_Point/SS_Color/SS_Line/SS_Circle` in `PutStat`, `SS.FBGetPx`/`GetPixel` in `GetStat` (auf die D4-Pufferkonvention umgestellt, da auch das bisher ueber D1 zurueckgab). Boot-Test zeichnet Rechteck+Kreis auf `/crtc0` und liest per `SS.FBGetPx` den korrekten Farbindex zurueck (Checkpoint `8`).

Danach echte Sichtpruefung ueber die Video-Bridge gemacht: kleiner Python-Client (Q9VF-Protokoll) verbindet waehrend des Testlaufs, liest `VIDEO_INFO`/`PALETTE`/`FRAME_FULL` und rendert ein PNG. Erster Versuch zeigte nur das Rechteck -- der Kreis war unsichtbar, weil das Testprogramm faelschlich Farbindex 2 im 1bpp-`INDEXED1`-Modus nutzte (nur Bit 0 zaehlt dort) -- eigener Testprogrammfehler, kein Treiberbug, mit Farbindex 1 behoben. Zusaetzlich noetig: `Q9_VIDEOBRIDGE_TCP_PORT`/`Q9_VIDEOBRIDGE_UDP_PORT`-Umgebungsvariablen in `q9boardrun.c` ergaenzt (analog `Q9_NETTTY_PORT`), da Andreas' eigener virtueller Terminalserver dauerhaft Port 2000/2001 belegt -- rein additiv, Default unveraendert. Ergebnis: Rechteck (10,10)-(70,70) und Kreis (Mitte 60,60, r=20) korrekt und ueberlappend sichtbar, 640x200 INDEXED1, exakt wie vom Testprogramm vorgegeben. **Damit ist 5.29s Kernfunktionalitaet (Register + Grundzeichenbefehle, Ende-zu-Ende inkl. echtem Bild) vollstaendig verifiziert.**

**Rect/Fill/Clip ergaenzt + verifiziert (2026-08-04, `mc6845.a` Edition 10):** `SS_Rect`/`SS_Fill` (reine achsparallele Linien/Zeilenfuellung, kein Bresenham noetig) und `SS_Clip` (optionales Clip-Rechteck in Static Storage, von `PlotPixel` zusaetzlich zur Geometrie geprueft, Default = kein Clipping) implementiert. Dabei **weiterer echter Kernel-Fund**: `SS_Clip` war der erste eigene Aufruf mit 4 echten Parametern (D2-D5) und deckte auf, dass **D5 den Sprung in den Treiber nicht zuverlaessig uebersteht** (kam konsequent als 0 an) -- nur D0/D2/D3/D4 sind bestaetigt zuverlaessig. Fix: `SS_Clip` nutzt nur D2/D3/D4 (D4 = Zeiger auf eine kleine {x1,y1}-Struktur im Speicher, derselbe Trick wie der D4-Pufferzeiger bei Datenrueckgabe, jetzt auch fuer Eingabe) -- das ist der allgemeine, unbegrenzt skalierende Weg fuer jeden kuenftigen Aufruf mit mehr als 3 Parametern, jetzt so in `defsfile` als Standing-Konvention dokumentiert.

Danach ergab ein Boot-Test scheinbar, dass `SS_Fill` trotz fehlerfreier Rueckmeldung GAR NICHTS zeichnete (Rect/Line/Circle dagegen schon) -- eine laengere Fehlersuche (Registerdump direkt aus dem Treiber, C-seitiger Vergleich, Reduktion von `DoFill` auf einen einzigen festverdrahteten Pixel) endete mit der Erkenntnis: **kein Treiberbug**. Das eigene Testprogramm (`vramtest.a`) nutzte `moveq #180,d2`/`moveq #240,d2` fuer die Move-Aufrufe vor Fill -- `moveq` erlaubt nur Werte von -128..127, 180/240 wurden also vorzeichenbehaftet falsch interpretiert (180→-76 usw.), wodurch die Startposition komplett falsch war und `PlotPixel` die Punkte korrekterweise als ausserhalb verwarf. Genau derselbe Fehlerklasse wie schon einmal bei `moveq #250` (s. weiter oben) -- eine gute Erinnerung, `moveq`-Werte immer auf den gueltigen Bereich zu pruefen.

Nach dem Fix per echtem Screenshot verifiziert: Rechteck-Umriss, gefuelltes Rechteck, und ein schmaler, hoher Balken, der zeigt, dass ein viel breiterer Fill-Versuch (100px) korrekt auf den per `SS_Clip` gesetzten 40px-Streifen zurechtgestutzt wurde -- alle vier Formen (urspruengliches Rechteck+Kreis, Rect, Fill, geclipptes Fill) gleichzeitig sichtbar und korrekt. | ✅ | Ada | Noch offen (kein Blocker mehr fuer den Kern): CLUT (Host-Paletten-Geraet fehlt, 5.27-Luecke)/Font (Font-Quelle `/dd/GDP/FONTS` bzw. die gefundenen `.font`-Dateien noch zu verifizieren)/BitBlt (auf Andreas' Wunsch bewusst zurueckgestellt, s. Diskussion -- passt nicht 1:1 zu unserem Ein-Framebuffer-Modell ohne Bitmap-Verwaltung)

**Nachtrag `defsfile` (2026-08-04): Nummerierungsfehler gefunden+behoben.** Beim echten `gdp.d` (org $100) stehen VOR den bisher bei uns eingetragenen Treiber-Kommandos (`SS_MCMode`...) noch 11 Bitmap-/Fenster-Kommandos (`SS_CrBm`/`SS_RmBm`/`SS_OWnd`/`SS_CWnd`/`SS_TWnd`/`SS_BWnd`/`SS_RWnd`/`SS_MWnd`/`SS_Scrf`/`SS_Font`/`SS_SetKey`) -- die fehlten bei uns komplett, wodurch unsere gesamte `SS_MCMode`..`SS_ShutTime`-Nummerierung um 11 verschoben war. Jetzt als Platzhalter (nicht implementiert) korrekt vor `SS_MCMode` eingefuegt. Zusaetzlich den kompletten `org $200`-GetStt-Block nachgetragen (`SS_BmInfo`/`SS_WdInfo`/`SS_GetWid`/`SS_ComInfo`/`SS_GetBmId`/`SS_MRead`/`SS_Mouse`/`SS_GetPick`/`SS_MaxMem`/`SS_OWInfo`/`SS_RelGlob`/`SS_Strip`/`SS_MaxBpp`/`SS_GetFkeys`, fehlte bisher komplett). Alles nur Platzhalter, nichts davon implementiert -- Build nach der Aenderung erneut verifiziert (0 Fehler). `SS.FBGetPx`s Kommentar dabei auch korrigiert (stand noch faelschlich "D1.l=Wert raus", jetzt korrekt D4-Pufferkonvention).

**Architektur-Notiz zu Bitmaps (mit Andreas geklaert):** Der echte Bitmap-Speicher (`bm_start`-Zeiger in `w.h`s `BM_DESC`) liegt vermutlich im Video-/Grafikkarten-eigenen Speicher (eigener `SS_GDPMem`/`SS_MaxMem`-Pool, getrennt von normaler OS-9-Speicherverwaltung -- passt zum HRG-82786 als eigenstaendiger Grafik-Coprozessor mit Onboard-Speicher). Bei uns waere das Analogon unser `VRAM_START`/`VRAM_LENGTH`-Bereich. Wichtiger Unterschied: der echte 82786 hatte eigene Intelligenz (Speicherverwaltung/Blitting/Kompositing in Hardware) -- unser MC6845 ist nur ein einfacher CRT-Timing-Chip, jede Bitmap-/Fensterverwaltung muesste bei uns komplett in Software im eigenen Treiber nachgebaut werden, wenn wir das mal angehen.
**Nachtrag (2026-08-10, Claude): vermeintliche "Checkpoint-8-Regression seit Edition 11" aufgeklärt — KEIN Treiberbug.** Symptom laut Session-Notiz: `SS.FBGetPx` bei (10,10) lieferte nicht mehr Farbindex 1, FgCol als Ursache bereits ausgeschlossen, Quelle auf sauberem Edition-11-Stand ohne Debugcode. Ursache **NICHT** in `mc6845.a` gefunden — stattdessen: `os9`-ToolShed `copy -r` setzt beim Kopieren auf ein OS-9-Image das RBF-Dateiattribut **Owner-Execute nicht** (Attribut-String bleibt `----r-wr` statt `----rewr`). `F$Load` verweigert für ein Treiber-Modul (`System State Process`) ohne dieses Bit den Dienst — mit einer denkbar unauffälligen Fehlermeldung (`load: can't load "..." -` ganz ohne Fehlertext), die weder auf Rechte noch auf ToolShed hindeutet. `ident`/`dir -e`/CRC-Prüfung bleiben dabei alle unauffällig "gut", nur `load` selbst verweigert. Per differentiellem Test zweifelsfrei eingegrenzt: dieselbe Host-Datei lud auf einem frisch geklonten Image zuverlässig, sobald `os9 attr -e <pfad>` NACH dem Kopieren nachgezogen wurde; unveränderte, seit Wochen funktionierende Fremdmodule (`oxc16954`) zeigten dasselbe Symptom, sobald sie frisch per ToolShed kopiert wurden. **Fix/Workaround (kein Code-Fix nötig): nach jedem `os9 copy -r` eines Treiber-/Systemzustand-Moduls zusätzlich `os9 attr -e "<image>,<pfad>"` ausführen** — sollte in `test/expect/test_gdp_5_29_run.exp`/Deployment-Doku als Standardschritt ergänzt werden, bisher nur manuell nachgezogen. **Nach dem Fix: kompletter `vramtest`-Lauf (alle Checkpoints inkl. 8, Line_OE, Log_Op XOR/AND/OR, Rect/Fill/Clip) zu 100% grün** — Edition 11 (`PlotPixel`/`DoCircle`/`DoLine`/`CombineLogOp`) ist damit als fehlerfrei bestätigt, keine Codeänderung an `mc6845.a` nötig gewesen. `OS9SYS.gdp-test-20260803.hda` läuft jetzt mit frisch gebautem, debugcode-freiem Edition-11-`mc6845` + korrekt gesetztem Execute-Bit.

**Nachtrag (2026-08-10, Claude): Font/CLUT-Quellen gefunden -- historischer GDP-Treiber + Fonts + CLUT-Dateien.** Auf `/Volumes/SSD1TB/2q9/GREASEWEAZLE/#005_OS9_GDP_V103/extracted/` (Greaseweazle-Diskettenabzug, von Andreas benannt) liegt der komplette historische GDP-Treiber `IO/gdp2661.a` samt Font-Quellen im Assembler (`IO/mc_ascii.font.a`, `IO/mc_germ.font.a`) und einem `CLUT/`-Verzeichnis mit fertigen Paletten (`bw.clut`/`rgb.clut`/`sp.clut`/... ). Weitere Disketten mit GDP-Bezug im selben Ordner: `#007_OS9_GDP_USR` (zusätzlich `FONT`/`FONTSRC`/`CLUT`-Verzeichnisse), `#006_OS9_OWS_V21_Backup`. **Font-Binärformat vollständig geklärt** (aus `mc_ascii.font.a`, nicht mehr geraten): Header (`dc.l` Basisadresse=0, `dc.w` Link-Count, `dc.w` Fontgröße, `dc.w` Zellbreite, `dc.w` Zellhöhe) gefolgt von `fonttab` (256 Word-Einträge, einer pro Zeichencode 0-255) gefolgt vom eigentlichen Glyphen-Block `font:`. `fonttab[c]` = Wort-Offset (relativ zu `font:`) zum ENDE von Zeichen `c` = Anfang von Zeichen `c+1`; der Start von Zeichen `c` steht folglich in `fonttab[c-1]` (Kontrollzeichen 0-31 sind alle 0, `fonttab[31]`=0 liefert korrekt den Start von Space=32). Je Glyph: 1 Header-Wort (bei `mc_ascii.font.a` durchgängig `$0707` = Breite/Höhe 7×7) + 8 Datenworte (unteres Byte = eine 8-Pixel-Bitmapzeile, MSB links). `SS_Font` selbst kommt im rohen `gdp2661.a`-Treiber NICHT vor -- Font/Text lief historisch über den Fenster-Manager (den wir bewusst nicht haben, s.o. "Fenster-Manager bewusst weggelassen"). **Für unsere Umsetzung heißt das:** kein Versuch, `SS_Font`/Fenstermanager-kompatibel zu sein, sondern ein neuer eigener Custom-Code (analog `SS.FBGetPx`), der einen String direkt per `PlotPixel`-Technik ins VRAM blittet, gespeist aus einem der historischen `.font`-Datenmodule (per `mkdatmod`/`r68` neu gebaut, oder als vorgebautes Modul direkt von der Diskette übernommen, noch zu prüfen welches vorliegt). CLUT-Umsetzung kann testweise gegen die realen `.clut`-Dateien verifiziert werden (Format dieser Dateien selbst noch ungeprüft, nächster Schritt vor der Implementierung). **Noch offen (nächste Schritte):** (1) `.clut`-Dateiformat klären, (2) Host-Paletten-Gerät entwerfen (analog Framebuffer/MC6845, 5.27-Lücke), (3) Font-Blit-Routine in `mc6845.a` implementieren + Boot-Test gegen ein reales `.font`-Modul, (4) ggf. `mc_germ.font.a` für Umlaute mit prüfen.

**Nachtrag (2026-08-10, Claude, direkt im Anschluss): CLUT-Gerät + SS.FontBlit implementiert, per Boot-Test vollständig grün verifiziert -- 5.29 damit inhaltlich abgeschlossen.**

*CLUT-Gerät (löst die 5.27-Lücke):* neue Dateien `src/kernel/clut.{h,c}` -- 4-Byte-MMIO-Fenster `$FFFFA010-13` (Index + R/G/B), 256 Einträge, Default-Zustand Identitäts-Graustufe. Verdrahtet in `m68krt.{h,c}` (`q9_m68krt_attach_clut`) und `q9boardrun.c`. `videobridge.{h,c}` sendet jetzt die ECHTE, vom Gast programmierte CLUT statt der bisherigen Graustufen-Platzhalterpalette (`build_grayscale_clut` bleibt nur als Fallback bei `clut=NULL`); ein `generation`-Zähler löst bei Änderung ein neues `PALETTE`-Telegramm aus, analog zum bestehenden Stride/Höhe/Modus-Vergleich. `.clut`-Dateiformat der historischen Disketten dabei NICHT gebraucht (eigenes, einfacheres 4-Byte-Register-Design gewählt, kein Parsing der `.clut`-Dateien nötig). `Makefile` bekam einen kleinen Fix (`SLIRP_FLAGS` fehlte der Parent-Include-Pfad für Homebrews `libslirp.pc`, das `-I<includedir>/slirp` statt `-I<includedir>` liefert -- betraf `make native` auf macOS unabhängig von diesem Thema).

*`SS.FontBlit`* (in `mc6845.a`, Edition 12, eigener Code ab `$1003` -- NICHT das echte `SS_Font`, das bleibt "nicht implementiert" für spätere Fenstermanager-Kompatibilität): `D4.l`→`{string_ptr:long, font_ptr:long}`, zeichnet einen nullterminierten String ab der aktuellen Plotter-Position (`DrawX`/`DrawY`) in `FgCol`, per `PlotPixel`-Blit wie `SS_Rect`/`SS_Fill`. Font-Quelle: `mc_ascii.font.a` von der Diskette, unverändert mit dem eigenen `r68`/`l68` als **raw Binärmodul** gebaut (`l68 -r=0`, kein OS-9-Modul-Header nötig, Byte 0-3 = "entry"-Feld, Byte 4 = `fontbeg`-Start) und vom Gast per `I$Open`/`I$Read` roh eingelesen (F$Link scheiterte auf diesem Kernel konsequent mit `E$MNF` trotz korrektem `mdir`-Eintrag -- Ursache nicht geklärt, aber irrelevant, da der Roh-Datei-Weg zuverlässig funktioniert und keinen Modul-Wrapper braucht).

**Zwei echte Bugs beim Aufbau der `fonttab`-Auswertung gefunden+gefixt** (beide NICHT das, was die lange Fehlersuche am Ende brauchte, s.u.): Offset-Regel war zunächst um eins verschoben (`Start=fonttab[c-1]` statt korrekt `Start=fonttab[c]`, Ende=`fonttab[c+1]`) -- durch einen gezielten Debug-GetStt-Code (Register-Dump der berechneten Start-/Ende-Offsets) gefunden, der danach wieder entfernt wurde.

**Wichtigste Erkenntnis der Sitzung, kein Treiberbug:** nach dem Offset-Fix zeichnete `SS.FontBlit` an der ursprünglichen Testkoordinate (400,50) weiterhin scheinbar nichts, UND ein anschließender Kontroll-`SS_Point` an einer völlig neuen Koordinate schlug ebenfalls fehl -- sah zunächst nach genereller `PlotPixel`-Beschädigung durch die neue Routine aus (führte fast zur kompletten Entfernung von `SS.FontBlit`). Tatsächliche Ursache: der frühere `SS_Clip`-Testblock im selben Testprogramm setzt eine Clip-Region (`x:250-290, y:0-199`) und **setzt sie nie zurück** -- jeder Zeichenversuch außerhalb dieses Rechtecks wird von `PlotPixel` (by design) still verworfen, kein Bug. Mit einer Testkoordinate innerhalb der Region (`260,195` statt `400,50`) lief alles sofort korrekt. **Zweiter, während derselben Verifikation gefundener echter Testprogrammfehler** (bereits dritte Instanz dieser Fehlerklasse im Projekt, s. Edition-10-Eintrag weiter oben): `moveq #195,d3` für die Y-Koordinate -- `moveq` erlaubt nur -128..127, 195 kippte vorzeichenbehaftet ins Negative und ließ `SS.FBGetPx` mit `E$Param` fehlschlagen; Fix: `move.w #195,d3`.

**Boot-Test (Edition 12, finale Fassung, `vramtest_v2`): komplette Checkpoint-Kette zu 100% grün** -- `12Y34678EprGtHvIjJnK9AzBCDLMNOPQ5`: alle bisherigen 5.29-Checks (Rect/Fill/Clip/Line_OE/Log_Op) weiterhin fehlerfrei, plus `L`/`M` (SS_clut/SS_clutall), `N`/`O` (Font-Rohdatei geladen, SS.FontBlit ausgeführt), `P`/`Q` (Font-Pixel exakt an der erwarteten Bitposition gesetzt/nicht gesetzt). Damit ist 5.29 (Rect/Fill/Clip/Line_OE/Log_Op/CLUT/Font-Grundfunktionen) inhaltlich vollständig -- offen bleiben nur noch Ausbauschritte ohne Blockercharakter: Font/Text mit Zeilenumbruch, `mc_germ.font.a` (Umlaute), CLUT-Laden direkt aus einer `.clut`-Datei (bisher nur eigenes 4-Byte-Register-Interface).

| 5.30 | **Maus-Unterstützung.** Zyklisches Ereignis, das die Mausposition überträgt (mindestens bei Änderung) — braucht vermutlich IRQ + kleinen Event-Puffer im Emulator (Positions-Delta + Button-Status), reines Polling wäre zu träge (analog DUART-RX-Muster). Neue Q9-Frame-Protokollnachricht als Rücktelegramm (Client → Server, bisher ist das Protokoll rein Server → Client). **Design-Entscheidung (Andreas, 2026-08-03): Cursor-Darstellung passiert ausschließlich client-seitig in Q9 Frame als Overlay, NICHT im emulierten Framebuffer** — Protokoll überträgt nur Position/Sichtbarkeit/Form, gezeichnet wird nur auf Client-Seite (entspricht echten Hardware-Cursor-Sprites, unabhängig vom Framebuffer-Inhalt) | 💡 | Ada | Nach 5.27 (braucht die Host-Bridge für die Rücktelegramme) |

**Design-Vorüberlegung Maus+Tastatur (mit Andreas, 2026-08-04, noch nicht umgesetzt):** Im echten `gdp.d`/`mouse.h` (historisches Diskimage) gefunden — Maus läuft über ein echtes OS-9-**Datenmodul** `MDEV_Data` (Struct `MOUSREC`: x/y/button/type + Aux-Feldpaar), das mehrere serielle Maustreiber (`mouse_logi.a`/`mouse_genius.a`/`mouse_summa.a`/`mouse_nce.a`/`mouse_pilot.a`, je ein Gerät pro Maustyp) befüllen; `SS_Mouse`/`SS_MRead` (unser eigener GetStt-Dispatch in `mc6845.a`) lesen es einfach per `F$Link("MDEV_Data")` aus (Namens-Linking löst das Adressproblem automatisch, kein Host-Wissen über Gast-Adressen nötig). **Für uns:** eigenes kleines Gastprogramm (Ersatz für `mouse_logi.a` & Co.) liest Mausdaten über ein NEUES TCP-Telegramm von Q9-Frame und schreibt sie ins gleiche `MDEV_Data`-Format. Tastatur ist anders gelöst: kein Datenmodul, stattdessen `SS_SetKey` (bindet GDP an einen bestehenden Pfad-Deskriptor; im Original schlicht `/term`, gefunden im Boot-Skript `CMDS/gdpload`: `setkey /term 160 0`) + `SS_MRead`/neue `SS_Aux*`-Calls (`SS_AuxKbd`/`SS_AuxRead`/`SS_AuxRdy`/`SS_AuxSSig`/`SS_AuxPut`, weiterer bisher fehlender Block bei `org $220` in `gdp.d` — **noch NICHT in unser `defsfile` nachgetragen**, im Gegensatz zum bereits nachgetragenen `$100`/`$200`-Block). Da bei uns (anders als beim Original mit lokal angeschlossener Tastatur) auch die Tastatur remote über Q9-Frame kommen soll, reicht "vorhandenen Pfad wiederverwenden" nicht — Plan: ein zweites, `nettty`-artiges Gerät (eigene TCP-Verbindung, gleiches Poll-Pattern wie `Q9_NETTTY_PORT`) eigens für GDP-Tastatureingaben, `SS_SetKey` zeigt dann bei uns auf dessen Pfad statt auf `/term`. Alles bewusst nur als Design festgehalten, Umsetzung erst wenn wir an 5.30 dran sind.
| 5.31 | **Interaktives Tuning-Tool für MC6845/Si5351A.** Kleines Werkzeug, mit dem sich die Registerwerte beider Bausteine live/interaktiv verändern lassen (Live-Vorschau über die bestehende Q9-Frame-Anzeige), plus Knopf zum Übernehmen der gewählten Werte in den Descriptor (5.28). **Anzeige-Anforderung (Andreas, 2026-08-03):** bei R1/R6 immer BEIDE Werte parallel anzeigen — Rohwert in "Zeichen"/Bytes UND die daraus abgeleitete Pixelzahl (Granularität hängt vom Video-Modus ab, s. `mc6845.h`-Kopfkommentar) —, damit man beim Tunen nicht verrutscht. Andreas hatte für den MC68000 früher ein vergleichbares Tool; Risiko dort war reale Monitor-Sync bei zu extremen Werten — bei uns entsprechend "nur" ein verzerrtes Q9-Frame-Bild, keine Hardware-Gefahr | 💡 | Ada | Nach 5.24/5.25 (Registermodell muss stehen); unabhängig vom Rest |
| 5.32 | **Test/Validierung Ende-zu-Ende.** Kleines Gastprogramm, das MC6845 + Si5351A programmiert und ins VRAM schreibt; verifiziert wird, dass Q9 Frame darauf korrekt reagiert (richtige VIDEO_INFO/PALETTE, sichtbare FRAME_UPDATE-Rechtecke, korrekter Moduswechsel bei Registeränderung zur Laufzeit) — passend zum sonstigen Projektstil (`runtests.sh`, Boot-Test gegen echtes Image) | 💡 | Ada | Nach mindestens 5.24-5.29 |

---

### Phase 6 — Mehrere Zielarchitekturen (💡 Vorschlag, 2026-08-11, mit Andreas besprochen)

Andreas möchte Q9 Forge über die Motorola-68K-Familie hinaus erweitern:
**RISC-V32**, **ARM64** (bereits angefangener nativer Q9-Port, konkretes
Mac-Modell noch offen) und **x86 32-Bit**. Für x86 wurde die Modusfrage
geklärt: "32-Bit Real Mode" existiert auf x86 nicht (Real Mode ist zwingend
16-Bit) — Zielmodus ist **Protected Mode, flach**, angelehnt an OS-9000 auf
386+ (Microwares eigener portabler, in C geschriebener OS-9-Nachfolger für
386/68020+/PowerPC/MIPS/SPARC; Boot-Sequenz-Details nicht mit Primärquelle
verifiziert, aber architektonisch die einzig mögliche Basis für einen
portablen 32-Bit-Kernel).

Pro Zielarchitektur zwei grundverschiedene Bauarten (Details/Begriffe siehe
`Q9Forge/AI_CONTEXT.md`, Abschnitt "Mehrere Zielarchitekturen"):

- **Typ A — Board-Emulator**: vollständige Hardware-Emulation (CPU + Board),
  läuft auf Linux/Windows/Mac, führt unveränderten Gast-Code aus. Heutige
  Bauart von Q9 Flux, bisher nur für Motorola 68K.
- **Typ B — native Runtime**: Q9-OS nativ für die Zielarchitektur übersetzt,
  läuft direkt auf echter Hardware/echtem Host-Kernel, keine Hardware-
  Emulation, nur dünne HAL-Übersetzung der Syscalls. Entspricht dem
  archivierten Mini-Kernel-Konzept (`Q9RESUME-Kernel`).

Heutiger Stand ist bewusst nur Struktur/Planung (ROADMAP.md +
AI_CONTEXT.md aktualisiert) — noch keine Code-Arbeit. Alle drei
Zielarchitekturen sind grob gleich eingeordnet, ohne Priorisierung.

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| 6.1 | Namen für Typ A/Typ B final festlegen (Arbeitsbegriffe: "Board-Emulator" / "native Runtime") und in AI_CONTEXT.md als verbindlich markieren | 💡 | — | Aktuell nur Arbeitsbegriffe, noch nicht als "verbindliche Namen" bestätigt. **2026-08-13-Nachtrag:** Terminologie-Kollision entdeckt — "Target" ist im Projekt bereits fest für die CPU-Zielarchitektur reserviert (Makefile `TARGET=m68k\|riscv32\|x86_32`, s. 6.8), kann deshalb NICHT zusätzlich als Name/Oberbegriff für Typ B (natively kompiliertes Q9-OS je Host-Betriebssystem) wiederverwendet werden — Andreas' eigener Vorschlag ("build-target" als Oberbegriff, `Q9-Mac`/`Q9-Apple` als Verzeichnis darunter) kollidierte deshalb mit sich selbst und wurde von ihm selbst zurückgezogen. Weiterhin offen, keine Eile. |
| 6.2 | Repo-/Verzeichnisstruktur pro Zielarchitektur festlegen (z. B. eigenes Q9-Forge-Teilprojekt je Zielarchitektur vs. Unterordner in Q9-Flux) | ✅ | — | **2026-08-13 mit Andreas geklärt:** ALLES in EINEM Repo (Q9-Flux) für alle Typ-A-Zielarchitekturen — entspricht dem faktischen Vorgehen der RISC-V-Arbeit dieser Session (TinyEMU/PLIC/NuttX-Bring-up landete als Unterordner in Q9-Flux, kein eigenes Teilprojekt). Offene Detailfrage, von Andreas bewusst nicht vorentschieden: ob `TARGET=` je Zielarchitektur ein eigenes Binary erzeugt oder nicht ("das heisst aber nicht dass es eigene Binaries geben kann. Aber evtl. auch nicht...") — wird bei 6.8 konkret geklärt. Typ-B-Repo-Frage (eigenes Projekt je Zielarchitektur vs. gemeinsam) bleibt offen, da noch kein Typ-B-Code existiert — s. 6.1. |
| 6.3 | Priorität für die erste konkrete Umsetzung festlegen | 💡 | — | Andreas wollte bewusst noch keine Priorisierung (alle drei Zielarchitekturen gleich grob einordnen) — offen, bis das gewünscht wird |
| 6.4 | x86-Boot-Sequenz (Real Mode → Protected Mode Umschaltung) und OS-9000-Referenzverhalten genauer verifizieren, falls x86-Typ-A angegangen wird | 💡 | — | Heutige Aussage ("Protected Mode, flach") ist architektonisch sicher, aber ohne Primärquelle zur genauen OS-9000-Boot-Sequenz |
| 6.5 | `src/kernel/cpu_backend.h` einführen: kleine Vtable (reset/execute/set_irq/is_stopped/ctx), analog zu `devreg.h`'s Geräte-Vtable — `q9boardrun.c` ruft danach nur noch die Vtable auf statt Musashi/`m68krt`-Funktionen direkt | ✅ | Claudia | 2026-08-13: `cpu_backend.h` angelegt (nur die vier Operationen der Hauptschleife — `debug_state`/`quicc_acks` bewusst NICHT in der Vtable, das ist 68k-spezifische Diagnose, keine allgemeine CPU-Eigenschaft). `m68krt.c` bekam `q9_m68krt_get_backend()`, das eine `q9_cpu_backend_t` mit duennen Wrappern um die bestehenden `q9_m68krt_reset/execute/set_irq/is_stopped` befuellt (`ctx` = `q9_m68krt_t*`, ueberwiegend ungenutzt, da Musashi ein Singleton ist — reine Anpassung an die generische Signatur). `q9boardrun.c` ruft jetzt ausschliesslich `cpu.reset/execute/set_irq/is_stopped(cpu.ctx, ...)`. Verifiziert: `make host` (0 Warnungen mit `-Wall -Wextra`) + `make test` (0/30 Fails) + manueller Boot-Smoke-Test (`--rom romimage.dev.running.BIN`, kam bis zum OS-9-`$`-Prompt) — Verhalten bit-identisch zu vorher, wie bei reinem Refactoring erwartet. TinyEMU-API (`third_party/tinyemu/Q9_VENDOR.md`) passt fast 1:1 auf diese Vtable — genau der Anschlusspunkt fuer 6.8. |
| 6.6 | Verzeichnis-Umzug: eigenständige Geräte-Module in einen eigenen Unterordner unter `src/devices/<name>/` verschoben (mc6845, clut, framebuf, quicc, videobridge, net (slirp_net/vmnet_net/bpf_net)) | ✅ | Claudia | Andreas' tatsächliche Entscheidung (2026-08-11) weicht vom obigen Ursprungsplan ab: `q9board.c`/`m68krt.c`/`devreg.c`/`boardcfg.c`/`q9boardrun.c` bleiben bewusst in `src/kernel/` — "alles, was kein eigenes Gerät ist, bleibt dort drin", auch `q9board.c`, obwohl es DUART+CF+RTC72421+Timer+Bus/Remap-Logik bündelt (das Aufteilen wäre echte Chirurgie mit Risiko gewesen, kein reines Verschieben, heute bewusst nicht gemacht). `quicc.c/.h` ist trotzdem nach `src/devices/quicc/` gewandert, obwohl 68k/CB030-spezifisch, weil es nach Andreas' Regel ein eigenständiges Gerät ist. Alle `#include`-Pfade auf relative Pfade umgestellt (z. B. `../../kernel/devreg.h`), Makefile (`BOARD_SRC/HDR`/`BOARD_NET_SRC/HDR`/`SLIRP_SRC/HDR`) angepasst, Pfad in der 5.11-Überschrift in `docs/HANDBUCH.md` korrigiert. Verifiziert: `make native` + `make test` (0/30 Fails) + manueller Boot-Smoke-Test (`--rom romimage.dev.running.BIN`, kam bis zum OS-9-`$`-Prompt) — alles grün. |
| 6.7 | `boardcfg.h`/`.q9`-Schema generalisieren: von CB030-Festfeldern (CF-Bus, vmnet-Keys) hin zu generischen `[device] type=... name=... base=...`-Blöcken, die über die devreg-Typ-Registry instanziiert werden | 🟡 | Claudia | **2026-08-13, Pilot-Schritt (Andreas' Idee):** statt Laufzeit-JSON (neue Fremdabhaengigkeit, widerspraeche boardcfg.c's "kein Fremdmodul") eine **statisch kompilierte Schema-Tabelle** je Geraetetyp, im Stil der bestehenden Typ-Registry (`devreg.h`) — Feldname/Typ(INT\|STR\|ENUM)/Pflicht/Min-Max bzw. Enum-Werte/Freitextbeschreibung. Neu: `src/kernel/devschema.h/.c` (Registry+Validierung) + Pilot-Schema fuer `cf` (9 Felder, deckt `q9_cfg_cf_t` 1:1 ab) + `test/08_test_devschema.c` (`make test-devschema`, Teil von `make test`). **Bewusst additiv:** `boardcfg.c`s tatsaechliches Parsing/Verhalten ist NOCH NICHT umgestellt — das ist der naechste Teilschritt, wenn Andreas den Ansatz nach dem Pilot freigibt. **Nachtrag (2026-08-13):** `Q9_FIELD_BOOL` ergaenzt (eigener Typ statt Zwei-Werte-Enum, fuer Andreas' "Ja/Nein"-Felder aus der Editor-Planung, s. `docs/Q9FLUX_EDITOR_de.md`) + Schema `memory` (RAM/ROM/NVRAM, Andreas' Speicher-Beispiel aus derselben Planung — vorausschauend beschrieben, `boardcfg.h` hat noch KEIN Speicher-Config-Struct dafuer, haengt an 5.19). **Gefundener, bewusst NICHT geloester Widerspruch:** das Feld `descriptor` heisst bei `cf` (String, Descriptor-Name fuer den ROM-Generator) etwas anderes als bei `memory` (Bool, reine "wird ueberhaupt einer gebraucht"-Info-Flagge aus der Editor-Planung) — beide Bedeutungen bestehen aktuell nebeneinander, Vereinheitlichung erst nach Andreas' Entscheidung. Verifiziert: `make test` (0 Fails, inkl. erweitertem Schema-Test), `make host` unveraendert. **Offen:** boardcfg.c auf generische `[device]`-Bloecke + Schema-Validierung umstellen, weitere Geraetetypen (duart68681, timer, quicc, ...) nachziehen, Ruecksicht auf bestehende `.q9`-Dateien (Abwaertskompatibilitaet), `descriptor`-Bedeutungskonflikt klaeren. |
| 6.8 | Makefile: `TARGET=m68k\|riscv32\|x86_32`-Variable einführen (Default `m68k` = heutiges Verhalten unverändert), steuert welcher `common/`+`<arch>/`-Satz + welcher Drittanbieter-CPU-Kern gebaut wird; Artefakte nach `build/<platform>/<target>/` | ✅ | Claudia | 2026-08-13: bewusst KLEINER umgesetzt als der Ursprungstext — kein `common/`+`<arch>/`-Umbau von `src/kernel/` (zwischen dem 68k-Board und den RISC-V-Bring-up-Stufen gibt es noch keine echte gemeinsame Basis) und keine `build/<platform>/<target>/`-Verschachtelung (6.2-Klärung: ob TARGET je Ziel eigene Binaries braucht, liess Andreas bewusst offen). Stattdessen ein neues `make build TARGET=...`, das nur auf bestehende, unveränderte Targets dispatcht: `m68k` (Default) → `make host` 1:1; `riscv32` → `test-rvboard`+`test-rvtimer`+`test-rvextirq` (Rauchtest ohne Fetch-Vorlauf, gleiches Toolchain-fehlt-graceful-skip-Muster wie bei m68k ohne gcc); `x86_32` → saubere "noch nicht implementiert"-Meldung statt stillem Nichtstun. Verifiziert: `make host` unverändert (0 Warnungen), `make test` (0/30 Fails), `make build` (Default) baut identisch zu `make host`, `make build TARGET=riscv32` läuft alle drei Stufen grün durch, `TARGET=x86_32`/unbekanntes TARGET brechen sauber mit Exit-Code ab statt fehlzuschlagen oder etwas Falsches zu tun. |

---

### Phase U — Userland-Werkzeuge (Codex-Baustelle, separater Nebenschauplatz)

**Nicht Teil des normalen Phasenablaufs** — läuft parallel zu Phase 3/4, auf
einem eigenen Branch `codex-userland` (eigenes Arbeitsverzeichnis
`../Q9-codex-userland`, Basis war Stand nach 4.1), bearbeitet von **Codex**
(OpenAI, `codex exec`, Modell GPT-5.5) im Auftrag von Claudia/Andreas.
**Isolationsprinzip**: Arbeitsbereich strikt auf `userland/` begrenzt, kein
`git commit`/`push` durch Codex selbst — Claudia baut nach jeder Runde
unabhängig nach und verifiziert (PASS + warnungsfrei + Diff nur in
`userland/`), bevor gepusht wird. Kein Einfluss auf `main`/die
Autonomie-Routine. Ziel: eine kleine C-Bibliothek (`libq9`), die die bereits
implementierten Syscalls kapselt, plus darauf aufbauende Datei-Werkzeuge —
als Quellcode-Vorarbeit, NICHT als ladbare Q9-Module (das ist die offene
Architekturfrage **O6** in PROJECT.md: WASM-Import-ABI vs. 68k-PIC-Code,
zu klären ab Phase 5/6).

| # | Schritt | Status | Wer | Notizen |
|---|---------|--------|-----|---------|
| U.1 | `libq9`: Wrapper um die fertigen Syscalls + `q9cat`/`q9copy` + nativer Testharness | ✅ | Codex | Testharness bootet den echten Kernel nativ (POSIX-HAL) gegen ein selbst gebautes FAT16-Image, statt zu mocken. → Details: ARBEITSPLAN_ARCHIV.md |
| U.2 | `q9dir`: Verzeichnis auflisten | ✅ | Codex | Liest rohe 32-Byte-FAT16-Directory-Einträge über `I$Read` auf einen Verzeichnispfad (Kernel-Unterstützung existierte schon seit 3.3); Parsing in… → Details: ARBEITSPLAN_ARCHIV.md |
| U.3 | `q9mkdir` + `q9rm` | ✅ | Codex | Rundet das ursprünglich gewünschte "dir/list/copy"-Set ab; kein rekursives Löschen. → Details: ARBEITSPLAN_ARCHIV.md |
| U.4 | `q9touch` + `q9stat` + Testumgebung konsolidiert | ✅ | Codex | Sieben Tools insgesamt (cat/copy/dir/mkdir/rm/touch/stat); README vollständig; Test-Redundanzen aufgeräumt. → Details: ARBEITSPLAN_ARCHIV.md |
| U.5 | Funktions-Kommentare nachtragen (Andreas' Review: Code gut, aber unkommentiert) | ✅ | Codex | 88 Funktionen in 16 Dateien im Q9-Standardstil (Function/Desc/Call-Header) kommentiert; reine Ergänzung, +520/-0 Zeilen, kein Verhalten geändert (per… → Details: ARBEITSPLAN_ARCHIV.md |
| U.6 | argc/argv-`main()`-Einsprungpunkte für alle sieben Tools (Vorbereitung auf spätere 68k-Kompilierung) | ✅ | Codex | Eigener Branch `codex-m68k-entrypoints`, `q9<name>_main(argc, argv)` neben unveränderten `_run()`-Funktionen, `-h`/`--help` überall, `-v`/`--verbose`… → Details: ARBEITSPLAN_ARCHIV.md |
| U.7 (Idee) | Lange Dateinamen (LFN) auch in `q9dir`/`q9stat` anzeigen | 💭 | — | LFN-Parse-Logik steckt privat in `fat16.c` — Duplizieren oder Kernel müsste sie exportieren |
| U.8 (Idee) | Tools als echte ladbare Q9-Module, sobald Phase 5/6 (Modul-Ausführung) und O6 (Verpackungsformat) stehen | 💭 | — | heute nur Design-Vorgriff |
| U.10 | Bestehende Userland-Tools auf echtem OS-9 testen (im CB030-Emulator) | 💡 | Codex + Andreas | Andreas' Idee (2026-07-05): Solange Q9s eigene 68k-Runtime noch nicht steht, das emulierte echte OS-9 als Prüfstand für die U.1–U.6-Tools nutzen — validiert die Tool-Logik auf einem echten OS und beweist nebenbei, dass Q9s API-Design OS-9-artig genug ist. Drei Voraussetzungen: (1) **Compiler-Pfad** — MWOS-Cross-Compiler (Windows) oder besser der native OS-9-C-Compiler auf dem CF-Image (→ nach 5.5b). (2) **Portabilitätsschicht** — dünnes OS-9-Backend für die Tools gegen die OS-9-C-Library (Datei-I/O fast Unix-artig; Verzeichnis-Lesen + Fehlercodes anders) — gute Codex-Aufgabe, klar umrissen. (3) **Transportweg** für die Binaries: ins CF-Image einbauen ODER per `kermit` über die emulierte serielle Leitung (steckt im Bootfile — wäre gleich ein Härtetest für die DUART-Emulation). Einschränkung bewusst akzeptiert: OS-9-spezifischer Portierungsanteil wandert nicht 1:1 zu Q9 mit. Wird 🟢, sobald 5.5b (CF-Image) steht und der Compiler-Weg geklärt ist |
| U.9 | OS-9-Klassiker-Tools nachbauen: `q9mdir`, `q9mfree`, `q9procs`, `q9free`, `q9devs`, `q9ident` | 💡 | Codex | Andreas' Idee (2026-07-05, nach dem OS-9-Boot-Meilenstein): die vertrauten OS-9-Systemkommandos als Q9-Userland-Tools, gegen Q9s Syscall-API — `q9mdir` (Modul-Directory listen, kurz + `-e` erweitert wie das Original), `q9mfree` (freier Speicher), `q9procs` (Prozesstabelle), `q9free` (Block-Device-Belegung), `q9devs` (Gerätetabelle), `q9ident` (Modul-Header/CRC einer Datei anzeigen). Gleiches Muster wie U.1–U.6: `q9<name>_main(argc, argv)`-Einsprungpunkte, Testharness, isolierter Branch. VORAUSSETZUNG klären, bevor es 🟢 wird: welche der nötigen Kernel-Auskunfts-Syscalls (Modul-Directory iterieren, Prozesstabelle lesen, Freispeicher abfragen) existieren schon bzw. müssen vorher von Claudia ergänzt werden — sonst baut Codex gegen Luft |

**Stand 2026-07-04**: U.1–U.5 fertig, jede Runde von Claudia unabhängig
nachgebaut (`userland/build.sh`, PASS, `-Wall -Wextra` warnungsfrei), Branch
`codex-userland` gepusht (Commits `bf5d494`, `a94ca93`). Nebenbefund bei der
Kommentar-Durchsicht: auch der Hauptkernel wurde stichprobenartig geprüft
(fast lückenlos kommentiert, eine echte Lücke `is_leap()` in `syscall.c`
gefunden und behoben, Commit `1caf2b3`). **Nach Andreas' Durchsicht per Pull
Request #1 in `main` gemerged (2026-07-04 abends, Merge-Commit `ebdde4b`)** —
Worktree und Branch aufgeräumt, `userland/` ist damit fester Bestandteil des
Hauptbaums. Anbindung an O6 (Modul-Verpackung) bleibt offen für später.

---

## 💭 Ideenspeicher (noch nicht eingeplant)

Ideen, die während der Arbeit auftauchen, aber (noch) kein Teil der Planung
sind — keine 💡-Vorschläge zur Freigabe, sondern eine reine Merkliste ohne
Zeitdruck. Kein Status, keine Phase, kein „Wer" — schaffen es vielleicht
irgendwann als 💡-Vorschlag in eine echte Phase, müssen aber nicht. Unterschied
zu „⛔ Geparkt": dort stehen akute Klärungsbedarfe für laufende Arbeit, hier
Zukunftsideen ohne Handlungsdruck.

| Idee | Kontext | Warum (noch) nicht eingeplant |
|------|---------|-------------------------------|
| `vmnet_net.c/.h` und `bpf_net.c/.h` von `src/devices/net/` nach `src/hal/` (z. B. `hal/macos/`) verschieben, weil sie Host-Maschinen-spezifisch sind (nur macOS), keine simulierte Gast-Hardware — aktuell ist die "welche Host-Maschine"-Unterscheidung inkonsistent verteilt zwischen `src/hal/` (native/posix, 2-teilig) und Ad-hoc-`Makefile`/`#ifdef __APPLE__`-Bedingungen an anderer Stelle | 6.6-Diskussion 2026-08-11 | Beim Geräte-Umzug aufgefallen; würde auch bedeuten, den aktuell 2-teiligen HAL-Split (native/posix) in einen 3-teiligen (windows/macos/linux) passend zum `PLATFORM_DIR` des Makefiles umzubauen — eine größere, eigene Entscheidung, nicht Teil des heutigen reinen Verschiebe-Schritts |
| `main()` entduplizieren: aktuell fast wortgleich doppelt vorhanden in `hal_windows.c` und `hal_posix.c` (Argumente parsen + Config laden + `q9_board_boot()` aufrufen) | 6.6-Diskussion 2026-08-11 | Aufgefallen bei der Erklärung "welche Datei ruft Musashi auf" — echter Einstiegspunkt ist `q9boardrun.c`s `q9_board_boot()`, aber der CLI-Parsing-Wrapper drumherum ist pro Plattform copy-paste; geringes Risiko, keine Dringlichkeit |
| Kernel-Tabellen als Datenmodule verpacken (analog OS-9s `F$DatMod`) | docs/MODULES.md, Abschnitt 5 | Setzt das Modulsystem (Phase 2) voraus; passt nur zu read-mostly Daten, nicht zu häufig mutierenden Tabellen (Pfad/Geräte); keine MMU-Durchsetzung vorhanden (weder WASM noch aktuelles 68k-Ziel) |
| Modul-Gruppen (gemeinsames Unlink mehrerer zusammen geladener Module) | docs/MODULES.md, Abschnitt 4 | Braucht Multi-Modul-Dateien, die es noch nicht gibt (Phase-2-Besprechung 2026-07-03) |
| OS-9-Dreiklang File-Manager/Treiber/Descriptor statt kombiniertem `q9_dev_t` | docs/MODULES.md, Abschnitt 3 | Lohnt sich erst bei mehreren Instanzen desselben Treibers mit unterschiedlicher Konfiguration (Phase-2-Besprechung 2026-07-03) |
| `--dump`-Modus in `q9mod` (Modul-Header lesbar anzeigen, "ident"-artig) | Phase-2-Besprechung 2026-07-03 | Selbsttest-Muster reicht zur Verifikation während der Entwicklung; kein PC-Tool nötig, bis mal ein echtes produziertes Modul von Hand inspiziert werden muss |
| LFN-Schreiben im FAT16-Manager (lange Namen beim Anlegen neuer Dateien) | Phase-3-Besprechung 2026-07-03 | 8.3-Alias-Generierung + Mehrfach-Directory-Einträge = viel Kleinkram; LFN-Lesen (3.3) deckt den Alltagsfall (am Mac befüllte Images) schon ab |
| MBR-Partitionstabelle: Partitionen als Sub-Block-Devices (z.B. `/d0.1`), MBR-Parser als dünne Schicht zwischen Block-Device und VFS | Phase-3-Besprechung 2026-07-03 | Superfloppy reicht für Phase 3; spätestens Phase 7 (SD-Karte am 68k-Board ist praktisch immer partitioniert) nötig — Einstiegspunkt im Code bleibt frei, gut testbar mit hdiutil/fdisk-Images |
| Original-OS-9-File-Manager (RBF, PCF) via Emulator laufen lassen, nativer Übergang erst am Treiber-Entry (HLE-Muster). **Update 2026-07-03 nachts (Andreas' Fund): NitrOS-9 ist GPL und quelloffen (github.com/nitros9project/nitros9)** — RBF dort in 6809-Assembler: (a) rechtlich saubere Modul-Quelle für die 6809-Runtime (Phase 8), GPL-Module dürfen anders als MWOS weitergegeben werden; (b) lesbare Referenz für eine eigene native C-Reimplementierung (nur Logik verstehen, nicht abschreiben — GPL färbt ab); (c) ToolShed-Tools des Projekts lesen/schreiben RBF-Images am PC → Test-Images ohne CoCo-Hardware | Phase-3-Besprechung 2026-07-03; PROJECT.md Phase 6/8 | Braucht bit-exakten Nachbau der OS-9-Kernelstrukturen (Path-Descriptor, Static Storage, System-State-Calls) — lohnt v.a. für RBF (echte OS-9-Disketten lesen!), für FAT ist nativ schneller. Frühestens Phase 6. ACHTUNG Lizenz: MWOS-Module dürfen nicht weitergegeben werden — nur für Andreas' privaten Gebrauch |
| **OS9exec** (Lukas Zeller/Beat Forster, GPL, sourceforge.net/projects/os9exec) als Referenz fuer Phase 6: 68k-Emulation + OS-9-Kernel-Nachbau in C auf Syscall-Ebene (TRAP->Syscall-Bridge, genau Q9s Phase-6-Architektur); Semantik-Nachschlagewerk fuer F$/I$-Randfaelle; evtl. auch RBF-Strukturcode (pruefen, ob Image-Mount unterstuetzt wird oder nur Host-FS-Mapping) | Andreas' Hinweis 2026-07-03 nachts; PROJECT.md Phase 6 | Erst relevant, wenn die Musashi-Runtime ansteht; GPL: Logik verstehen, nicht abschreiben |
| **Kermit als robuster Dateitransfer-Weg** statt/ergänzend zum rohen Paste in `build`: `CMDS/kermit` liegt schon im Image, sc68681-Emulation existiert. Kermit überträgt in kleinen Paketen (~96 Byte) mit ACK je Paket und Prüfsumme — die Bremse steckt schon im Protokoll (Sender wartet auf Antwort, bevor er weiterschickt), kein künstliches Pacing nötig, und da Kermit binär ohne Zeichen-Echo läuft, umgeht es auch den 5.7-Bug (blockierendes THRA-Echo) weitgehend. Bei Übertragungsfehlern automatischer Retransmit statt stillem Datenverlust wie beim rohen Paste. **Ausdrücklich unabhängig von 5.7** — 5.7 (blockierendes `write()` beim Konsolen-Echo) ist die eigentliche Fehlerursache und muss so oder so behoben werden, egal ob Kermit je zum Einsatz kommt | Andreas 2026-07-09, im Nachgang zur Paste-Korruptions-Analyse diskutiert | Noch kein 💡-Vorschlag, weil U.10 (Voraussetzung: Compiler-Pfad + Portabilitätsschicht) Kermit als Transportweg für die Userland-Tools ohnehin schon nennt — erst dort einordnen, wenn der Bedarf konkret wird |
| **Weitere Diagnosewege fuer den 5.15-TCP-Haenger** (OS-9 sendet nach ~2,4 KB keine weiteren `telnetd`-Daten mehr, Host-ACKs mit offenem Fenster kommen weiter): (1) **`netstat -p tcp`** bzw. protokollspezifische Statistik-Kommandos live waehrend des haengenden Zustands abfragen (retransmits/persist-timer-Zaehler koennten in OS-9-eigenen Kernel-Countern sichtbar sein, auch ohne sptcp-Quellcode); (2) **Laenger als 60s warten** (bisher max. ~65s getestet) und pruefen, ob/nach wie vielen Minuten sich die Verbindung tatsaechlich selbst erholt (klaert, ob es ein sehr langsamer, aber funktionierender Timer ist oder ein permanenter Stillstand); (3) **`ftpd`** statt `telnetd` als Vergleichsprotokoll testen (beide nutzen denselben `sptcp`, aber unterschiedliche Anwendungsschicht — grenzt ein, ob der Bug in `sptcp` selbst oder spezifisch in `telnetd` sitzt); (4) **Disassemblierung** von `telnetd`/`sptcp` (`OS9/68030/CMDS/BOOTOBJS/SPF/sptcp`, `OS9/*/CMDS/telnetd`) mit einem 68k-Disassembler (z.B. `objdump`/IDA/Ghidra mit M68K-Modul) — einzig verbleibender Weg an die eigentliche Logik heran, da kein Quellcode existiert; (5) **Aeltere/neuere Modul-Version** pruefen: andere MWOS-SDK-Versionen oder spaetere Microware-Patches (falls im Fundus vorhanden) koennten den Bug bereits beheben, ohne dass wir ihn selbst verstehen muessen; (6) **Verhalten mit `spip` statt `sptcp` direkt** (rohes IP/ICMP ueber laengere Zeit, ohne TCP) als Kontrolltest, um TCP-spezifische vs. allgemeine SPF-Stack-Probleme zu unterscheiden | 5.15, 2026-07-14 | Braucht entweder viel Wartezeit (Langzeittest), ein Disassemblier-Tool das noch nicht im Werkzeugkasten ist, oder zusaetzliches SDK-Material (Patches) das ggf. gar nicht existiert — noch kein 💡-Vorschlag, weil unklar ist, ob sich der Aufwand lohnt oder ob es beim aktuellen Workaround (das betrifft nur `telnetd`/eth0, nicht die produktiv genutzten `/x1-8`-Terminals) bleibt |

---

## ⛔ Geparkt / mit Andreas zu besprechen

- **ROM-Rebuild-Drift (entdeckt beim 5.10-Kontroll-Build, 2026-07-14):** Der Port-Build laeuft fehlerfrei durch, aber das aus den Quellen gebaute `ROMBUG/romimage.dev` unterscheidet sich massiv vom produktiv genutzten `romimage.dev.running.BIN` (das ist ein RAM-Resave vom 10.7.) und **bootet im Emulator nicht** (haengt vor dem Bootstrap-Banner). Vermutlich Folge der ttyS/OX16C954-Experimente in systype.d/ROM_CBOOT. Fuer 5.10/5.6 unkritisch (Laufzeit-Lade-Weg gegen running.BIN), aber der ROM-Build aus Quellen ist derzeit nicht reproduzierbar — mit Andreas klaeren, welcher Stand ins ROM soll. Nebenbefund: der Wrapper `tools/macos/bin/os9merge` ruft faelschlich os9make.exe auf (Copy-Paste-Fehler), merge.exe direkt funktioniert.

- **emsdk fehlt auf dem Desktop AF-PC** (noch unverändert): wasm-Build/Browser-Test dort
  weiterhin nicht möglich. Auf dem Mac Mini seit 2026-07-04 behoben (siehe unten) — betrifft
  jetzt nur noch den Desktop-Rechner, nicht mehr den Autonomie-Betrieb.
- **4.7 Browser-Seite (WebAssembly.instantiate im Worker) bewusst nicht umgesetzt** (2026-07-04):
  ARBEITSPLAN nennt "bzw. WebAssembly.instantiate im Browser" als Alternative zur nativen
  wasm3-Runtime. Umgesetzt wurde nur die native Seite (wasmproc.c) — ein Q9_MOD_WASM-Gastprogramm
  aus dem Worker heraus zu instanziieren (verschachteltes WASM: der Q9-Kernel selbst laeuft im
  Worker schon als WASM, ein Gastmodul muesste per JS `WebAssembly.instantiate` zur Laufzeit
  dazugeladen und dessen Importe per `Module.ccall`-Bruecke zurueck in den Kernel verdrahtet
  werden) ist eine eigene, nicht triviale Design-/Implementierungsaufgabe (worker.js-Aenderungen,
  JS<->WASM-Grenze). Mit Andreas zu besprechen, ob/wann das eigenstaendig eingeplant wird — bis
  dahin liefert `F$Fork`/`F$Chain` auf `Q9_MOD_WASM` im wasm-Build (ohne `-DQ9_HAVE_WASM3`)
  weiterhin `E$NEMod`, exakt wie vor 4.7.

---

## Erledigt

Vollständiger Session-Verlauf (alle abgeschlossenen Schritte im Detail, chronologischer Arbeitslog): siehe `ARBEITSPLAN_ARCHIV.md`.
