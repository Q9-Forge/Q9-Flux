# ToDo-Liste
- [x] 0. Phase 0: Fundament (2026-07-02 komplett, Details in ARBEITSPLAN.md)
- [x] 1. Phase 1: Kernel-Basis (Syscalls, Device-Modell, Konsole) — KOMPLETT 2026-07-03
  - [x] 1.1 Syscall-Design: OS-9-Nummern + Register (E7, docs/SYSCALLS.md)
  - [x] 1.2 Dispatcher + I$Read/Write/ReadLn/WritLn, F$Exit/ID/Time, REPL, Tests
  - [x] 1.3 Device-Modell + Konsolen-Treiber
  - [x] 1.4 I$Dup + I$Close
  - [x] 1.5 F$PrsNam + F$CmpNam (name.c)
  - [x] 1.6 I$Attach + I$Detach (namensbasiert)
  - [x] 1.7 /nil als zweiter Treiber
  - [x] 1.8 I$GetStt/I$SetStt (SS.Ready, SS.EOF)
  - [x] 1.9 Echte Uhrzeit (q9_hal_time, F$Time/F$STime)
  - [x] 1.10 POSIX-HAL (macOS/Linux, termios statt conio)
- [ ] 2. Phase 2: Modulsystem (Header, q9mod, Directory, Loader)
- [ ] 3. Phase 3: Filesystem (VFS + erster FS-Typ)
- [ ] 4. Phase 4: Prozesse (kooperativer Scheduler)
- [ ] 5. Phase 5: Shell
- [ ] 6. Phase 6: 68k-Runtime (Musashi)
- [ ] 7. Phase 7: 68k-Target nativ (Vinculum)
- [ ] 8. Phase 8: Vision (6809-Runtime, Netzwerk, wasm2c, Self-Hosting)

# Letzter Arbeitsstand

Zuletzt bearbeitet: 2026-07-07 (Windows AF-PC, CB030/OS9SYS-CF-Boot).
OS9SYS-CF-Boot funktioniert lokal: `local_images/OS9SYS.hda` bootet mit
`romimage.dev.running.BIN` direkt von CompactFlash, erkennt `OS9Boot`, startet
den CF-Treiber und fuehrt das CF-`startup` aus (`iniz /dd`, `iniz /c0`,
`chx /c0/CMDS`, `chd /c0`, `setenv TERM q9`, `motd`, Prompt).

Start unter Windows:
`.\build\native\q9.exe --cb030 .\local_images\roms\romimage.dev.running.BIN --cf .\local_images\OS9SYS.hda`

Wichtige Erkenntnisse:
- Bootfile-Erzeugung braucht den formatierbaren Descriptor `/c0_fmt`; `/c0`
  ist formatgeschuetzt. Fehler `000:255` = `E$Format`.
- Ein zu kleines/falsch zusammengesetztes `OS9Boot` fuehrt beim Boot zu
  `Sysgo can't chx to 'CMDS'`, `Sysgo can't open 'startup'`, Fehler `000:215`
  = `E$BPNam`.
- Ursache war letztlich ein Schreibfehler in der Bootlist plus falscher
  Descriptor. Mit korrigierter Bootlist und `/c0_fmt` bootet CF.
- Windows-`umacs`-Bedienung funktioniert mit `TERM=q9`: der Windows-HAL mappt
  `_getch()`-Sondertasten (`0xE0` + Scan-Code) auf ANSI-Sequenzen; `termcap`
  nutzt `ku=\E[A`, `kd=\E[B`, `kl=\E[D`, `kr=\E[C`. Direkte `0xE0`/`\340`
  in `termcap` war nicht stabil.
- `SYS/termcap` muss OS-9-CR-Zeilenenden haben; LF fuehrte zu "unknown terminal
  type".
- Nicht gleichzeitig Toolshed und Emulator auf dasselbe `.hda` schreiben.
- Vor Neubuilds alte `q9*.exe`-Prozesse pruefen/killen, sonst wird die EXE
  gesperrt oder ein alter Prozess getestet.

Dokumentiert in `docs/OS9SYS_BOOT.md`, `docs/CB030.md`,
`Q9_CURRENT_STATUS.md`, `docs/HANDBUCH.md`.

## Session 2026-07-17: CF-Partitionen, Offset-Fix und PCF/RBF-Teststand

- Profil `emu.q9`: `c0` RBF eigenes Bootimage, `d0` PCF eigenes FAT16-Image,
  `e0` PCF und `e1` RBF als getrennte Partitionen im gemeinsamen Image.
- `d0`/`e0` beginnen bei Hostsektor 64; `e1` bei `0x200040` (1 GiB + 64).
- Ursache des vorherigen Fehlers eindeutig: der CF-Treiber las/schrieb LSNs
  ohne `PD_LSNOffs`. `tools/cfq9_format.a` wurde in `CF_Read`/`CF_Write`
  korrigiert. `PD_PartSiz` wird weiterhin vom RBF/PCF-Manager anhand des
  Descriptors begrenzt; der Treiber selbst meldet die maximale CF-Größe.
- `cfidef` Edition 43 erfolgreich neu gebaut, CRC/parity gut, ins Testimage
  kopiert. Emulator-Boot danach ohne Descriptor-/Attach-Fehler, `8 devices online`.
- Nach Wiederherstellung des FAT-Anfangs aus `q9-e0-pcf-1gb.img` funktionieren
  `dir /d0` und `dir /e0` inklusive `Q9DIR/INFO.TXT` und `README.TXT`.
- `format /e1_fmt` hatte vor dem Offset-Fix am falschen Ort geschrieben; nicht
  wiederholen, bevor der neue `cfidef` geladen ist. `pcformat -f=3 -np` wird von
  `pcformat` formal abgewiesen, weil Fixed-Disk-Format 3 einen physischen
  Formatpfad verlangt. `dcheck` gilt nur für RBF; PCF wird mit `dir`, `free` und
  Schreib-/Lese-Roundtrip geprüft.
- `dsave` kopiert rekursiv nach RBF (`e1`). PCF/FAT akzeptiert nicht alle langen
  Namen/OS-9-Attribute; Einzeldateien lassen sich mit `copy` übertragen.
- `make test` nach dem Treiber-/Image-Stand: alle sechs Tests PASS.

Zuletzt bearbeitet: 2026-07-03 (dritte Session, Mac Mini, erste Session dort)
PHASE 1.10 (POSIX-HAL) ABGESCHLOSSEN: src/hal/posix/hal_posix.c neu (termios
raw+nonblocking statt conio, clock_gettime statt GetTickCount64, Rest wie
Windows-HAL). Makefile waehlt HAL automatisch per $(OS) (Windows_NT vs. sonst),
PYTHON-Variable erkennt python3/python. Test 01 generalisiert (Target-Check
"native-" statt "native-win64"). make test PASS auf macOS (native-macos),
Build warnungsfrei. Q9 baut damit auf macOS/Linux — Voraussetzung fuer
24/7-Autonomiebetrieb (docs/AUTONOMIE.md) erfuellt. Naechster Schritt:
Phase 2 (Modulsystem, 2.1/2.3a), Details siehe ARBEITSPLAN.md.

PHASE 1 (1.1-1.9) KOMPLETT (Kernel v1.80, zweite Session, Desktop AF-PC).
Andreas hat 1.4-1.9 freigegeben, alle sechs Schritte abgearbeitet, je Schritt
ein Release-Commit (1.30-1.80):
- Device-Modell: Geraete-/Pfadtabelle (IOMan), /term + /nil, Attach/Detach
  per Name (name.c, case-insensitiv), Dup/Close, GetStt/SetStt (SS.Ready/SS.EOF)
- Echte Uhrzeit: q9_hal_time in beiden HALs, Kalenderlogik 2000-2136 im Kernel,
  F$Time (Datum/Zeit gepackt + Wochentag), F$STime mit Validierung
- 22 Selbsttest-Checks, Tests 01-03 PASS (nativ; wasm ungetestet, emsdk fehlt
  auf AF-PC — auf dem Laptop bauen/testen!)
MWOS-ABGLEICH KOMPLETT (M:\MWOS ist auch auf AF-PC gemountet, nicht nur
Laptop): E$Diff war $E2 (Kollision mit E$NoChld) -> E_DIFFER $A5 korrigiert.
SS-Codes waren schon korrekt. F$Time/F$STime hatten d0/d1 vertauscht (echtes
OS-9: d0=Zeit, d1=Datum) -> behoben. Kernel jetzt v2.00. Ausserdem:
docs/SYSCALL_ROADMAP.md (alle 97 OS-9-Syscalls mit Status) und docs/MODULES.md
(Modulsystem-Referenz: Header, Directory, Link-Counting, Sticky-Module) neu.
HEUTE ABEND: Phase-2-Besprechung (Modulsystem, Punkte 2.1-2.4 stehen als Idle,
Design-Fragen in ARBEITSPLAN.md: Modul-Gruppen? File-Manager/Treiber/
Descriptor-Dreiteilung? Details in docs/MODULES.md).
ARBEITSPLAN.md-Statusmodell: 💡 Vorschlag / 💤 Idle / 🟢 Ready / 🔄 / ✅ / ⛔
+ Wer-Spalte. Arbeitsregel 4: nach jedem Schritt sofort commit+push.
Repo: github.com/foellmy51/Q9 (privat). Toolchain: docs/TOOLCHAIN.md
(Laptop User foell + Desktop AF-PC User AF, dort kein emsdk).
Erkenntnis des Tages (1.2): Konsolen-Kanal ist UTF-8-Bytestrom; nackte Bytes >0x7F
sind im Terminal C1-Steuerzeichen (0x90=DCS verschluckt Output) -> TextDecoder-
Streaming im JS-Host, SetConsoleOutputCP(CP_UTF8) im nativen Host.

# Detaillierte Beschreibungen

## 0. Projekt-Gründung (2026-07-02)
Status: Abgeschlossen
- Name Q9 festgelegt (Anlehnung an OS-9, Q bewusst offen)
- Architektur ausdiskutiert: portabler C-Kern, HAL, Dual-Target WASM+68k
- Modulsystem nach OS-9-Vorbild mit Language-Byte (WASM/68k/6809),
  Sync-Bytes "Q9" ($51 $39), CRC, q9mod-Packer
- Andreas' Idee: 68k-Module bekommen automatisch Musashi-Runtime
  untergesetzt; später sogar 6809-Runtime für original OS-9/6809-Binaries
  (Syscall-Semantik von OS-9/6809 und /68k ist weitgehend identisch)
- Offene Entscheidungen O1-O4 siehe PROJECT.md

## Session 2026-07-12 abends (interaktiv mit Andreas): Schritt 5.11 komplett — OS-9 PINGT DEN HOST

Ganzer Bogen an einem Abend: QUICC-Ethernet von null bis zum funktionierenden ping.

### MWOS-Seite (Repo /Volumes/SSD1TB/projects/MWOS, dort NICHT committet — Andreas' Entscheidung ausstehend)
- Original-Microware-SPF-Treiber sp360 + Descriptor spqe0 aus OS9/CPU32/PORTS/QUADS
  in den Q9-Port uebernommen (OS9/68030/PORTS/Q9/SPF/), fuer 68020/030 uebersetzt
  (ALL_TRGTS k020, gendefs.h-Includepfad ergaenzt), beide Module CRC Good
- Komplette SPF-Infrastruktur: ETC (interfaces.conf: enet0=10.0.0.1/24,
  binding /spqe0/enet, hostname q9 -> steckt in inetdb2!), neues Bootflavor
  net.bl (98 Module, 838 KB — passt NICHT ins 512K-ROM, gedacht fuer os9gen
  in den CF-Bootbereich), startspf-Vorlage, CHANGELOG/context.txt gepflegt
- Adressentscheidung: QUICC-Basis $FFFF2000 (8K, kollisionsfrei), PORTADDR
  $FFFF2C00 im spqe0 (Treiber maskiert & $FFFFF000, Kanalbits 8-9 -> SCC1)

### Q9-Seite (committet + gepusht: a43f804, d94cd81, e42361e)
- src/kernel/quicc.c/.h NEU: 8K-Fenster (DPRAM/PRAM/Register, Offsets per
  offsetof aus Motorolas quicc.h verifiziert: PRAM +$C00, CR +$15C0, SCC1
  +$1600), CP-Kommandos synchron, TX/RX-BD-Ringe mit tbptr/rbptr-Fort-
  schreibung, SDMA = direkte Gast-RAM-Zugriffe, SCCE/SCCM/CIPR/CIMR-Pegel,
  IRQ Level 5 / Vektor 254; Mini-NAT-Backend: Proxy-ARP + ICMP-Echo als
  Gegenstelle 10.0.0.2 (User-Mode, kein Root/TAP)
- m68krt.c: QUICC-Dispatch in allen 6 Speicher-Hooks + IACK-Zweig Level 5;
  cb030run.c: Instanz/attach/poll/IRQ; Makefile: quicc.c im CB030_SRC
- Host-Rauchtest (BD-Mechanik+ARP) UND End-to-End: test_quicc_net.exp
  (Klon-Image OS9SYS.claudia-net-test.hda, netmods-Merge mit 23 SPF-Modulen
  via ToolShed): load netmods + mbinstall + ipstart, ping 10.0.0.2 ->
  "64 bytes from 10.0.0.2: ttl=255 time=0 ms", netstat: enet0 UP, 0 Fehler
- make test PASS, make native warnungsfrei; HANDBUCH: Abschnitt 3 + neu 5.11

### Erkenntnisse (dokumentiert in ARBEITSPLAN 5.11 + startspf)
1. sysmbuf = Coldstart-Systemmodul (Typ $C): beim Laufzeit-Laden MUSS
   mbinstall vor ipstart laufen ("sysmbuf is not installed" sonst)
2. ToolShed-Kopien brauchen attr -e -pe, sonst Error 214 beim load
3. dev.bf war mit 511.970 Bytes exakt an der 512K-ROM-Kante — deshalb
   Netzwerk als eigenes net-Flavor, dev.bl unangetastet

### Offen / naechste Schritte
- Stuendliche Routine (q9-arbeitsplan) ist PAUSIERT (Andreas' Wunsch,
  vor Wiederaktivierung fragen bzw. auf Zuruf)
- Ausbaustufe TCP/UDP-NAT im Backend (telnet/ftp Gast<->Host) — bei
  Bedarf als neuer 💡-Vorschlag
- MWOS-Repo: Q9-Port-Aenderungen sind unversioniert (ganzer Ordner
  untracked) — Committen ist Andreas' Entscheidung
- net.bf via os9gen in den CF-Bootbereich = Andreas' Image-Pflege

## Session 2026-07-13 nachts (Claudia + Andreas, interaktiv): 5.12 vmnet — OS-9 IM INTERNET

Vom Netz-Setup bis zum Internet in einer Nacht-Session:
1. Vorspiel: Produktiv-Image OS9SYS.hda netz-eingerichtet (netmods,
   /dd/ETC, SYS/startspf) + Netz-AUTOSTART. Zwei Fallen dokumentiert:
   os9 copy braucht -l fuer Textdateien (LF->CR); das Boot-Skript ist
   /dd/SYS/startup, NICHT /dd/startup (Legacy).
2. Release 1.63: --net vmnet (vmnet.framework Shared Mode, Gateway
   10.0.0.2 = alte Mini-NAT-Adresse, MAC-Uebersetzung Gast<->vmnet).
3. MWOS: netdb_dns statt netdb_local (beide = Modul "netdb"!), lokale
   resolv.conf (idbgen crasht ohne domain-Zeile!) + routes.conf,
   netmods v2. Details in MWOS/context.txt Teil 4+5.
4. ERGEBNIS (sudo-Test Andreas): ping 8.8.8.8 raus, ping heise.de mit
   DNS-Aufloesung, ping vom Mac zum Gast 10.0.0.1 (3/3, ~1.4ms) —
   raus UND rein. ARBEITSPLAN 5.12 abgehakt (Commit a0030e7).
Nebenfund: alter codex-q9-screen-Emulator lief seit Freitag auf
DEMSELBEN CF-Image — beendet; nie zwei Emulatoren auf ein Image.
Offen als Idee: telnetd/ftpd aktivieren (Mac -> OS-9 einloggen).

## Session 2026-07-13 nachmittags: 5.13 Bridge-Backend + Adressumstellung

Bridge-Backend per BPF (--net bridge:<ifname>) vorbereitet, s. ARBEITSPLAN 5.13
(Code steht, Test mit echter zweiter physischer NIC steht noch aus). Dabei
Adressierung ueberall auf 192.168.200.2 (Gast) / 192.168.200.1 (Gegenstelle,
vorher 10.0.0.1/10.0.0.2), Subnetz auf /16 umgestellt — betrifft quicc.c/.h,
vmnet_net.c/.h, test_quicc_net.exp sowie MWOS Q9-Port SPF/ETC/*.conf. Alle
10.0.0.x-Erwaehnungen weiter oben in diesem Log spiegeln den historischen
Stand vor dieser Umstellung. Rechner ist waehrend dieser Session abgestuerzt,
working directory war komplett uncommitted, nichts verloren.

## Session 2026-07-14 (Claudia, autonom): 5.17 Geraete-Registry — alle sechs Geraete umgezogen

Groesster/riskantester ARBEITSPLAN-Punkt (5.17) komplett abgeschlossen, s.
Releases 1.68-1.73. Neue Datei src/kernel/devreg.c/.h (Entscheidung E14):
q9_device_t/Vtable (read8/write8 Pflicht, read16/32/write16/32 optional —
NULL wird aus Byte-Zugriffen synthetisiert, CF+QUICC behalten eigene Pfade),
level_held-Flag (level-gehaltene IRQ-Quellen vs. einmaliger Timer-Puls),
irq_vector_fn-Hook (DUART/nettty haben laufzeit-/pro-Kanal-dynamische
Vektoren). Statische Instanz-Registry + explizite Typ-Registry (KEINE
__attribute__((constructor))-Magie). Alle sechs Bestandsgeraete (68681-DUART,
Compact-Flash, Timer/IRQ3, RTC72421, Netz-Terminals /x1..x8, QUICC) einzeln
umgezogen, je mit eigenem Commit + Boot-Test (Klon-Image
local_images/OS9SYS.claudia-517-devreg.hda, lokale Konsole + Port 2000 +
QUICC-Ping). cb030_read_byte/cb030_write_byte (cb030.c) kennen danach nur
noch REMAP-Trigger + RAM/ROM. make test PASS nach jedem Schritt, make native
durchgehend warnungsfrei. Zwei dokumentierte, bewusst in Kauf genommene
Nebeneffekte ohne Funktionsverlust (Reassert-Tie-Break-Reihenfolge bei
gleichzeitigen Interrupts, minimale Idle-Sleep-Fensteraenderung durch
generische nettty-Abfrage im Hauptschleifen-Poll) — Details in ARBEITSPLAN.md
5.17-Notiz. Doku: docs/HANDBUCH.md Abschnitt 5.12 (neu), PROJECT.md E14.
Grundlage fuer 5.18 (Binaersuche-Dispatch + Benchmark, naechster geplanter
Schritt, aktuell noch 💤 Idle bis Andreas freigibt).

## Session 2026-07-16 (Claude + Andreas, interaktiv): 5.19a — Board-Config-Datei + mehrere CF-Images

Andreas' Auftrag: erster Positionsparameter (ohne "-") = Config-Datei (Extension
".q9" default), darin Emulator-Config MIT mehreren Imagedateien vom Typ rbf oder
pcf (fat12/16); Optionen bleiben; weitere rbf/fat-Images erzeugen (funktionierende
Buffergroesse = 512 B/Sektor).

### Umgesetzt
- src/kernel/boardcfg.c/.h NEU: INI-Parser (C99, kein Fremdmodul), [board]
  name/rom/net + beliebig viele [cfN] (type=rbf|pcf, bus=onboard|rc2014,
  unit=master|slave, image=). Pfade relativ zur Config-Datei. Extension-Regel
  (.q9), Kollisionspruefung (zwei Images auf derselber Einheit -> Fehler).
- CF-Emulation mehrfach instanziierbar (cb030.c/.h Ver. 2.00): CF-Zustand aus
  q9_cb030_t in neuen Typ q9_cf_t/q9_cf_unit_t gezogen. Zwei Interfaces:
  Onboard $FFFFE000 (c0..c3) + RC2014-SC145 $FFFFC010 (e0/f0), Master/Slave ueber
  DEV-Bit (Bit 4) in LBA3 ($E0/$F0). Beide nutzen q9_devtype_cf; Registeroffset
  ueber dev->base. type=pcf schaltet die RBF-256-Byte-Heuristik ab.
- m68krt.c: q9_m68krt_attach_cf2() registriert das RC2014-Interface (nur wenn die
  Config dort Images anhaengt). cb030run.c (Ver. 1.70): q9_cb030_boot() bekommt
  cfg-Parameter, verteilt CF-Images, Vorrang Default<Config<CLI.
- hal_posix.c/hal_native.c: neue main()-Argumentparser (erster Non-"-"-Param =
  Config, .q9-Default), alte Optionen (--cb030/--cf/--net/--selftest) unveraendert.
- tools/make_fat_image.py NEU: FAT12/16-Superfloppy-Generator (512 B/Sektor).
  local_images/q9data-rbf.img (ToolShed os9 format -bs512 -c32), q9-fat12.img,
  q9-fat16.img erzeugt (lokal, gitignored).
- docs/q9board.example.q9 (Beispiel), docs/HANDBUCH.md Abschnitt 5.13 (neu, Ver.
  2.10), ARBEITSPLAN 5.19 auf 🟡 (Teil 5.19a fertig, Rest 💤).

### Verifiziert
- make test PASS (neuer Selbsttest "5.19: Board-Config-Parser"), make native
  warnungsfrei (nur bekannte Alignment-Warnung).
- Config-gesteuerter Boot des echten OS9SYS-Klons bis Login (super) + dir /dd +
  Logout (test_519_config_boot.exp, APFS-Klon, danach geloescht).
- Alter CLI-Weg --cb030 <rom> --cf <img> byte-identisch (gegengetestet).

### Offen (Rest von 5.19, weiter 💤, haengt an 5.18)
Speicher-/Geraete-Abschnitte (type=ram|rom, [uart0]/[eth0]...), Instanziierung
ALLER Gerraetetypen ueber die Typ-Registry, Fensterueberlappungs-/Vektor-
kollisions-Validierung. Bis dahin Nicht-CF-Geraete hartkodiert wie in 5.17.

## Session 2026-07-23 (Codex + Andreas): vmnet-Remotezugriff / historischer Netzwerkstand

### Ergebnis
- Das aktuelle OS-9-Image zeigt nach Boot bereits korrekt `192.168.200.2 netmask 0xffffff00` (/24). Der Versuch, per `idbgen` erneut `inetdb/inetdb2` zu erzeugen, war nicht noetig und wurde zurueckgerollt: der Lauf erzeugte zwar Dateien, aber `fixmod` im Image war inkompatibel ("cannot execute binary file"), sodass `loadidb` danach die Module nicht laden konnte. Das Produktivimage wurde aus `local_images/OS9SYS.hda.backup-vor-netmask-20260723` wiederhergestellt.
- Proxmox ohne Route versucht `192.168.200.2` direkt auf `vmbr0` per ARP zu finden; `arp` zeigte `192.168.200.2 (unvollstaendig)`. Die Mac-LAN-Adresse `192.168.123.152` war dagegen mit MAC bekannt. Die temporaere Route `ip route add 192.168.200.0/24 via 192.168.123.152 dev vmbr0` macht Ping und Telnet sofort funktionsfaehig. Der Telnet-Haenger ist davon getrennt und laut Andreas im aktuellen Image behoben.
- Der alte `OS9SYS.claudia-net-test.hda`-Stand verwendet `10.0.0.1/24`; Mac-lokaler TCP-Test zu `10.0.0.1:23` war erfolgreich, ist aber fuer den Proxmox-Test nicht relevant. Ein aelterer `192.168.200.2`-Stand wurde ebenfalls ohne Route getestet: gleiche ARP-/Routing-Situation.

### Wichtige neue Spur: Host-Endpunkt von vmnet
- `bridge100` des Macs ist aktuell `192.168.200.1/24`, Mitglied `vmenet0`. Ein Versuch, vmnet temporaer mit Host-Endpunkt `192.168.200.2` zu starten, scheiterte mit Status 1009, weil das bestehende `bridge100` noch mit `.1` aktiv war. Das ist kein Gegenbeweis gegen den historischen `.2`-Aufbau; fuer einen echten Wechsel muss vmnet vollstaendig zurueckgesetzt werden.
- Der interne Q9-Terminalserver `/x1..x8` ist ein Host-TCP-Server auf Port 2000 (`INADDR_ANY`), getrennt vom OS-9-QUICC/Telnetdienst auf Port 23. Andreas hat ihn historisch ueber `192.168.200.2:2000` angesprochen. Das deutet darauf hin, dass frueher der vmnet-Host-Endpunkt bzw. eine Host-Portweiterleitung anders war. Naechster gezielter Test: nach Beendigung der anderen Claude-Session und einem passenden Mac-/vmnet-Reset den Host-Endpunkt `.2` reproduzieren und `192.168.200.2:2000` vom Proxmox pruefen. Bis dahin keinen Mac-Neustart machen.

### Aktueller Arbeitsstand
- `emu.q9` zeigt derzeit auf den archivierten alten Stand `local_images/archive_2026-07-22/OS9SYS.pks_telnetdc___mit_patch_fuer_telnet_Blockade.hda`.
- Fuer vmnet ist Root erforderlich; eine fehlende Passwortabfrage kann nur der macOS-`sudo`-Credential-Cache sein. Mit `sudo -k ...` laesst sich eine Abfrage erzwingen.
- Neue `.q9`-Keys sind im Q9-Code umgesetzt: `vmnet_ip`, `vmnet_gateway`, `vmnet_netmask`, `vmnet_dhcp_end`. `vmnet_ip` dokumentiert/zeigt die Gast-IP; es aendert die OS-9-IP im Image nicht automatisch.
- Keine weiteren Images anfassen. Kein Mac-Neustart, solange die andere Claude-Session laeuft. Fuer den naechsten Termin am privaten Windows-PC `ipconfig` und `route print` vergleichen.

## Session 2026-08-06 (Claudia + Andreas): nativer Windows-Build (`make native`)

### Ausgangslage
Repo frisch von Q9-Forge/Q9-Flux geklont (`C:\projects\Q9-Forge\Q9-Flux`), w64devkit
bereits lokal unter `C:\Users\foell\w64devkit` vorhanden. Makefile hatte die
Windows/POSIX-HAL-Weiche fuer `NATIVE_HAL_SRC` schon (`$(OS)`-Check), aber
`make native` brach in m68krt.c/videobridge.c beim Compile ab: `sys/socket.h` &
co. sind POSIX-only, unter Windows gibt's stattdessen Winsock2.

### Aenderung
- NEU `src/kernel/q9_sockcompat.h`: duenner Portabilitaets-Shim (nur Makros +
  `q9_sock_startup()`/`q9_sock_cleanup()`) -- Windows-Zweig bindet winsock2.h/
  ws2tcpip.h ein und mapped `Q9_SOCK_CLOSE`->closesocket, `Q9_SOCK_NONBLOCK`->
  ioctlsocket(FIONBIO), `Q9_SOCK_WOULDBLOCK`->WSAGetLastError()==WSAEWOULDBLOCK,
  `Q9_SOCK_IGNORE_SIGPIPE`->no-op (Windows kennt kein SIGPIPE fuer Sockets).
  POSIX-Zweig mappt 1:1 auf close/fcntl/errno/signal wie vorher.
- m68krt.c (OS-9-Netz-Terminals /x1../x8, Port 2000) und videobridge.c
  (Framebuffer-Streaming) benutzen jetzt diese Makros statt direkter POSIX-Calls;
  `write()`/`read()` auf Socket-fds durch `send()`/`recv()` ersetzt (unter Windows
  funktionieren CRT-`read`/`write` nicht auf SOCKET-Handles); `setsockopt()`-
  Aufrufe auf `(const char *)`-Casts umgestellt (Winsock-Signatur, auf POSIX
  unschaedlich).
- Makefile: neue Variable `NATIVE_NET_LIBS` (`-lws2_32` nur unter
  `$(OS)==Windows_NT`), ans `native`-Link-Kommando angehaengt.

### Verifiziert (lokal, w64devkit x86_64-w64-mingw32, gcc 16.1.0)
- `make native` baut warnungsfrei durch (nur zwei bereits vorher bestehende,
  Windows-unabhaengige Warnings in m68krt.c/quicc.c).
- `make test` (Sektor-Roundtrip-Selbsttest) PASS, 0/30 Checks fehlgeschlagen.
- `q9.exe --cb030 <synth. 4-Byte-Test-ROM>` bootet, `[OS-9 Net]`-Server oeffnet
  TCP+UDP 0.0.0.0:2000 wirklich (per `netstat` und per echtem `/dev/tcp`-Connect
  gegenverifiziert -- `accept()`/`recv()`/`send()` laufen sauber gegen Winsock).
- `q9.exe.exe` ist ein PE32+ x86_64-Windows-Executable.

### Offen / naechste Schritte
- vmnet/bridge-Ethernet-Backends bleiben macOS-only (Makefile-Guard
  `uname -s == Darwin`) -- unter Windows meldet `--net vmnet|bridge:<if>` sich
  sauber ab, kein Portierungsversuch unternommen (bewusst ausserhalb des
  aktuellen Auftrags).
- Kein `--net nat`-Backend unter Windows getestet (kein Board-Boot mit echtem
  ROM zur Hand in dieser Session, nur Ping des Terminal-Servers).

## Session 2026-08-07 (Claudia + Andreas): Windows-Feinschliff + slirp-Netzwerk-Backend

### Bugfixes (alle ausgeloest durch Andreas' interaktives Testen)
- **Doppel-Echo in Telnet** (m68krt.c 1.38): Server verhandelte gar kein Telnet --
  IAC WILL ECHO/SUPPRESS-GA bei Connect + telnet_filter_byte() filtert IAC-Antworten
  des Clients raus.
- **Performance** (hal_native.c 1.21): `timeBeginPeriod(1)` gegen Windows' 15.6ms-
  Timer-Aufloesung, die den CPU-Idle-Pfad (`q9_hal_sleep_ms(1)`) effektiv 15x
  langsamer machte als auf macOS/Linux.
- **Host-Escape** (hal_native.c 1.23): Ctrl-] ging auf Andreas' DE-Tastatur nicht
  (AltGr-Kollision), F10 kollidierte mit einem Snip-Tool, F1-F10 kamen generell nicht
  an -- jetzt Ctrl-Q (Default) + F10/F9 als Alternativen, alle per Env-Var
  (Q9_QUIT_CTRL/Q9_QUIT_SCAN/Q9_DEBUGDUMP_SCAN) konfigurierbar.
- **Telnet-Session einzeln trennen** (m68krt.c 1.39): Ctrl-Q (Default, Q9_NET_DISCONNECT_CTRL)
  trennt nur die eigene Verbindung, nicht den ganzen Emulator -- Pendant zum lokalen
  Host-Escape mit Kanal- statt Prozess-Reichweite.
- **Ctrl-C-Reset-Bug** (NICHT gefixt, zurueckgestellt): rohes Ctrl-C (0x03) an OS-9
  fuehrt zu "system state exception" + automatischem System-Reset, der anschliessende
  Reboot haengt (CF-Treiber-Init kommt nicht durch). Pure CB030/OS-9-Emulationslogik,
  nicht Windows-spezifisch, nicht angefasst.

### Neu: slirp-Netzwerk-Backend (`net = slirp`, plattformuebergreifend)
Andreas' Wunsch nach echter IP-Verbindung ohne physische Netzwerkschnittstelle
(vmnet ist macOS-only). Gewaehlte Loesung: `libslirp` (dieselbe Bibliothek wie
QEMU/VirtualBox "User-Mode-Networking") -- neues Backend `src/kernel/slirp_net.c`/`.h`,
Dispatch in `quicc.c`/`quicc.h` (`Q9_NET_SLIRP`), Config-Key `net_hostfwd` in
`boardcfg.h`/`.c` (aktuell geparst, aber s.u. nicht angewendet).

**Windows-Vendoring** (`third_party/slirp/windows/`, s. dortige `Q9_VENDOR.md`):
libslirp+glib2 (+3 Transitiv-DLLs) direkt aus den MSYS2-Mirror-Paketen als rohe
`.pkg.tar.zst`-Downloads extrahiert (python `zstandard`-Modul zum Entpacken, kein
zstd-Tool noetig) -- explizit KEIN MSYS2/Cygwin als Build-Voraussetzung (Andreas:
"andere wollen das Projekt auch mal bauen"), w64devkit allein reicht. Per `objdump`
verifiziert: nur 5 DLLs tatsaechlich noetig (~3.8 MB), nicht die von den Paket-
Metadaten grosszuegig deklarierte volle Kette (kein libffi/zlib/gcc-libs/winpthread).
macOS/Linux: kein Vendoring, `Q9_HAVE_SLIRP` ueber System-`libslirp` per pkg-config.

**Zwei Absturz-Bugs in dieser libslirp/glib2-Kombination gefunden + umschifft**
(beide per `gdb` isoliert, Makefile `Ver. 3.30`, `slirp_net.c` `Ver. 1.02`):
1. `slirp_add_hostfwd()` stuerzt IMMER ab (reproduziert auch in einem Standalone-Test
   ohne jeden Q9-Code) -- hostfwd deshalb vorerst deaktiviert (nur Warnung, kein Call).
2. `slirp_pollfds_poll()` stuerzt ab bei 0 registrierten Sockets -- NUR im echten
   q9.exe reproduzierbar, nicht in Standalone-Tests (uninitialisierter Speicher?).
   Fix: Aufruf nur noch bei `g_pollfd_count > 0`.

**Status:** Kernfunktion (ausgehende Verbindungen, OS-9 -> Internet) stabil
verifiziert -- kompletter OS-9-Boot bis "8 devices online" mit `net=slirp` aktiv,
`make test` PASS. Port-Weiterleitung (`net_hostfwd`, fuer eingehenden Zugriff z.B.
auf OS-9s telnetd von aussen) funktioniert NICHT, braucht weitere Untersuchung
(andere libslirp-Version? Upstream-Bug?) -- s. Q9_VENDOR.md fuer Details.

### Offen / naechste Schritte
- hostfwd-Bug in libslirp weiter untersuchen (evtl. andere Version testen, oder
  Upstream-Issue melden).
- `net=slirp` auf Mac/Linux noch nicht getestet (Code ist plattformneutral, aber
  System-libslirp-Pfad ueber pkg-config unverifiziert).
- emu.q9 steht inzwischen auf `net=slirp` (Andreas' Wunsch, spaeter am selben Abend) --
  nat/vmnet bleiben als Kommentare erreichbar.

## Session 2026-08-07 (Nacht, Claudia autonom): hostfwd-Absturz tiefer untersucht -- nicht geloest

Andreas bat um Weiterverfolgung, bis Telnet zu OS-9s echtem `telnetd` (192.168.200.2:23)
klappt, dann Feierabend ("bau dir gerne eine Loop"). Ergebnis: **hostfwd bleibt kaputt**,
trotz mehrstuendiger vertiefter Untersuchung. Alle Details in
`third_party/slirp/Q9_VENDOR.md` (Abschnitt "Vertiefte Untersuchung"), Kurzfassung:

- `slirp_add_hostxfwd()` (Alternativ-API) crasht am IDENTISCHEN Codepunkt wie
  `slirp_add_hostfwd()` -- kein Umweg moeglich.
- Andere libslirp-Version (4.8.0) laedt gar nicht gegen die vorhandene glib2-Version
  (ABI-Drift zwischen unabhaengigen MSYS2-Paketversionen).
- Quellcode-Nachverfolgung (tcp_listen/socreate/tcp_newtcpcb) zeigt KEINE
  Callback-Aufrufe im fraglichen Codepfad -- die Disassemblierung zeigt trotzdem einen
  Sprung durch einen ungueltigen Funktionszeiger, der eher nach GLibs EIGENER
  Windows-Fehlerbehandlung (Vectored Exception Handler, `G_VEH_CATCH`) aussieht als
  nach einem simplen NULL-Callback.
- Versuch, mit einem selbst gebauten Debug-Build (echte Symbole) einen sauberen
  Stacktrace zu bekommen: MSYS2 nur TEMPORAER installiert (nicht Projekt-Abhaengigkeit),
  scheiterte an Windows-GPG/pacman-Automatisierung (`pacman-key --init` haengte fest,
  `--noconfirm` reagierte nicht zuverlaessig auf gepipte Eingaben ueber die
  Git-Bash<->MSYS2-Grenze) -- abgebrochen, BEVOR der eigentliche Debug-Build begann.
  `C:\msys64` liegt noch auf der Maschine (unvollstaendig/kaputter Keyring), kann
  aufgeraeumt oder fuer einen saubereren zweiten Anlauf wiederverwendet werden.

**Empfehlung fuer naechsten Anlauf:** Upstream-Bugreport bei libslirp (haben echte
Debug-Symbole, kennen den Code) ist vermutlich schneller als ein eigener Debug-Build.
Alternativ: hand-rolled TCP-Relay in Q9 selbst (kein libslirp-hostfwd) -- machbar, aber
vom Umfang vergleichbar mit dem, was tcp_listen() intern selbst tut.

**Kernfunktion (ausgehende Verbindungen) bleibt unangetastet und stabil** -- kein
Regressionsrisiko durch diese Nacht-Session, nur weitere Diagnose ohne Code-Aenderung
an slirp_net.c (der Fix von vorhin -- g_pollfd_count>0-Guard -- bleibt unveraendert).

**2026-08-07, spaeter (auf Andreas' Bitte "dokumentiere alles"):** third_party/slirp/
Q9_VENDOR.md um einen "🔴 RESUME HERE"-Abschnitt ganz oben ergaenzt -- konkrete
naechste Kommandos, korrigierter Befund (Keyring war schon gueltig, pacman-key --init
vermutlich unnoetig gewesen), bekannte pacman-Prompt-Tuecken (Gruppenauswahl will
Leerzeile, Bestaetigung will "j", Antwortdatei nur ueber MSYS2s eigene bash anlegen).
Reproducer als third_party/slirp/repro_hostfwd_crash.c ins Repo gelegt (gebaut+verifiziert
crasht wie dokumentiert) -- muss beim naechsten Anlauf nicht neu geschrieben werden.

## Session 2026-08-07 (Abend, Claudia): hostfwd-Absturz GELOEST -- zwei eigene Bugs, kein libslirp-Bug

Andreas' Freigabe: "mach da weiter wo du aufgehoert hast". RESUME-HERE-Plan aus der
Nacht-Session befolgt -- MSYS2-Toolchain (gcc/gdb/meson/ninja/pkgconf/glib2/git) ohne
`pacman-key --init` installiert, lief diesmal glatt durch (Befund von der Nacht-Session
bestaetigt: Keyring war schon gueltig, der Init-Schritt war die eigentliche Bremse).
libslirp v4.9.3 aus dem Quellcode mit `meson --buildtype=debug` gebaut (identische Version
wie vendort, damit 1:1 vergleichbar), Reproducer dagegen gelinkt, unter `gdb -batch -ex run
-ex bt` laufen lassen -- Absturz sofort mit vollem symbolisiertem Backtrace reproduziert.

**BUG 1 (Ursache des seit der Nacht-Session dokumentierten Absturzes):** Eigener
Konfigurationsfehler, kein libslirp-Bug. `slirp_register_poll_socket()` (src/slirp.c)
ruft `cb->register_poll_socket()` nur wenn `cfg_version >= 6` ist -- sonst faellt es in
den deprecated `cb->register_poll_fd()`-Zweig, den wir absichtlich NULL gelassen hatten
(laut Header nur die neuere _socket-Variante noetig). Mit `cfg.version = 1` (Stand seit
5.14 "Erster Wurf") griff also immer der falsche, NULL-Zeiger-Zweig. Fix: `cfg.version = 6`
in `slirp_net.c` (Ver. 1.03).

**BUG 2 (nach Fix 1 per gdb neu aufgedeckt, echter Speicherfehler seit 1.00 latent):**
`SlirpCb cb;` war eine LOKALE Stack-Variable in `q9_slirp_start()`. `slirp_new()` speichert
davon aber nur den Zeiger ("slirp->cb = callbacks;"), keine Kopie -- nach Rueckkehr der
Funktion ist der Zeiger dangling. War nie aufgefallen, weil `slirp_pollfds_poll()`
(einziger Dereferenzierer aus dem Hauptloop) nur bei `g_pollfd_count > 0` laeuft, und ohne
hostfwd/aktive Verbindung war das nie der Fall -- der Bug also inert. Erst der durch Fix 1
aktivierte hostfwd-Listener-Socket machte `g_pollfd_count > 0` schon beim ersten Poll und
liess `slirp->cb->clock_get_ns()` auf eine laengst wiederverwendete Stack-Adresse springen
(Crash-Adresse 0x5fe3b8, kein Modul -- klassisches Symptom). Fix: `SlirpCb` als `static
g_cb` (Datei-Lebensdauer, passt zum ohnehin Singleton-artigen `g_slirp`) in `slirp_net.c`
(Ver. 1.04).

**Verifiziert (echter q9.exe, vendorte Windows-Libs, nicht nur Debug-Build):**
- `make native` + `make test` sauber (0/30 Checks fehlgeschlagen)
- Kompletter OS-9-Boot bis "8 devices online" mit `net=slirp`, hostfwd aktiv
- `netstat` zeigt Port 2323 wirklich im LISTEN-Zustand
- Eingehende TCP-Verbindung von aussen auf Port 2323 crasht den Emulator NICHT mehr
  (lief 5+ Sekunden stabil weiter) -- Kernziel der Nacht-Session erreicht
- OS-9s eigener `telnetd`-Dienst antwortete auf der Testverbindung noch nicht (kein
  Banner) -- das ist jetzt ein OS-9-seitiges Konfigurationsthema (Dienst evtl. nicht
  automatisch gestartet), KEIN slirp/hostfwd-Bug mehr. Naechster Schritt bei Bedarf:
  pruefen, ob/wie `telnetd` in OS-9 (netmods/startspf) tatsaechlich gestartet wird.

`emu.q9`: `net_hostfwd = tcp:2323:23` wieder aktiviert (war auskommentiert).
`third_party/slirp/repro_hostfwd_crash.c` bleibt im Repo (per Reproducer-Kommentar auf
`cfg.version = 6` korrigierbar/nachvollziehbar, urspruengliches Crash-Verhalten mit
`cfg.version = 1` weiterhin als Gegentest reproduzierbar).

**Commit:** 610cca2 "5.14: slirp-Netzwerk-Backend + Windows-Feinschliff" -- buendelt den
gesamten Wochenend-Stand (Telnet/Perf/Host-Escape-Fixes der fruehen Session + slirp-Backend
+ beide hostfwd-Bugfixes), auf Andreas' Wunsch als EIN Commit, gepusht nach
github.com/Q9-Forge/Q9-Flux (main). `.gitignore`: `.q9-flux/` ergaenzt (mehrere GB grosse
Boot-/Testimages, waren aus Versehen fast mitgestaged -- gehoeren nie ins Repository).

**Naechste Schritte:**
- OS-9-telnetd-Start pruefen (netmods/startspf durchsehen, ob/wie der Dienst hochkommt)
- `net=slirp` auf Mac/Linux weiterhin ungetestet (Code plattformneutral, System-libslirp
  ueber pkg-config unverifiziert)
- third_party/slirp/windows_debug/ (temporaeres Debug-Vendoring fuer diese Session) wurde
  wieder geloescht, nicht committet -- bei Bedarf reproduzierbar per third_party/slirp/
  Q9_VENDOR.md "RESUME HERE"-Anleitung (MSYS2-Toolchain ist jetzt aber schon installiert,
  C:\msys64, dieser Schritt entfaellt beim naechsten Mal)
